node_modules/
dist/
data/
.turbo/
*.tsbuildinfo

# Cache narzędzi testowych
.vitest/
test-results/
playwright-report/

# Katalogi danych suit przeglądarkowych (własne bazy, własne porty)
.e2e-data/
.e2e-scripted*/

# Licznik tur subskrypcji wydanych przez spece modelowe. Kopia robocza, nie
# dowód: rejestr zamkniętego grantu leży w docs/evidence/ i nikt go nie nadpisuje.
.e2e-model-turns/

# Kopie stanu lokalnego — zawierają PRAWDZIWE dane użytkownika (rozmowy,
# kompozycje, pliki). Nie mogą trafić do repozytorium ani do obrazu.
backups/

.env
.env.*

# Lokalne ustawienia narzędzi agentowych i logi
.claude/settings.local.json
*.log
.swap-data/
