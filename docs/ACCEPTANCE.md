# Macierz odbioru szablonu

> Plik generowany przez `node scripts/acceptance-matrix.mjs` z `docs/ARCHITECTURE.md` (wymagania) i
> `docs/acceptance/assessment.json` (oceny). Nie edytuj ręcznie — `pnpm check:acceptance` wykrywa dryf.

**Stan kodu ocenianego:** commit d142b85 (kod z 7d6bd78 + przeniesione poprawki AgenticApp z 2026-09-17: ui_filter i zawężanie w adresie, alwaysLoad, wyszukiwanie "*" z totals; poza tym kod identyczny z AgenticApp, różnice wyłącznie narzędzi szablonu — docs/CONSOLIDATION-UPDATE-2026-09-17.md); regresja w czystej kopii d142b85: pnpm verify 278/278, pnpm test:e2e 72/72 (cztery tury na prawdziwym modelu), sonda ui_filter na prawdziwym modelu, pnpm check:module-swap, start produkcyjny — docs/evidence/template-update-2026-09-17/przebieg-d142b85/. Oceny z 2026-09-16 nieobjęte zmianą kodu pozostają bez zmian (regresja nie oblała żadnego testu, na którym się opierały).  
**Data oceny:** 2026-09-17 (aktualizacja ocen z 2026-09-16)  
**Charakter:** ocena różnic dla bazowej wersji szablonu (faza konsolidacji). Oceny: analiza kodu i testów szablonu wobec treści kryteriów, z historyczną oceną wykonawcy AgenticApp jako punktem wyjścia, skorygowana o wyniki regresji. To nie jest odbiór 200 kryteriów.

## Podsumowanie (wyliczone)

Kryteriów w specyfikacji: **200** w 12 warstwach (13+17+18+15+17+17+13+14+16+19+24+17); prób odbiorowych: **27**.

| Stan w szablonie | Liczba |
|---|---|
| potwierdzone | 59 |
| częściowe | 118 |
| niespełnione | 15 |
| niesprawdzone | 8 |
| **Razem** | **200** |

| Rodzaj dowodu | Liczba |
|---|---|
| test kontraktu lub logiki | 56 |
| test GUI bez modelu | 52 |
| rzeczywisty model | 47 |
| analiza kodu | 28 |
| — | 11 |
| symulacja | 6 |

| Pochodzenie dowodu | Liczba |
|---|---|
| szablon | 189 |
| — | 11 |

| Ostatnia ocena w AgenticApp (historyczna) | Liczba |
|---|---|
| brak oceny (kryterium spoza 95) | 105 |
| potwierdzone | 95 |

| Stan prób odbiorowych w szablonie | Liczba |
|---|---|
| potwierdzone | 1 |
| częściowe | 22 |
| niespełnione | 4 |
| niesprawdzone | 0 |
| **Razem** | **27** |

**Warstwy zamknięte — 0 z 12:** brak.

| Warstwa | Kryteria | Otwarte | Otwarte kryteria |
|---|---|---|---|
| L1 — Runtime i środowisko full stack | 13 | 6 | L1.2, L1.6, L1.8, L1.9, L1.11, L1.12 |
| L2 — Komponenty i nawigacja frontendowa | 17 | 13 | L2.1, L2.3, L2.5, L2.6, L2.8, L2.9, L2.11, L2.12, L2.13, L2.14, L2.15, L2.16, L2.17 |
| L3 — Dynamiczna kompozycja interfejsu | 18 | 14 | L3.2, L3.3, L3.4, L3.5, L3.7, L3.10, L3.11, L3.12, L3.13, L3.14, L3.15, L3.16, L3.17, L3.18 |
| L4 — Czat i zarządzanie rozmowami | 15 | 9 | L4.3, L4.5, L4.6, L4.7, L4.8, L4.9, L4.11, L4.13, L4.15 |
| L5 — Komunikacja i zdarzenia | 17 | 11 | L5.2, L5.3, L5.4, L5.5, L5.6, L5.8, L5.11, L5.12, L5.13, L5.14, L5.15 |
| L6 — Kontekst aplikacji dla agenta | 17 | 16 | L6.1, L6.2, L6.3, L6.4, L6.5, L6.6, L6.7, L6.8, L6.9, L6.10, L6.11, L6.12, L6.14, L6.15, L6.16, L6.17 |
| L7 — Orkiestracja backendowa | 13 | 7 | L7.3, L7.4, L7.8, L7.9, L7.10, L7.11, L7.13 |
| L8 — Harness i uwierzytelnienie Claude | 14 | 12 | L8.2, L8.3, L8.5, L8.6, L8.7, L8.8, L8.9, L8.10, L8.11, L8.12, L8.13, L8.14 |
| L9 — Model domeny i funkcje backendu | 16 | 13 | L9.2, L9.3, L9.4, L9.5, L9.6, L9.7, L9.8, L9.11, L9.12, L9.13, L9.14, L9.15, L9.16 |
| L10 — Trwałość, cache i artefakty | 19 | 13 | L10.2, L10.5, L10.6, L10.7, L10.9, L10.11, L10.12, L10.14, L10.15, L10.16, L10.17, L10.18, L10.19 |
| L11 — Pliki, sandbox i cykl życia zadań | 24 | 18 | L11.3, L11.4, L11.5, L11.7, L11.9, L11.10, L11.11, L11.12, L11.13, L11.14, L11.15, L11.16, L11.18, L11.19, L11.20, L11.22, L11.23, L11.24 |
| L12 — Obserwowalność i odbiór integracji | 17 | 9 | L12.1, L12.2, L12.3, L12.5, L12.7, L12.10, L12.12, L12.13, L12.15 |

**Kontrola spójności:** każde kryterium specyfikacji ma dokładnie jedną ocenę, każda próba jest powiązana z kryterium, każde otwarte kryterium ma opis braku i pakiet backlogu.

Znaczenie pól: **Stan w szablonie** dotyczy wyłącznie dowodu uzyskanego na kodzie tego repozytorium. „Potwierdzone” wymaga takiego dowodu; wynik historyczny z AgenticApp jest pokazany osobno i sam nie zalicza kryterium (L12.10). Rodzaj dowodu nie jest statusem.

## Próby odbiorowe

| Próba | Warstwy | Stan w szablonie | Dowód | Braki | Powiązane kryteria |
|---|---|---|---|---|---|
| **T01** — uruchomienie | L1 | **częściowe** | czyste kopie 7d6bd78 i d142b85: install z lockfile, pnpm verify (build), pnpm start — frontend serwowany przez backend, dane startowe (template-consolidation/przebieg-2-7d6bd78/, template-update-2026-09-17/przebieg-d142b85/). Błędna konfiguracja instancji testowej daje czytelny błąd przed operacją (tests/isolation.test.ts). | Brak zbudowanego pakietu przy pnpm test:e2e kończy się surowym ERR_MODULE_NOT_FOUND zamiast czytelnego komunikatu; brak logowania Claude i brak przeglądarek Playwright nie mają próby negatywnej. | L1.1, L1.2, L1.3, L1.4, L1.5, L1.6, L1.7, L1.12, L1.13 |
| **T02** — geometria powłoki | L2, L4 | **częściowe** | e2e/chat-drawer.spec.ts przy 1680 px i 1120 px: szuflada otwarta i zamknięta, kompozytor dostępny, canvas klikalny; e2e/chat-layout.spec.ts: geometria wątku i przynależność kontrolek. | Obie szerokości okna dają ten sam panel czatu 560 px, więc wąski kontener nie jest faktycznie sprawdzony; brak próby zasłoniętej kontrolki przy innej szerokości panelu. | L2.2, L2.5, L2.9, L2.10, L2.12, L4.1 |
| **T03** — odtworzenie nawigacji | L2, L4, L8, L10 | **częściowe** | e2e/session-restore.spec.ts: reload bez klikania listy, kontynuacja sesji, Wstecz/Dalej, przestrzeń wraca z rozmową, usunięta i cudza rozmowa (serwer skryptowany). | Opóźniona odpowiedź poprzedniego wyboru nie jest sprawdzona, a analiza kodu wskazuje brak ochrony przed nadpisaniem nowszej przestrzeni (L2.8); kontynuacja sesji potwierdzona na scenariuszu, nie na prawdziwym modelu. | L2.3, L2.7, L2.8, L2.11, L4.2, L4.3, L4.4, L4.5, L4.7, L8.5, L10.3, L10.4 |
| **T04** — odpowiedź nowej rozmowy | L3, L4 | **częściowe** | pierwsza odpowiedź po nadaniu ID pozostaje widoczna (L4.10); proza i wynik narzędzia w czacie (e2e/tool-activity.spec.ts, e2e/agent-ui.spec.ts). | Brak próby renderowania OpenUI Lang w wiadomości oraz częściowego i błędnego opisu UI; odpowiedź bez tekstu sprawdzona tylko dla strumieniowania. | L3.4, L3.11, L3.12, L4.1, L4.9, L4.10, L4.15 |
| **T05** — przyrostowy tekst | L5, L12 | **potwierdzone** | e2e/streaming.spec.ts: obserwacja wewnątrz strony, co najmniej dwa stany przed końcem wykonania; kontrole negatywne: cała odpowiedź na końcu i echo/podpowiedzi nie zaliczają; e2e/agent-ui.spec.ts na prawdziwym modelu (77 różnych długości przed końcem, pomiary-e2e/22-strumien-model.json); tests/stream-verdict.test.ts. | — | L5.1, L5.8, L5.9, L5.10, L5.13, L5.15, L12.5, L12.6, L12.11, L12.12, L12.13 |
| **T06** — historia narzędzi | L4, L5, L10 | **częściowe** | e2e/tool-activity.spec.ts: wywołanie, wynik i odpowiedź widoczne podczas pracy, po reload, przełączeniu rozmowy i restarcie backendu; błąd narzędzia jako błąd; tests/projection.test.ts. | Argumenty narzędzi nie są sprawdzane w przeglądarce; widoczność nie jest powiązana z fazą running; brak próby błędnego toolCallId w GUI; reconnect trwającego wykonania odtwarza zdarzenia od początku (L5.6). | L4.4, L4.6, L4.11, L4.12, L4.13, L4.14, L4.15, L5.3, L5.6, L5.8, L5.11, L5.13, L10.4 |
| **T07** — pełna zmiana UI | L3, L5, L6 | **częściowe** | e2e/agent-ui.spec.ts na prawdziwym modelu: agent dodaje kartę powiązaną z wykonaniem, przeżywa reload; walidacja katalogu i rozdzielone wersje geometrii/treści w testach kontraktu. | Zmiana, przesunięcie i usunięcie karty przez model nie mają próby w szablonie; karty kind openui z nieznanym komponentem przechodzą walidację (L3.3). | L3.1, L3.2, L3.3, L3.8, L3.9, L3.10, L3.12, L3.13, L5.4, L5.12 |
| **T08** — szkic i kontekst | L3, L6, L9 | **niespełnione** | get_context istnieje, a rozróżnienie szkicu i zapisu jest opisane w wyniku narzędzia. | Narzędzie zwraca kontekst ze startu wykonania — nie ma kanału odczytu nowszego kontekstu podczas pracy (L6.3, L6.9); brak próby szkicu kontra zapis w szablonie (L6.5). | L3.5, L6.1, L6.2, L6.3, L6.5, L6.8, L6.9, L6.10, L9.6 |
| **T09** — odczyt i mutacja domeny | L6, L9, L10 | **częściowe** | e2e/app.spec.ts i e2e/measurements.spec.ts: odczyt i mutacja przez UI z widocznym wynikiem bez reload; pochodzenie ceny do pliku; tests/contracts.test.ts i tests/domain-comparison.test.ts: reguły, konflikt wersji, dostęp. | Ta sama reguła HTTP/MCP nie jest egzekwowana dla wszystkich operacji (L9.2: wagi kryteriów i limit wyszukiwania); przejście po relacjach i selektywny odczyt przez agenta tylko historycznie. | L6.4, L6.6, L6.7, L6.11, L9.1, L9.2, L9.3, L9.4, L9.5, L9.6, L9.9, L9.13, L9.16, L10.1, L10.5 |
| **T10** — konkurencja i idempotencja | L7, L9 | **częściowe** | kolejka per rozmowa i idempotencja operacji zapisu w kodzie; testy powtórzenia pojedynczej operacji w tests/contracts.test.ts. | Brak testów współbieżności tej samej sesji i zwolnienia kolejki po błędzie/anulowaniu (L7.4, L7.10); ten sam klucz z inną treścią zwraca stary wynik (L9.14); zapis zestawienia ignoruje operationId (L9.7). | L7.4, L7.8, L7.9, L7.10, L9.7, L9.14 |
| **T11** — atomowość | L9, L10 | **niespełnione** | artifacts.create ma próbę awarii w środku zapisu. | Część wieloetapowych zapisów działa poza transakcją (wagi kryteriów, publikacja pliku, rozmowa z pierwszą wiadomością, klucz idempotencji); test „nieudana zmiana pozycji oferty” odrzuca dane przed zapisem, więc nie wymusza awarii w połowie (L9.8, L9.15). | L9.8, L9.15 |
| **T12** — zakres dostępu | L2, L6, L10 | **częściowe** | e2e/access-context.spec.ts: po przełączeniu tożsamości brak wierszy poprzedniej, powrót przywraca dane; tests/access-context.test.ts; e2e/session-restore.spec.ts: cudza rozmowa niedostępna. | Próba przeładowuje stronę, więc nie sprawdza czyszczenia w tej samej instancji klienta; opóźnione strumienie i kontekst poprzedniego właściciela nie są odcinane (L6.12, L10.11). | L2.6, L2.8, L2.11, L6.4, L6.11, L6.12, L10.6, L10.7, L10.8, L10.11, L10.13 |
| **T13** — snapshot i live | L9, L10 | **częściowe** | tests/live-artifacts.test.ts: snapshot zachowuje treść, live odczytuje aktualne dane po mutacji, deskryptor bez kodu, kontrola dostępu. | Żaden test przeglądarkowy nie otwiera artefaktu; podgląd i pełny widok oraz stan po restarcie w UI nie są sprawdzone; edycja w UI nie unieważnia otwartego artefaktu live (L10.14, L10.15). | L10.1, L10.4, L10.8, L10.9, L10.10, L10.12, L10.13, L10.14, L10.15 |
| **T14** — plik i sandbox | L8, L11 | **częściowe** | e2e/files-agent.spec.ts na prawdziwym modelu: wgranie przez GUI, zmiana skoroszytu kodem w sandboxie po zgodzie, wynik jako nowa wersja do pobrania, oryginał identyczny bajt w bajt. | Próby niedozwolonego odczytu, zapisu i sieci dla wszystkich klas narzędzi nie są wykonywane w szablonie (L11.3, L11.4, L11.11); publikacja wyniku nie jest atomowa; checksum po sprzątaniu workspace nie jest sprawdzany. | L11.1, L11.2, L11.3, L11.4, L11.5, L11.10, L11.11, L11.16, L11.24 |
| **T15** — zgody | L5, L11 | **częściowe** | e2e/files-agent.spec.ts na prawdziwym modelu: prośba o zgodę na uruchomienie kodu pojawia się w GUI i zgoda wznawia właściwe wykonanie. | Odmowa, ponowiona i spóźniona zgoda nie mają próby; rozstrzygnięcie zgody po samym requestId bez powiązania z wykonaniem (L11.13). | L5.5, L11.9, L11.12, L11.13, L11.19 |
| **T16** — anulowanie i restart | L5, L7, L11 | **częściowe** | e2e/measurements.spec.ts: Stop z interfejsu kończy wykonanie bez dalszych zapisów (959 ms, scenariusz bez modelu); e2e/background-tasks.spec.ts: jawne Stop; tests/run-lifecycle.test.ts: restart oznacza przerwane zadania. | Zakończenie procesów potomnych i kolejka po Stop nie są mierzone na prawdziwym SDK (L11.7, L11.14); brak wykrywania utraty transkryptu SDK — wznowienie po cichu zaczyna nową sesję (L7.13). | L5.2, L5.7, L5.14, L5.17, L7.3, L7.10, L7.13, L11.6, L11.7, L11.8, L11.14, L11.15 |
| **T17** — dostęp do modelu | L8 | **częściowe** | trzy tury na subskrypcji w regresji (e2e/agent-ui.spec.ts, e2e/files-agent.spec.ts); tests/auth.test.ts: klasyfikacja błędów; e2e/tool-activity.spec.ts: symulowany limit widoczny w UI. | Aplikacja nie odczytuje źródła klucza z SDK, więc przebieg przez OAuth subskrypcji nie jest potwierdzony wprost; wygaśnięcie, odmowa odświeżenia i odwołanie są sprawdzone tylko na klasyfikatorze (L8.2, L8.10–L8.12). | L8.1, L8.2, L8.3, L8.4, L8.6, L8.7, L8.8, L8.9, L8.10, L8.11, L8.12, L8.13, L8.14 |
| **T18** — kopia i migracja | L1, L10 | **częściowe** | tests/migration.test.ts: migracja z zachowaniem danych i bez skutku przy drugim uruchomieniu; tests/runtime.test.ts: kopia katalogu danych z checkpointem WAL. | scripts/backup-state.mjs i scripts/migration-rehearsal.mjs nie są wykonywane w szablonie; platform-0003 na niepustej tabeli files niesprawdzona, a próba migracji zgłosiłaby fałszywą zmianę; odtworzenie według procedury nie było przećwiczone. | L1.6, L10.2, L10.16, L10.17, L10.18, L10.19 |
| **T19** — izolacja testów | L1, L12 | **częściowe** | e2e/support/isolation.ts i tests/isolation.test.ts: port, katalog i etykieta instancji walidowane przed operacją; regresja w czystej kopii nie dotknęła instancji ani danych użytkownika (izolacja-agenticapp.txt). | Ścieżki porównywane tekstowo bez realpath — dowiązanie .e2e* do data/ przeszłoby kontrole (L1.11); pozostawiony serwer skryptowany z tą samą etykietą zostałby zaakceptowany (L1.9); pnpm acceptance i run-agent domyślnie kierują zapisy na 127.0.0.1:8791 (L1.8). | L1.8, L1.9, L1.10, L1.11, L12.5, L12.10, L12.12 |
| **T20** — granica modułu | L7, L9 | **częściowe** | pnpm check:boundaries (z kontrolami negatywnymi), tests/platform-boundary.test.ts (platforma bez modułu i z modułem kontrolnym), pnpm check:module-swap (wymiana w warstwie składania, platforma bez zmian, build, start, powłoka). | Moduł kontrolny nie ma połówki UI; połówka UI przykładu wymaga tras aplikacji do typecheck; testy platformy zależą od modułu przykładowego (BL-06). | L9.10, L9.11, L9.12 |
| **T21** — telemetria | L12 | **częściowe** | agent_runs i run_events wiążą rozmowę, wykonanie i zdarzenia; aplikacja działa bez eksportera (tests/observability.test.ts); pomiary pierwszego tekstu liczone od startu wykonania. | Brak odrębnego kodu błędu modelu (model_failed nie jest emitowany); metryki dla kolejności tekst–narzędzie i narzędzie–tekst nie są zbierane w regresji; brak skanu sekretów w diagnostyce po prawdziwym przebiegu. | L12.1, L12.2, L12.3, L12.6, L12.8, L12.12, L12.13, L12.16 |
| **T22** — praca w tle | L4, L5, L6, L7, L11 | **częściowe** | e2e/background-tasks.spec.ts: przełączenie rozmowy i reload nie anulują zadania, powrót pokazuje wynik bez ponownego uruchomienia, Stop wskazanego wykonania; e2e/ui-navigation.spec.ts: zadanie w tle nie przejmuje widoku. | Test pracy w B restartuje serwer, więc zadanie A już nie trwa — równoległa praca A/B nie jest wykazana w przeglądarce; reconnect odtwarza zdarzenia od początku, co może powtórzyć nawigację lub prośbę o zgodę (L5.6). | L4.2, L4.5, L4.6, L5.2, L5.6, L5.12, L5.14, L5.17, L6.12, L6.14, L7.2, L7.3, L7.9, L11.6, L11.15, L11.17, L11.18, L11.19 |
| **T23** — nawigacja agenta | L2, L5, L6 | **częściowe** | e2e/ui-navigation.spec.ts (serwer skryptowany): widok otwarty, ustawienie podświetlone bez zmiany wartości, Wstecz/Dalej, nieznany cel z odmową, brak przejęcia widoku z tła; potwierdzenie klienta przez /api/runs/:id/ui-ack. Od d142b85: e2e/agent-ui.spec.ts „pytanie o dane przenosi na ich widok…” — prawdziwy model wywołuje ui_navigate i ekran kończy na /data. | Polecenie pokazania USTAWIENIA z podświetleniem przez prawdziwy model nie ma próby (scenariusz skryptowany omija handler ui_navigate); ochrona przed ponowieniem polecenia istnieje tylko w pamięci komponentu i nie przeżywa reload (L2.15). | L2.4, L2.13, L2.14, L2.15, L5.6, L5.16, L6.13 |
| **T24** — obrazy i skoroszyty | L8, L10, L11 | **częściowe** | e2e/files-agent.spec.ts na prawdziwym modelu: treść obrazu pod mylącą nazwą, analiza i zmiana wieloarkuszowego XLSX w sandboxie, oryginał bez zmian, wynik dostępny po powrocie. | Uszkodzony plik i nieobsługiwane formuły nie mają próby; asercja komórki przyjmuje formułę z zapisaną wartością; wynik jest wersją pliku bez podglądu artefaktu i nie jest sprawdzany po restarcie; JPEG, CSV i tekst bez próby z modelem. | L8.1, L8.4, L10.9, L11.1, L11.20, L11.21, L11.22, L11.23, L11.24 |
| **T25** — znajdź i pokaż wartość | L2, L6, L9 | **niespełnione** | — | Brak semantycznego opisu aktywnego UI, mapowania rekord–pole na cel prezentacyjny i wskazania wartości mimo filtra lub paginacji (BL-01). | L2.16, L6.15, L6.16, L6.17, L9.4 |
| **T26** — filtrowanie przez rozmowę | L2, L6, L10 | **częściowe** | Od d142b85: e2e/view-filter.spec.ts (serwer skryptowany) — polecenie zawęża widok, wiersze i pasek zgodne (3 z 4), odświeżenie i Wstecz, link, przywrócenie pełnego zakresu przyciskiem i przez agenta, nieznane pole bez zmiany widoku, filtr nie przechodzi na inny ekran; sonda na prawdziwym modelu: zawężenie do PL i przywrócenie (docs/evidence/template-update-2026-09-17/przebieg-d142b85/09-sonda-ui-filter-model.json). | Brak sortowania; kolejne pytanie nie uwzględnia filtra, bo zawężenie nie trafia do kontekstu agenta (L6.1, L6.17); kontrola negatywna „zmiana danych zamiast filtra” i „pusty zbiór przedstawiony jako błąd” bez osobnej próby GUI; model użył najpierw wartości spoza deklaracji (0 z 4 widoczne chwilowo). | L2.6, L2.17, L6.1, L6.15, L6.17, L10.6, L10.12 |
| **T27** — własne widoki agenta | L3, L4, L6, L9 | **niespełnione** | Kompozycja canvasu z katalogu i trwałe przestrzenie istnieją (L3.6). | Brak przestrzeni „Widoki agenta” związanej z rozmową i generowanych, interaktywnych kompozycji z odświeżaniem po mutacji (BL-02). | L3.3, L3.6, L3.7, L3.13, L3.14, L3.15, L3.16, L3.17, L3.18, L6.14 |

## Kryteria

### Warstwa 1 — Runtime i środowisko full stack

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L1.1** | Frontend i backend uruchamiają się z udokumentowanej konfiguracji na docelowym systemie. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/support/boot-server.ts uruchamia produkcyjny apps/server/dist/server.js z konfiguracją z resolveTestInstance (PORT, APP_DATA_DIR, APP_WEB_DIST, APP_ALLOWED_ORIGINS), a e2e/app.spec.ts „lewa nawigacja, canvas i czat wspolistnieja” potwierdza działający frontend serwowany przez backend; regresja wykonuje dodatkowo czystą instalację, pnpm build i pnpm start z danymi startowymi według README (sekcja Uruchomienie). Nie obejmuje: systemów innych niż Linux (README: sprawdzone wyłącznie na Fedora 44), trybu pnpm dev ani obrazu Docker. | — | — | T01 | potwierdzone — przebieg: instancja audytowa :8795 z dist, /api/health 200 i /api/status; dowód z AgenticApp 2026-09-15, poza szablonem |
| **L1.2** | React i TypeScript są najnowszymi stabilnymi wersjami; lockfile i zgodność zależności są sprawdzone. | **częściowe** | analiza kodu (szablon) | Wersje przypięte dokładnie w package.json (react i react-dom 19.3.0 w apps/web i packages/platform-ui, typescript 7.0.2) i pnpm-lock.yaml; regresja wykonuje pnpm install --frozen-lockfile oraz pnpm verify (typecheck tsc --noEmit, build, testy), co sprawdza spójność lockfile i praktyczną zgodność zależności. Nie obejmuje: sprawdzenia w rejestrze npm, że to nadal najnowsze stabilne wersje (ustalenie historyczne z 2026-09-15); .npmrc ma strict-peer-dependencies=false i auto-install-peers=true, więc niezgodności peer nie przerywają instalacji. | Brak wykonywanego sprawdzenia „najnowsze stabilne” (np. porównania z wersją w rejestrze npm zapisanego z dowodami) i brak kontroli ostrzeżeń peer przy instalacji; stan „najnowsze” może się zdezaktualizować bez żadnego sygnału w regresji. | BL-12 | T01 | potwierdzone — test: React 19.3.0 i TS 7.0.2 uznane za najnowsze 2026-09-15, instalacja bez ostrzeżeń peer; rejestru npm nie sprawdzano ponownie |
| **L1.3** | Build produkcyjny oraz sprawdzanie typów kończą się bez błędów; działanie nie zależy od serwera developerskiego Vite. | **potwierdzone** | test GUI bez modelu (szablon) | pnpm verify wykonuje pnpm typecheck (tsc -p tsconfig.json --noEmit) i pnpm build (vite build frontendu i esbuild backendu, od 7d6bd78 przed testami). Wszystkie specyfikacje e2e działają na zbudowanym apps/web/dist serwowanym przez backend (e2e/support/boot-server.ts, e2e/support/scripted-server.ts przez serveStatic), bez serwera Vite. Nie obejmuje: pnpm test:e2e nie buduje sam i wymaga wcześniejszego pnpm build lub pnpm verify (udokumentowane w README od 7d6bd78). | — | — | T01 | potwierdzone — przebieg: vite build + esbuild, node dist/server.js bez flag, testy przeglądarkowe na buildzie; dowód z AgenticApp |
| **L1.4** | Backend obsługuje zwykłe żądania i strumienie bez buforowania odpowiedzi do końca generacji. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” (prawdziwy model): MutationObserver w stronie (e2e/support/streamProbe.ts) i judgeStream wymagają co najmniej 2 różnych długości odpowiedzi w fazie queued lub running oraz sawPartialAnswer, a rejestr ma ponad 3 zdarzenia TEXT_MESSAGE_CONTENT; kontrole negatywne w e2e/streaming.spec.ts „cala odpowiedz naraz na koncu jest rozpoznana jako brak strumieniowania” i „wiadomosc uzytkownika i podpowiedzi nie zaliczaja asercji strumieniowania” (serwer skryptowany) oraz tests/stream-verdict.test.ts. Zwykłe żądania pokrywają wszystkie testy API. Nie obejmuje: pracy za proxy ani z kompresją odpowiedzi. | — | — | T01 | potwierdzone — przebieg: run 8f8325 z 20 zdarzeniami TEXT_MESSAGE_CONTENT — liczba zdarzeń, nie obserwacja przyrostu w stronie |
| **L1.5** | Konfiguracja prywatna nie trafia do pakietu frontendu ani odpowiedzi HTTP. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/durability.test.ts „zbudowany frontend nie zawiera wartosci tokena”, „zbudowany backend nie zawiera wartosci tokena”, „baza danych aplikacji nie zawiera wartosci tokena”, „diagnostyka uruchomieniowa nie wypisuje tokena” szukają rzeczywistych accessToken i refreshToken z ~/.claude/.credentials.json; tests/runtime.test.ts „rzeczywista wartosc tokena nie wystepuje w odpowiedzi /api/status”; tests/contracts.test.ts „nie ujawnia sekretow w statusie”; e2e/app.spec.ts „ustawienia pokazuja tryb subskrypcji i nie ujawniaja sekretow”. Sekret sesji HMAC jest prywatnym polem #secret w packages/platform-server/src/auth/session.ts, frontend nie czyta import.meta.env (kod). Nie obejmuje: bez pliku poświadczeń testy durability kończą się bez asercji (warunek secrets.length === 0), więc wynik zależy od zalogowanej maszyny; skanowana jest odpowiedź /api/status i strona Ustawień, nie każdy endpoint. | — | — | T01 | potwierdzone — test: token nie występuje we frontendzie, backendzie, bazie i diagnostyce; kontrola zależy od obecności lokalnego poświadczenia |
| **L1.6** | Restart zachowuje trwałe dane; zatrzymanie aplikacji nie pozostawia niezarządzanych procesów roboczych. | **częściowe** | test GUI bez modelu (szablon) | Trwałość po restarcie: e2e/tool-activity.spec.ts „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu” (zatrzymanie i ponowny start procesu serwera skryptowanego, identyczne identyfikatory wiadomości), tests/contracts.test.ts „plik przetrwa restart procesu backendu”, tests/run-lifecycle.test.ts „historia przezywa restart backendu”, tests/runtime.test.ts „restart oznacza przerwane zadania zamiast udawac kontynuacje”. Zatrzymanie: tests/runtime.test.ts „zatrzymanie serwera przerywa wszystkie uruchomienia” sprawdza wyłącznie AbortSignal; apps/server/src/main.ts shutdown wywołuje abortAll i server.close (kod). Nie obejmuje: procesów potomnych SDK i CLI Claude oraz sandboxu po zatrzymaniu aplikacji. | Brak dowodu w szablonie, że SIGTERM lub SIGINT w trakcie rzeczywistego wykonania nie zostawia procesów claude ani procesów sandboxu (zero osieroconych procesów potwierdzono tylko w audycie AgenticApp); regresja nie wykryje wycieku procesów roboczych. | BL-03 | T01, T18 | potwierdzone — przebieg: restart instancji audytowej, 5/5 plików, przerwany run jako failed; brak osieroconych procesów tylko w audycie |
| **L1.7** | Dostęp lokalny ma jawne zasady origin i autoryzacji; token subskrypcji nie pełni roli tokena dostępu do aplikacji. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „bez sesji aplikacji zwraca 401”, „odrzuca zadanie z niedozwolonego origin” (403), „identyfikator wlasciciela z ciala zadania nie daje dostepu”; tests/session.test.ts (tożsamość sesji). Kod: packages/platform-server/src/http/app.ts (allowlista APP_ALLOWED_ORIGINS dla żądań z nagłówkiem Origin), auth/session.ts (własne ciasteczko HMAC z data/session.secret; plik poświadczeń Claude czytany tylko dla subscriptionType i expiresAt). Zasady opisane w README (Konfiguracja). Nie obejmuje: żądania bez nagłówka Origin przechodzą kontrolę origin i są chronione wyłącznie sesją — zgodnie z projektem, bez osobnego testu. | — | — | T01 | potwierdzone — test: allowlista origin (403), 401 bez sesji, ciasteczko HMAC; token subskrypcji nie jest tokenem dostępu |
| **L1.8** | Instancja testowa ma odrębny katalog danych, adres i identyfikację; konfiguracja kierująca test na instancję użytkownika jest odrzucana przed operacją zapisu. | **częściowe** | test kontraktu lub logiki (szablon) | tests/isolation.test.ts: „domyslny port aplikacji nie jest portem testowym”, „katalog danych aplikacji jest odrzucony”, „adres wskazujacy inny port niz instancja testowa jest odrzucony”, „APP_BASE_URL wskazujacy instancje uzytkownika przerywa konfiguracje”, „oznaczona instancja nie wstanie na domyslnym porcie”, „loadConfig odrzuca oznaczona instancje przed utworzeniem katalogu”. playwright.config.ts wywołuje resolveTestInstance przy wczytaniu, e2e/support/fixtures.ts i e2e/global-setup.ts sprawdzają etykietę agenticapp-test z /api/health przed pierwszym testem (opis: raport §4b Rezultat 3). Nie obejmuje: negatywnej ścieżki assertIsolatedInstance (odpowiedź instancji bez etykiety) — brak testu; etykieta jest stała dla wszystkich instancji testowych i nie identyfikuje przebiegu. | Udokumentowane w README polecenie pnpm acceptance (scripts/acceptance-agent.mjs, scripts/run-agent.mjs) domyślnie kieruje na http://127.0.0.1:8791, czyli port instancji użytkownika, bez sprawdzenia etykiety, i wykonuje zapisy (zmiana ilości pozycji, dodanie kart); README podaje porty e2e 8795–8799, a specyfikacje używają 8793–8799. Skutek: próba z prawdziwym modelem spoza e2e może pisać przez instancję użytkownika. | BL-12 | T19 | brak oceny (kryterium spoza 95) |
| **L1.9** | Przygotowanie bazy testowej kończy się przed jej otwarciem przez serwer; żaden setup nie usuwa bazy pod działającym procesem. | **częściowe** | analiza kodu (szablon) | e2e/support/boot-server.ts usuwa .e2e-data, wykonuje pnpm migrate i pnpm seed i dopiero potem importuje dist/server.js; Playwright z reuseExistingServer:false odmawia startu, gdy adres już odpowiada. ScriptedInstance.prepareDatabase (e2e/support/scripted.ts) jest wywoływane w beforeAll przed start() w każdej specyfikacji skryptowanej. Poprawka kolejności opisana w raporcie §4b Rezultat 3. Dowodem jest analiza kolejności w kodzie harnessu; brak testu wykrywającego odwrotną kolejność. | prepareDatabase nie sprawdza, czy port lub katalog .e2e-scripted-* jest używany przez działający proces (np. serwer skryptowany osierocony po przerwanym przebiegu); wtedy katalog jest usuwany pod działającym procesem, nowy proces nie zajmie portu, a #waitForHealth i verifyIsolatedInstance przyjmą starą instancję, bo etykieta nie jest unikalna dla przebiegu. | BL-12 | T19 | brak oceny (kryterium spoza 95) |
| **L1.10** | Sprzątanie testów dotyczy wyłącznie procesów i zasobów danego przebiegu; nie stosuje szerokiego dopasowania nazw procesów użytkownika. | **potwierdzone** | analiza kodu (szablon) | ScriptedInstance.stop (e2e/support/scripted.ts) wysyła SIGTERM, a po 5 s SIGKILL wyłącznie do procesu potomnego, który sam uruchomił; serwer główny e2e zatrzymuje Playwright jako własny webServer; scripts/dev-server.sh, audit-server.sh i closure-server.sh zabijają tylko pid z własnego pidfile; w repozytorium nie ma pkill ani killall; harness usuwa wyłącznie zweryfikowane katalogi .e2e* w repozytorium, a testy w tests/ używają mkdtemp i dispose. Opis: raport §4b Rezultat 3. Nie obejmuje: sprzątania procesów osieroconych po awarii samego Playwright; brak testu wykonywanego. | — | — | T19 | brak oceny (kryterium spoza 95) |
| **L1.11** | Izolacja ścieżek uwzględnia rzeczywisty cel katalogu i dowiązania; testy nie usuwają danych wskazanych poza swoim zakresem. | **częściowe** | test kontraktu lub logiki (szablon) | tests/isolation.test.ts „katalog poza repozytorium jest odrzucony”, „katalog bez prefiksu testowego jest odrzucony”, „katalog danych aplikacji jest odrzucony” — assertTestDataDir (e2e/support/isolation.ts) i assertTestInstanceIsIsolated (packages/platform-server/src/config.ts) porównują ścieżki leksykalnie (path.resolve, relative, basename). rmSync w boot-server.ts i scripted.ts dotyczy tylko zweryfikowanego katalogu .e2e*; dla dowiązania usuwa sam link, nie cel. Nie obejmuje: rzeczywistego celu katalogu i dowiązań. | Brak realpathSync lub lstat w e2e/support/isolation.ts i config.ts: katalog .e2e-* będący dowiązaniem do data/ użytkownika (albo APP_DATA_DIR wskazujący taki link) przechodzi wszystkie kontrole i oznaczona instancja testowa pisze do danych użytkownika; brak testu z dowiązaniem. | BL-12 | T19 | brak oceny (kryterium spoza 95) |
| **L1.12** | Wersje rozwiązanych pakietów, Node, SDK i zewnętrznego CLI oraz stan kodu są zapisane razem z dowodami; lockfile nie jest jedynym źródłem wersji CLI. | **częściowe** | analiza kodu (szablon) | docs/evidence/template-update-2026-09-17/przebieg-d142b85/01-srodowisko.txt (czysta kopia d142b85, 2026-09-17): system, Node 24.19.0, pnpm 9.15.9, zewnętrzne CLI claude, pełny hash commita, sha256 pnpm-lock.yaml; start-czysta-kopia.txt: wersje z /api/status (node, hono, drizzle, @mastra/core, @mastra/claude, claude-agent-sdk 0.3.270, zod). Wcześniejszy przebieg: docs/evidence/template-consolidation/przebieg-2-7d6bd78/. Nie obejmuje: rozwiązanych wersji React, TypeScript i OpenUI przy dowodzie; wersji CLI wbudowanej w SDK; pliki pomiarów zapisywane przez testy mają tylko znacznik czasu. | Dowody generowane przez testy nie niosą commita ani wersji, a zapis wersji przy regresji nie obejmuje wszystkich rozwiązanych pakietów ani wersji CLI wbudowanej w SDK (może różnić się od globalnego claude). Wyniku testu nie da się więc samodzielnie powiązać z użytym runtime. | BL-12 | T01 | brak oceny (kryterium spoza 95) |
| **L1.13** | Czyste uruchomienie z instrukcji i lockfile odtwarza aplikację bez nieudokumentowanych plików poprzedniej instalacji. | **potwierdzone** | test GUI bez modelu (szablon) | Czyste kopie (git clone poza repozytorium, bez node_modules, dist i data): 7d6bd78 — install 0, verify 0 (257/257), test:e2e 0 (63/63), start produkcyjny (docs/evidence/template-consolidation/przebieg-2-7d6bd78/); d142b85 — install 0, verify 0 (278/278), test:e2e 0 (72/72), start produkcyjny, check:module-swap 0 (docs/evidence/template-update-2026-09-17/przebieg-d142b85/); commit końcowy — install i verify w kopii sklonowanej z GitHuba (wynik w tagu i w docs/CONSOLIDATION-UPDATE-2026-09-17.md). Nieudany czysty klon bf83bb6 (kolejność testów i buildu) naprawiony w 7d6bd78. Nie obejmuje: nowej maszyny — przeglądarki Playwright z lokalnego cache; pnpm dev; mapowania portu kontenera. | — | — | T01 | brak oceny (kryterium spoza 95) |

### Warstwa 2 — Komponenty i nawigacja frontendowa

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L2.1** | Komponenty domenowe mają typowane właściwości i są zarejestrowane w katalogu OpenUI. | **częściowe** | test kontraktu lub logiki (szablon) | Serwer: cardComponents modułu (packages/module-procurement/src/server/index.ts) i platform-components.ts mają schematy Zod; tests/contracts.test.ts „odrzuca nieprawidlowe wlasciwosci znanego komponentu” i „odrzuca nieznany komponent karty”; tests/platform-boundary.test.ts „przyjmuje dowolny modul testowy: migracja, narzedzie, trasa, komponent”. Przeglądarka: buildRegistry (packages/platform-ui/src/catalog/registry.tsx) dołącza openuiComponents modułu do openuiLibrary z @openuidev/react-ui (kod). Nie obejmuje: rejestracji wszystkich komponentów domenowych w katalogu OpenUI. | Tylko 2 z 7 komponentów kart domeny (OfferComparison, OfferCostChart) są zdefiniowane przez defineComponent w katalogu OpenUI; caseSummary, offerList, deliveryTerms, offerItemForm i provenance istnieją wyłącznie we własnym katalogu kart, a renderery React dostają props typu Record<string, unknown>. Komentarz w registry.tsx powołuje się na nieistniejący tests/catalog-parity.test.ts — rozjazd katalogu serwera i rendererów ujawnia się dopiero w runtime („Brak renderera”). | BL-10 | — | potwierdzone — kod: 10 komponentów kart ze schematami Zod i rendererami; rejestracja w katalogu OpenUI nie była osobno sprawdzana |
| **L2.2** | Domyślna kompozycja obejmuje lewą nawigację i prawy czat, z adaptacją do mniejszych ekranów. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/app.spec.ts „lewa nawigacja, canvas i czat wspolistnieja” (region nawigacji i region rozmowy widoczne) i „hamburger zwija i rozwija menu”; e2e/chat.spec.ts „na waskim ekranie menu jest zwiniete, a czat nadal dostepny” (820 px); e2e/chat-drawer.spec.ts przy 1680 i 1120 px. Kolejność nawigacja, canvas, czat wynika z siatki .pf-shell w packages/platform-ui/src/styles.css (poniżej 1100 px czat w drugim wierszu). Nie obejmuje: asercji współrzędnych (nawigacja po lewej, czat po prawej) — ustalone analizą CSS, nie pomiarem. | — | — | T02 | potwierdzone — przebieg i test: chat-drawer.spec 6/6 przy 1680 i 1120 px po naprawie D-1, z kontrolą siły testu |
| **L2.3** | Nawigacja, odświeżenie oraz Wstecz/Dalej przywracają właściwą aktywną rozmowę i przestrzeń pracy bez ręcznego wyszukiwania rozmowy na liście. | **częściowe** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts (serwer skryptowany): „po przeladowaniu ta sama rozmowa, jej historia i przestrzen pracy”, „przestrzen pracy wraca razem z rozmowa” (reload przywraca ?c= i ?s= oraz karty przestrzeni bez szuflady), „Wstecz i Dalej przelaczaja rozmowy bez mieszania historii”; tests/session-restore.test.ts (decideRestore). Kod: apps/web/src/router.tsx retainSearchParams(['c','s']), SpaceSync.tsx, ConversationSync.tsx. Nie obejmuje: przestrzeni przy Wstecz/Dalej oraz zachowania c i s przy przejściach przez linki. | Nowe brzmienie wymaga rozmowy i przestrzeni: test Wstecz/Dalej sprawdza tylko rozmowę; zmiana przestrzeni w obrębie rozmowy zapisuje adres przez replace (raport §7 pkt 12), więc Wstecz jej nie przywraca; brak testu, że przejście przez Link zachowuje c i s. | BL-10 | T03 | potwierdzone — test: session-restore.spec (6) i session-restore.test (11); dawne brzmienie „rozmowę lub przestrzeń”, obecne wymaga obu |
| **L2.4** | Dynamiczne wnętrze UI nie wymaga generowania nowych plików tras ani wykonywalnego kodu aplikacji. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts (prawdziwy model): nowa karta procurement.costChart pojawia się na canvasie bez przeładowania i bez zmian kodu; apps/web/src/router.tsx ma statyczne drzewo tras, a kompozycja to dane CardSpec walidowane przez ComponentCatalog.validate (packages/platform-server/src/registry/catalog.ts); nawigacja agenta korzysta z katalogu celów (registry/ui-targets.ts), bez nowych tras. Nie obejmuje: kart kind openui (brak testu). | — | — | T23 | potwierdzone — przebieg: raw-probe-canvas.log — karty zmieniane bez nowych tras; rozszerzone 2026-09-16 o nawigację agenta |
| **L2.5** | Formularze i podstawowe interakcje działają z klawiatury, mają etykiety i widoczny fokus. | **częściowe** | test GUI bez modelu (szablon) | e2e/app.spec.ts „menu jest obslugiwalne z klawiatury i ma widoczny fokus” (focus() i toBeFocused, Enter otwiera ekran spraw); e2e/chat-drawer.spec.ts „otwarcie i zamkniecie dziala z klawiatury, kompozytor pozostaje dostepny” (Enter, kontrolka zamykania przyjmuje fokus). Kod: globalne :focus-visible w styles.css, etykiety pól .pf-field. Nie obejmuje: widoczności fokusu, etykiet i obsługi formularzy z klawiatury. | Żaden test nie sprawdza, że fokus jest widoczny (obrys) ani że pola formularzy (np. karta procurement.offerItemForm, wgrywanie pliku) mają etykiety i działają z klawiatury; nazwa testu obiecuje „widoczny fokus”, a asercja sprawdza tylko toBeFocused. | BL-10 | T02 | potwierdzone — test: nawigacja klawiaturą, etykiety pól, globalne :focus-visible; widoczność fokusu nie była asertowana |
| **L2.6** | Ładowanie, brak danych, błąd i brak dostępu mają rozróżnialne stany prezentacji. | **częściowe** | test GUI bez modelu (szablon) | e2e/access-context.spec.ts „po przelaczeniu zaden wiersz poprzedniej tozsamosci nie jest widoczny” — data-testid access-denied z data-error-code forbidden lub not_found i brak case-detail-page; e2e/app.spec.ts „brakujace dane sa oznaczone jako brakujace” (pf-missing w karcie); e2e/session-restore.spec.ts stan conversation-missing. Kod: QueryErrorState (packages/platform-ui/src/components/ErrorState.tsx) rozróżnia access-denied i query-error; karty mają Loading, Failure i pf-state--empty. Nie obejmuje: stanów ładowania, pustego zbioru i ogólnego błędu w GUI. | Brak testu, że ładowanie (np. „Wczytywanie przestrzeni…”), brak danych (pf-state--empty) i błąd pobrania (query-error) są widoczne i odróżnialne od siebie; po przełączeniu tożsamości test sprawdza zero kafelków, a nie komunikat pustego stanu. | BL-10 | T12, T26 | potwierdzone — test: access-context.spec (access-denied) i live-artifacts; ładowanie i pusty stan bez testu GUI |
| **L2.7** | Po przeładowaniu wracają ta sama aktywna rozmowa i przestrzeń bez ręcznego otwierania szuflady; następne polecenie trafia do właściwej rozmowy. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts (serwer skryptowany): „po przeladowaniu ta sama rozmowa, jej historia i przestrzen pracy” (ten sam ?c=, ta sama liczba wiadomości, jedna rozmowa w backendzie), „dalsze polecenie po przeladowaniu kontynuuje te sama sesje” (1 rozmowa, 2 uruchomienia, niezmieniony claudeSessionId; wejście przez adres ?c= na nowej stronie), „przestrzen pracy wraca razem z rozmowa” (?s= po reload, karty przestrzeni). e2e/agent-ui.spec.ts (prawdziwy model) po reload pokazuje wiadomości rozmowy bez szuflady. Mechanizm: raport §4b Rezultat 1. Nie obejmuje: kontynuacji sesji na prawdziwym modelu po reload (sprawdzona na stand-in). | — | — | T03 | brak oceny (kryterium spoza 95) |
| **L2.8** | Wybór rozmowy, zmiana przestrzeni, przejścia przez linki i Wstecz/Dalej zachowują spójny kontekst; stara odpowiedź sieciowa nie cofa nowszego wyboru. | **częściowe** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts „Wstecz i Dalej przelaczaja rozmowy bez mieszania historii”; e2e/chat.spec.ts „lista rozmow, przelaczanie i usuwanie”; tests/session-restore.test.ts decideRestore (m.in. „Wstecz na poprzednia rozmowe wraca do niej, nie publikuje biezacej”); tests/access-context.test.ts „odpowiedz, ktora mimo wszystko dobiegnie, jest odrzucana po epoce” (tylko zmiana tożsamości). Nie obejmuje: opóźnionej odpowiedzi poprzedniego wyboru rozmowy lub przestrzeni ani przejść przez linki. | ConversationSync.followConversationSpace (packages/platform-ui/src/chat/ConversationSync.tsx) po await GET /api/conversations/:id wywołuje setSpace bez sprawdzenia, czy rozmowa jest nadal wybrana — wolniejsza odpowiedź wcześniejszego wyboru może nadpisać przestrzeń wybraną później (zabezpieczenia brak w kodzie, brak testu z opóźnioną odpowiedzią); brak testu zachowania kontekstu przy przejściach przez linki. | BL-10 | T03, T12 | brak oceny (kryterium spoza 95) |
| **L2.9** | Szufladę rozmów można otworzyć i zamknąć na szerokim i wąskim panelu; canvas ani overflow nie przechwytują jej interakcji. | **częściowe** | test GUI bez modelu (szablon) | e2e/chat-drawer.spec.ts przy viewport 1680 i 1120 px: „zamknieta szuflada nie zaslania rozmowy” (pokrycie panelu do 2 px), „otwarcie i zamkniecie dziala z klawiatury, kompozytor pozostaje dostepny”, „canvas pod spodem pozostaje klikalny przy zamknietej szufladzie” (elementFromPoint przy lewej krawędzi panelu należy do czatu); otwarcie myszą „Open sidebar” w e2e/chat.spec.ts i e2e/agent-ui.spec.ts. Kontekst: raport §4d. Nie obejmuje: wąskiego kontenera czatu i zamykania myszą. | Oba viewporty dają ten sam kontener czatu 560 px (kolumna --pf-chat-width powyżej 1100 px), więc wąski panel nie jest sprawdzony; przy szerokości do 1100 px czat zajmuje pełną szerokość w drugim wierszu i szuflada nie ma tam testu; zamknięcie tylko z klawiatury; przechwycenie kliknięć sprawdzone tylko przy zamkniętej szufladzie, a e2e/chat.spec.ts klika wiersze z force:true. | BL-10 | T02 | brak oceny (kryterium spoza 95) |
| **L2.10** | Układ jest sprawdzony przy rzeczywistej szerokości kontenera; zagnieżdżony provider lub nadpisanie CSS nie jest traktowane jako dowód konfiguracji. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/chat-layout.spec.ts „watek zajmuje panel, a nie ulamek jego szerokosci” (getBoundingClientRect: wątek do panelu powyżej 0,9) i „nic naszego nie jest dzieckiem kontenera czatu”; e2e/chat-drawer.spec.ts mierzy w pikselach pokrycie panelu przez szufladę. Testy oceniają zmierzoną geometrię w działającym kontenerze, nie obecność nadpisania CSS (kontrola siły: raport §4d i §5). Nie obejmuje: drugiej szerokości kontenera — przy obu viewportach kontener ma 560 px (zob. L2.9). | — | — | T02 | brak oceny (kryterium spoza 95) |
| **L2.11** | Usunięta lub niedostępna rozmowa i błąd pobrania mają czytelny stan zastępczy; cudze tytuły i wiadomości nie są ujawniane. | **częściowe** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts „usunieta rozmowa daje czytelny stan zastepczy, nie pusty ekran” (conversation-missing, data-kind gone, aktywny kompozytor, przycisk nowej rozmowy) i „rozmowa innego wlasciciela nie jest pokazywana” (brak wiadomości i tytułu cudzej rozmowy w .pf-chat); tests/session-restore.test.ts classifyThreadFailure „awaria zadania jest odroznialna od usuniecia”. Mechanizm: raport §4b Rezultat 1. Nie obejmuje: widoku stanu failed w przeglądarce. | Stan „błąd pobrania” (data-kind failed, ponowienie) ma tylko test klasyfikacji w Vitest; brak testu GUI, że nieudane wczytanie historii pokazuje czytelny stan zastępczy zamiast pustego wątku. | BL-10 | T03, T12 | brak oceny (kryterium spoza 95) |
| **L2.12** | Pan, zoom, przesunięcie i zmiana rozmiaru kart działają na gotowej bibliotece canvasu i nie przerywają czatu ani interakcji formularza. | **częściowe** | test GUI bez modelu (szablon) | e2e/app.spec.ts „przesuniecie karty jest trwale i nie zmienia jej tresci” (przeciągnięcie nagłówka karty w React Flow, geometryVersion rośnie, specVersion bez zmian, pozycja po reload). Kod: packages/platform-ui/src/canvas/CanvasHost.tsx używa @xyflow/react (pan, zoom, Controls, MiniMap, onMoveEnd zapisuje viewport), treść karty ma nodrag i nowheel. Nie obejmuje: panu, zoomu, zmiany rozmiaru oraz braku zakłóceń czatu i formularza. | Brak NodeResizer lub innej kontrolki — użytkownik nie może zmienić rozmiaru karty w UI (gałąź dimensions w onNodesChange obsługuje tylko pomiar); pan i zoom bez testu; brak testu, że przeciąganie lub pan nie przerywa pisania w kompozytorze ani w formularzu karty. | BL-10 | T02 | brak oceny (kryterium spoza 95) |
| **L2.13** | Polecenie agenta otwiera wskazany widok, ustawienie lub przestrzeń i pokazuje właściwy element, zamiast tylko opisywać drogę do niego. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „pytanie o dane przenosi na ich widok, a nie tylko je opisuje” (PRAWDZIWY model, czysta kopia d142b85, 25,6 s): start na /files, polecenie „Co jest w dostawcach?”, asercja na pasku adresu — pathname /data i widoczny data-page, niezależnie od treści odpowiedzi; sonda docs/evidence/template-update-2026-09-17/przebieg-d142b85/09-sonda-ui-filter-model.json: model otwiera /data z zawężeniem, potwierdzenia klienta executed=true. e2e/ui-navigation.spec.ts (serwer skryptowany): otwarcie /files i podświetlenie ustawienia settings-auth. Nie obejmuje: podświetlenia ustawienia i przełączenia przestrzeni przez prawdziwy model. | Otwarcie widoku przez prawdziwy model ma teraz dowód, ale pokazanie ustawienia lub elementu (podświetlenie) i przełączenie przestrzeni (spaceId) przez model nie mają próby; scenariusz skryptowany nadal woła requestUi z pominięciem handlera ui_navigate. | BL-03 | T23 | brak oceny (kryterium spoza 95) |
| **L2.14** | Element docelowy może być odsłonięty, przewinięty i czasowo podświetlony; brak celu lub dostępu daje czytelny rezultat, bez zmiany wartości ustawienia. | **częściowe** | test GUI bez modelu (szablon) | e2e/ui-navigation.spec.ts „otwarcie ustawienia podswietla kontrolke i niczego nie zmienia” (data-ui-highlight=true, potem zdjęte; /api/status auth.method i credential.state bez zmian) oraz „niedostepny cel daje czytelna odmowe, a widok sie nie zmienia” (executed=false, unknown_target, pathname /cases); tests/ui-navigation.test.ts „element nieobecny na ekranie to porazka z wlasnym powodem”, „cudza przestrzen pracy jest odrzucona przed dotknieciem interfejsu”, „pokazanie ustawienia nie ma zadnej operacji zmiany wartosci”. Nie obejmuje: odsłonięcia ukrytej sekcji, przewinięcia oraz braku dostępu i nieobecnego elementu w GUI. | UiCommandRunner.reveal (packages/platform-ui/src/shell/UiCommandRunner.tsx) wykonuje tylko querySelector, scrollIntoView i atrybut podświetlenia — nie rozwija zwiniętych sekcji, a element ukryty, lecz obecny w DOM, liczy się jako pokazany; przewinięcie nie jest asertowane; odmowy not_present i forbidden sprawdzane tylko w Vitest z symulowanym potwierdzeniem; porównanie /api/status dotyczy pól, których UI i tak nie zmienia. | BL-10 | T23 | brak oceny (kryterium spoza 95) |
| **L2.15** | Nawigacja zlecona przez agenta zachowuje historię Wstecz/Dalej i aktualizuje kontekst; ponowienie zdarzenia nie powiela nawigacji. | **częściowe** | test GUI bez modelu (szablon) | e2e/ui-navigation.spec.ts „Wstecz i Dalej dzialaja po nawigacji wykonanej przez agenta” (/cases → /files, Wstecz do /cases, Dalej do /files) i „zadanie w tle nie przejmuje widoku innej rozmowy”; tests/ui-navigation.test.ts „polecenie niesie rozmowe i stabilny identyfikator”. Idempotencja: UiCommandRunner pamięta obsłużone commandId w useRef (kod). Nie obejmuje: aktualizacji kontekstu po nawigacji i ponowionego zdarzenia. Od d142b85: zawężenie widoku ustawione przez agenta trafia do adresu i wraca przyciskiem Wstecz oraz po odświeżeniu (e2e/view-filter.spec.ts „zawezony widok przezywa odswiezenie i wraca przyciskiem Wstecz”, serwer skryptowany). | Kontekst agenta (toAppContext w packages/platform-ui/src/state/appState.ts) nie zawiera aktywnego widoku ani trasy, więc otwarcie widoku przez agenta nie zmienia kontekstu kolejnego polecenia i nie ma testu kontekstu po nawigacji; zbiór obsłużonych commandId jest tylko w pamięci komponentu i po przeładowaniu pusty, a kursor lastSeq startuje od 0 — ponowne wykonanie nawigacji po reload w trakcie wykonania nie jest wykluczone i nie ma testu. | BL-10 | T23 | brak oceny (kryterium spoza 95) |
| **L2.16** | Polecenie „pokaż wartość pola tego rekordu” otwiera właściwy widok i przestrzeń, ujawnia rekord mimo filtra lub paginacji i wskazuje pole z wartością zgodną z backendem; potwierdzenie klienta identyfikuje rekord i pole. | **niespełnione** | — (—) | Brak implementacji: katalog celów (PLATFORM_UI_TARGETS, uiTargets modułu) zawiera widoki, ustawienia i karty, bez celów rekordu i pola; uiCommandSchema i wynik potwierdzenia (packages/platform-contracts/src/ui.ts) nie niosą identyfikatora rekordu ani pola; brak mechanizmu dostosowania filtra lub paginacji. Brak testów w tests/ i e2e/. README i docs/NEW-APPLICATION.md §5 wymieniają to wprost jako brak. | Polecenie „pokaż wartość pola tego rekordu” nie może otworzyć i wskazać konkretnego pola z wartością z backendu ani uzyskać potwierdzenia klienta identyfikującego rekord i pole; agent może najwyżej otworzyć widok lub kartę. | BL-01 | T25 | brak oceny (kryterium spoza 95) |
| **L2.17** | Agent ustawia i usuwa filtry oraz zmienia sortowanie przez typowane akcje. Wynik jest widoczny w kontrolkach i zbiorze wyników, trafia do kontekstu agenta i nie modyfikuje danych biznesowych. | **częściowe** | rzeczywisty model (szablon) | Od d142b85 narzędzie ui_filter (typowana akcja: targetId, predicates eq/neq/contains/in, label, clear) zawęża widok deklarujący UiTarget.filter; stan w adresie (?country=PL), pasek z liczbami i „Pokaż pełny widok”; zawężanie odsiewa tylko wiersze już pobrane, dane biznesowe nietknięte. e2e/view-filter.spec.ts (7 testów, serwer skryptowany): 3 z 4 wierszy, pasek, odświeżenie i Wstecz, wklejony link, przycisk przywraca pełny widok, nieznane pole = odmowa bez zmiany widoku, agent sam przywraca, zawężenie nie przechodzi na inny ekran; tests/view-filter.test.ts (21). Sonda na PRAWDZIWYM modelu (jednorazowa, poza regresją, docs/evidence/template-update-2026-09-17/przebieg-d142b85/09-sonda-ui-filter-model.json): „Pokaż tylko dostawców z Polski.” → /data?country=PL, 3 z 4, pasek; „Pokaż z powrotem wszystkich dostawców.” → 4 wiersze, bez parametru. | Brak sortowania i paginacji (nie istnieją w widokach ani w adresie). Stan zawężenia nie trafia do kontekstu agenta: AppContext.filters jest zawsze puste (setFilter bez wywołań), więc w kolejnej turze agent nie wie, że widok jest zawężony — zna tylko wynik z potwierdzenia w tej samej turze. Kontrolki to pasek i przycisk przywrócenia, bez edycji filtra przez użytkownika. Zawężanie działa tylko w widokach deklarujących pola i na danych już pobranych. W sondzie model dwa razy użył wartości spoza zadeklarowanych (country=Polska, country=Poland, po 0 z 4 widocznych chwilowo na ekranie), zanim użył kodu PL. | BL-01 | T26 | brak oceny (kryterium spoza 95) |

### Warstwa 3 — Dynamiczna kompozycja interfejsu

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L3.1** | Początkowy układ i układ zmieniony przez agenta korzystają z tego samego katalogu komponentów. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/platform-boundary.test.ts „domyslna kompozycja modulu testowego jest walidowana tym samym katalogiem” (domyślna wartość props nadana przez walidację katalogu); kod: ComponentCatalog.validate wywoływane dla kompozycji domyślnej (http/app.ts for-scope, services/base-data.ts) i w narzędziach canvas_add_card oraz canvas_update_card (agent/platform-tools.ts); w przeglądarce obie ścieżki renderuje CardBody z jednego ClientRegistry. e2e/agent-ui.spec.ts: karta dodana przez prawdziwy model przechodzi przez ten katalog. Nie obejmuje: kart openui, które walidacja przepuszcza bez sprawdzenia katalogu (zob. L3.3). | — | — | T07 | potwierdzone — przebieg: raw-probe-canvas.log — układ domyślny i agenta przez ComponentCatalog.validate() |
| **L3.2** | Agent może dodać, usunąć, przestawić i zmienić właściwości elementu przez obsługiwany opis kompozycji. | **częściowe** | rzeczywisty model (szablon) | Kod: narzędzia canvas_add_card, canvas_update_card, canvas_move_card, canvas_remove_card (packages/platform-server/src/agent/platform-tools.ts). e2e/agent-ui.spec.ts (prawdziwy model): dodanie karty procurement.costChart. tests/contracts.test.ts na poziomie serwisu: „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu”, „zapis pozycji karty nie konkuruje z zapisem jej tresci”, „dodanie karty z tym samym operationId nie tworzy dwoch kart”. Nie obejmuje: zmiany, przesunięcia i usunięcia wykonanych przez agenta. | Handlery canvas_update_card, canvas_move_card i canvas_remove_card nie są wywoływane przez żaden test ani przez model w szablonie; serwis removeCard nie ma testu kontraktu (DELETE tylko jako przygotowanie w agent-ui.spec.ts); dowód czterech operacji agenta istnieje tylko historycznie (audyt 2026-09-15). | BL-03 | T07 | potwierdzone — przebieg: audyt 2026-09-15 — add (hist.), update, move, remove przez model; raw-probe-canvas.log spoza szablonu |
| **L3.3** | Nieznany komponent, nieprawidłowe właściwości i niedozwolone odwołania nie są wykonywane ani zatwierdzane. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „odrzuca nieznany komponent karty” i „odrzuca nieprawidlowe wlasciwosci znanego komponentu”; tests/live-artifacts.test.ts „deskryptor wskazujacy nieznana operacje jest odrzucany przy zapisie” i „deskryptor z niepoprawnym wejsciem jest odrzucany przy zapisie”; walidacja przed zapisem we wszystkich ścieżkach HTTP i MCP (kod). Nie obejmuje: kart kind openui i odwołań do cudzych rekordów. | ComponentCatalog.validate dla kind openui sprawdza tylko niepusty source — nieznane komponenty i niepoprawne właściwości w OpenUI Lang są zatwierdzane i zapisywane; props z referencją do cudzej sprawy (caseId) są przyjmowane przy zapisie, a odmowa pojawia się dopiero przy odczycie; brak testów obu przypadków. | BL-10 | T07, T27 | potwierdzone — test: contracts.test.ts — nieznany komponent i złe props odrzucone w backendzie; karty openui nie były oceniane |
| **L3.4** | Częściowa lub błędna odpowiedź modelu nie niszczy ostatniego poprawnego układu. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu” (nieaktualna specVersion odrzucona, zostaje v2); walidacja katalogu rzuca przed zapisem (catalog.ts, platform-tools.ts); CardErrorBoundary w packages/platform-ui/src/canvas/CardBody.tsx izoluje błąd renderowania jednej karty (kod). Nie obejmuje: częściowej lub błędnej odpowiedzi modelu. | Błędny lub urwany opis OpenUI w karcie openui przechodzi walidację (wystarczy niepusty tekst) i przez canvas_update_card zastępuje ostatnią poprawną treść karty; brak testu przerwanego wykonania w trakcie kompozycji i renderowania uszkodzonej karty; scenariusz tool-error w e2e tylko emituje hooki, bez wywołania narzędzia. | BL-10 | T04 | potwierdzone — test: konflikt kompozycji zostawia poprzednią wersję; częściowa odpowiedź modelu nie była badana |
| **L3.5** | Zmiana kompozycji zachowuje zaznaczenia, filtry i niezapisane dane albo jawnie rozwiązuje konflikt przed ich utratą. | **częściowe** | test GUI bez modelu (szablon) | e2e/app.spec.ts „zmiana widoku karty przezywa przejscie miedzy ekranami” (odznaczony showExcluded w cardState zostaje po przejściu Ustawienia → Canvas); e2e/chat.spec.ts „zaznaczenie karty trafia do kontekstu rozmowy”; appState trzyma selection, cardState i drafts poza węzłami React Flow (kod). Nie obejmuje: zmiany kompozycji. | Brak testu, że dodanie, zmiana lub usunięcie karty (przez agenta albo refetch po canvas_changed) zachowuje zaznaczenie, stan widoku karty i niezapisany szkic formularza procurement.offerItemForm; brak jawnego rozwiązania konfliktu, gdy agent zmienia lub usuwa kartę ze szkicem; filtry kontekstu nie są używane (setFilter bez wywołań). | BL-10 | T08 | potwierdzone — test: stan widoku karty przeżywa zmianę ekranu; zmiana kompozycji przez agenta nie była badana |
| **L3.6** | Zapisana kompozycja jest odtwarzana po ponownym otwarciu aplikacji. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/app.spec.ts „przesuniecie karty jest trwale i nie zmienia jej tresci” (karta i pozycja po reload z backendu); e2e/agent-ui.spec.ts karta dodana przez model widoczna po reload; tests/runtime.test.ts „skopiowany katalog danych otwiera sie z kompletnym stanem” (karty w kopii bazy). Kod: viewport zapisywany w onMoveEnd i odtwarzany w CanvasHost. Nie obejmuje: asercji odtworzenia viewportu ani kompozycji po restarcie backendu w GUI. | — | — | T27 | potwierdzone — przebieg: karty, pozycje i widok po przeładowaniu i restarcie backendu instancji audytowej |
| **L3.7** | Dane biznesowe wyświetlane przez komponenty pochodzą z backendu; wygenerowane wartości nie zastępują trwałych rekordów. | **częściowe** | test GUI bez modelu (szablon) | e2e/app.spec.ts „tabela porownawcza pokazuje wartosci z backendu i przyczyny wykluczen”; e2e/measurements.spec.ts „zmiana pozycji oferty jest widoczna w zestawieniu bez przeladowania” (sumy na ekranie zmieniają się po mutacji backendu, porównanie z API); schematy props kart domeny niosą tylko referencje, a z.object usuwa nadmiarowe klucze (kod). Nie obejmuje: kart kind openui i platform.markdown. | Karta openui (dowolny OpenUI Lang z ogólnymi komponentami tabeli i wykresu openuiLibrary) i platform.markdown przyjmują treść z wartościami wygenerowanymi przez model bez powiązania z odczytem backendu; brak testu, że karta utworzona przez model nie niesie wartości biznesowych. | BL-10 | T27 | potwierdzone — przebieg: props niosą referencje, kwoty na ekranie zgodne z backendem (17-playwright.txt) |
| **L3.8** | Tożsamość karty pozostaje stabilna przy zmianie danych; wersja geometrii i wersja treści nie nadpisują się wzajemnie. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/app.spec.ts „przesuniecie karty jest trwale i nie zmienia jej tresci” (geometryVersion rośnie, specVersion i spec bez zmian dla tego samego card.id); tests/contracts.test.ts „zapis pozycji karty nie konkuruje z zapisem jej tresci” (osobne liczniki specVersion i geometryVersion, obie zmiany zachowane). Kontrakt rozdziału w packages/platform-contracts/src/canvas.ts. Stabilność tożsamości przy zmianie danych biznesowych wynika z konstrukcji (props niosą referencje, mutacje domeny nie zapisują canvas_cards) — bez osobnej asercji identyfikatora po mutacji. | — | — | T07 | brak oceny (kryterium spoza 95) |
| **L3.9** | Równoczesne przesunięcie użytkownika i zmiana treści przez agenta zachowują oba poprawne rezultaty albo ujawniają rozstrzygalny konflikt. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „zapis pozycji karty nie konkuruje z zapisem jej tresci”: przesunięcie użytkownika, potem zmiana treści z expectedSpecVersion sprzed przesunięcia — zachowane obie zmiany (x=400 i nowa treść); „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu” — konflikt treści zgłaszany jawnie (code conflict). Nie obejmuje: przeplotu w przeglądarce (przeciąganie w trakcie refetch po canvas_changed) ani przebiegu z prawdziwym modelem. | — | — | T07 | brak oceny (kryterium spoza 95) |
| **L3.10** | Cztery operacje kompozycji — dodanie, aktualizacja, przesunięcie i usunięcie — mają dowód rzeczywistego wywołania przez agenta, nie tylko test endpointu. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” (prawdziwy model): wyłącznie dodanie — nowa karta procurement.costChart spoza idsBefore, zdarzenia TOOL_CALL_START z prefiksem mcp__app__. Scenariusz tool-then-text w e2e/support/scripted-server.ts tylko emituje hooki dla mcp__app__canvas_add_card, bez wywołania narzędzia. Raport §6 wymienia cztery operacje canvasu wyłącznie z audytu AgenticApp. Nie obejmuje: aktualizacji, przesunięcia i usunięcia. | Aktualizacja, przesunięcie i usunięcie karty nie mają w szablonie dowodu wywołania przez prawdziwy model; dowód historyczny (raw-probe-canvas.log, audyt 2026-09-15) nie jest częścią repozytorium ani regresji. | BL-03 | T07 | brak oceny (kryterium spoza 95) |
| **L3.11** | Zwykły tekst, poprawny OpenUI, częściowy OpenUI i błędny opis są rozróżniane; rejestracja katalogu nie powoduje niewidocznych odpowiedzi. | **częściowe** | test GUI bez modelu (szablon) | Proza przy zarejestrowanym katalogu jest widoczna: e2e/session-restore.spec.ts „po przeladowaniu ta sama rozmowa, jej historia i przestrzen pracy” (assistant-message zawiera tekst odpowiedzi), a także e2e/streaming.spec.ts i e2e/agent-ui.spec.ts. Kod: makeAssistantMessage i isOpenUiLang (packages/platform-ui/src/chat/AssistantMessage.tsx) wybierają Renderer OpenUI albo MarkDownRenderer (naprawa N-2 z raportu). Nie obejmuje: poprawnego, częściowego i błędnego OpenUI. | Brak testu w tests/ i e2e/, który renderuje poprawny opis OpenUI Lang, częściowy opis w trakcie strumienia albo błędną składnię w wiadomości lub karcie openui; isOpenUiLang nie ma testu jednostkowego — rozróżnienie czterech przypadków nie jest sprawdzane. | BL-10 | T04 | brak oceny (kryterium spoza 95) |
| **L3.12** | Wygenerowany opis używa wyłącznie zarejestrowanych komponentów i odczytów; własna reprezentacja ma udokumentowane odwzorowanie na renderer. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „odrzuca nieznany komponent karty” i „odrzuca nieprawidlowe wlasciwosci znanego komponentu” (kind component); tests/live-artifacts.test.ts „deskryptor wskazujacy nieznana operacje jest odrzucany przy zapisie” (tylko zarejestrowane odczyty). docs/NEW-APPLICATION.md §4 opisuje cardRenderers i openuiComponents osobno. Nie obejmuje: kart openui i odwzorowania własnej reprezentacji na renderer. | Własna reprezentacja CardSpec kind component jest renderowana bezpośrednio przez React cardRenderers, bez udokumentowanego odwzorowania na OpenUI Lang i Renderer; karty openui nie są walidowane względem katalogu po stronie serwera, więc opis może użyć niezarejestrowanych komponentów. | BL-10 | T04, T07 | brak oceny (kryterium spoza 95) |
| **L3.13** | Wynik próby dynamicznego UI jest nowym lub zmienionym obiektem powiązanym z badanym wykonaniem; zastany element nie zalicza próby. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts: clearChartCards usuwa karty costChart z poprzednich przebiegów, test zapamiętuje idsBefore, sprawdza brak karty wykresu przed poleceniem i wymaga nowej karty procurement.costChart spoza idsBefore oraz zachowania wcześniejszych kart; bieżące wykonanie ma TOOL_CALL_START mcp__app__. Nie obejmuje: jawnego powiązania karty z badanym wykonaniem i operacji innych niż dodanie. | Test nie porównuje cardId z TOOL_CALL_RESULT tego wykonania z nową kartą, a canvas_cards nie przechowuje runId; korelacja wynika tylko z kolejności w jednym workerze; próba obejmuje wyłącznie dodanie, nie zmianę, przesunięcie i usunięcie. | BL-03 | T07, T27 | brak oceny (kryterium spoza 95) |
| **L3.14** | Widoki domyślne i „Widoki agenta” używają rzeczywistych kompozycji OpenUI i wspólnego katalogu React; próba obejmuje więcej niż pojedynczy statyczny komponent osadzony w czacie. | **niespełnione** | — (—) | Kompozycja domyślna (defaultComposition w packages/module-procurement/src/server/index.ts) to karty CardSpec kind component renderowane bezpośrednio przez React cardRenderers, nie kompozycje OpenUI Lang; przestrzeni „Widoki agenta” nie ma w nawigacji ani w kodzie. Istnieją klocki: karta kind openui renderowana przez Renderer z ClientRegistry.library i dwa komponenty defineComponent modułu. Brak testów. | Widoki domyślne nie są kompozycjami OpenUI, a „Widoki agenta” nie istnieją; próby wielokomponentowej kompozycji OpenUI poza czatem nie da się wykonać. | BL-02 | T27 | brak oceny (kryterium spoza 95) |
| **L3.15** | Osobna przestrzeń prezentacji agenta jest dostępna z nawigacji, powiązana z rozmową i zachowuje wygenerowane kompozycje po przełączeniu rozmowy oraz przeładowaniu. | **niespełnione** | — (—) | Brak osobnej przestrzeni prezentacji agenta: agent dodaje karty do roboczej przestrzeni sprawy współdzielonej z kompozycją domyślną. Klocki: rozmowa ma powiązanie spaceId odświeżane przy starcie wykonania, e2e/session-restore.spec.ts „przestrzen pracy wraca razem z rozmowa”, lista „Zapisane kompozycje” (/spaces) w nawigacji, karta dodana przez model w e2e/agent-ui.spec.ts widoczna po reload. | Nie ma przestrzeni „Widoki agenta” ani równoważnego obszaru canvasu przypisanego rozmowie jako miejsce jej prezentacji; brak dowodu zachowania wygenerowanych kompozycji po przełączeniu rozmowy (agent-ui.spec.ts po przełączeniu sprawdza tylko wiadomości). | BL-02 | T27 | brak oceny (kryterium spoza 95) |
| **L3.16** | Na pytanie wymagające zestawienia agent bez wskazania nazwy komponentu tworzy adekwatną prezentację danych; osobne polecenie wykresu daje rzeczywisty wykres z poprawnymi seriami, jednostkami i zakresem. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts (prawdziwy model): polecenie „Dodaj na canvasie karte z wykresem kosztow dla tej sprawy” bez identyfikatora komponentu daje nową kartę procurement.costChart widoczną jako card-cost-chart. Nie obejmuje: zestawienia bez wskazania formy ani poprawności serii, jednostek i zakresu wykresu. | Brak próby, w której pytanie wymagające zestawienia prowadzi do samodzielnie dobranej prezentacji; test wykresu sprawdza tylko typ komponentu, nie serie (oferty), jednostki (PLN) i zakres; brak przestrzeni „Widoki agenta”. | BL-02 | T27 | brak oceny (kryterium spoza 95) |
| **L3.17** | Kolejne polecenie zmienia istniejący widok agenta, np. filtr, grupowanie lub typ prezentacji, zachowując nieobjęte zmianą elementy i stan użytkownika. | **niesprawdzone** | — (—) | Klocki w kodzie: canvas_update_card zmienia spec jednej karty (inny komponent albo props, np. showExcluded) z kontrolą expectedSpecVersion, bez dotykania innych kart i cardState użytkownika (agent/platform-tools.ts, state/appState.ts). W szablonie nie ma testu ani przebiegu modelu, w którym kolejne polecenie zmienia istniejący widok; aktualizacja karty przez model była tylko w audycie AgenticApp. | Brak dowodu, że kolejne polecenie zmienia istniejący widok agenta (filtr, grupowanie, typ prezentacji) z zachowaniem pozostałych elementów i stanu użytkownika; grupowania nie ma w schematach komponentów, a „Widoki agenta” nie istnieją. | BL-02 | T27 | brak oceny (kryterium spoza 95) |
| **L3.18** | Wygenerowana tabela lub wykres korzysta z zarejestrowanego odczytu i odświeża się po mutacji backendu; interakcje uruchamiają te same autoryzowane operacje domenowe co widok domyślny. | **częściowe** | test GUI bez modelu (szablon) | Karty generowane przez agenta (kind component) używają tych samych rendererów i odczytów modułu co widok domyślny (CardBody → cardRenderers → useModuleData, kod). e2e/measurements.spec.ts „zmiana pozycji oferty jest widoczna w zestawieniu bez przeladowania” pokazuje odświeżenie po mutacji dla karty kompozycji domyślnej; tests/live-artifacts.test.ts „snapshot zamraza liczby, live pokazuje biezace — po zmianie zrodla” dla artefaktu live z zarejestrowanym odczytem. Nie obejmuje: tabeli lub wykresu wygenerowanego rozmową. | Brak testu, że tabela lub wykres utworzone przez agenta odświeżają się po mutacji i że ich interakcje wywołują te same autoryzowane operacje; ogólne komponenty openuiLibrary w karcie openui mogą przyjąć dane wpisane przez model bez zarejestrowanego odczytu; brak przestrzeni „Widoki agenta”. | BL-02 | T27 | brak oceny (kryterium spoza 95) |

### Warstwa 4 — Czat i zarządzanie rozmowami

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L4.1** | Czat korzysta z gotowego komponentu OpenUI, bez obowiązkowej zewnętrznej płatnej usługi. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/chat-layout.spec.ts „nic naszego nie jest dzieckiem kontenera czatu” i „artefakty sa osobna zakladka panelu” działają na AgentInterface z @openuidev/react-ui 0.13.10 (packages/platform-ui/src/chat/ChatPanel.tsx); testy czatu (e2e/chat.spec.ts, e2e/tool-activity.spec.ts) sterują gotowymi klasami openui-agent-*. Magazyn rozmów to restStorage na własnym /api/threads (chat/chatWiring.ts createChatStorage); w kodzie UI brak wywołań zewnętrznych usług (analiza kodu). Nie obejmuje zgodności z przyszłymi wersjami biblioteki. | — | — | T02, T04 | potwierdzone — przebieg: zrzuty 12/14-chat-*.png; AgentInterface 0.13.10 z restStorage na własnym backendzie, bez płatnej usługi |
| **L4.2** | Użytkownik tworzy rozmowę, widzi ją na liście, przełącza się między rozmowami i usuwa wybraną rozmowę. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/chat.spec.ts „lista rozmow, przelaczanie i usuwanie”: rozmowy widoczne w szufladzie, usunięcie z menu wiersza (Thread actions → Delete) i kontrola w /api/threads/get. Tworzenie w GUI (New chat + wysłanie polecenia) i przełączanie z kontrolą treści wątku: e2e/tool-activity.spec.ts „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu” (otwarcie rozmowy z szuflady po tytule), e2e/session-restore.spec.ts „Wstecz i Dalej przelaczaja rozmowy bez mieszania historii” (serwer skryptowany). Zastrzeżenie: w chat.spec rozmowy są zakładane przez API, a asercja po kliknięciu sprawdza tylko widoczny tytuł; pełne pokrycie wynika z sumy trzech plików. | — | — | T03, T22 | potwierdzone — test: 17-playwright chat.spec; usuwanie przez menu wiersza w UI z kontrolą wyniku w backendzie |
| **L4.3** | Rozmowy mają automatyczne sensowne tytuły oraz możliwość zmiany tytułu; mechanizm nie korzysta z API Anthropic. | **częściowe** | test GUI bez modelu (szablon) | tests/runtime.test.ts „skraca pierwsza wiadomosc do sensownego tytulu” (deriveTitle); e2e/chat.spec.ts „rozmowa dostaje sensowny tytul z pierwszej wiadomosci” (przez /api/threads/create, bez przeglądarki); e2e/tool-activity.spec.ts sprawdza tytuł rozmowy założonej poleceniem z GUI. Brak API Anthropic: packages/platform-server/src/services/conversations.ts (deriveTitle) to wyłącznie operacje na napisach (analiza kodu). Zmiana tytułu: PATCH /api/threads/update/:id w packages/platform-server/src/http/app.ts istnieje, bez testu. | Zmiana tytułu nie jest dostępna użytkownikowi: menu wiersza gotowego ThreadList (@openuidev/react-ui 0.13.10) ma tylko Delete, aplikacja nie dodaje własnej kontrolki, a endpoint PATCH nie ma testu. Skutek: możliwość zmiany tytułu istnieje tylko w API i nie ma dowodu. | BL-08 | T03 | potwierdzone — test: tytuł wyprowadzany lokalnie z pierwszej wiadomości, zero wywołań modelu; zmiana tytułu nie była sprawdzana |
| **L4.4** | Historia i tytuły wracają po odświeżeniu oraz restarcie backendu. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/tool-activity.spec.ts „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu”: po reload i po restarcie instancji skryptowanej oś narzędzi widoczna, identyfikatory wiadomości identyczne, rozmowa odnajdywana w szufladzie po tytule po przeładowaniu; e2e/session-restore.spec.ts „po przeladowaniu ta sama rozmowa, jej historia i przestrzen pracy”; e2e/agent-ui.spec.ts (model) reload bez szuflady. tests/run-lifecycle.test.ts „historia przezywa restart backendu” (symulacja); tests/migration.test.ts „zachowuje rozmowy, wiadomosci uzytkownika, kompozycje i dane domenowe” sprawdza tytuł po ponownym uruchomieniu. Tytuł po restarcie sprawdzany na bazie, nie w GUI. | — | — | T03, T06 | potwierdzone — przebieg + testy: ta sama rozmowa, tytuł i historia narzędzi po reload i restarcie; stare rozmowy odtwarzane backfillem |
| **L4.5** | Przełączenie rozmowy nie miesza wiadomości, kontekstu ani wyników trwających wykonań. | **częściowe** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts „Wstecz i Dalej przelaczaja rozmowy bez mieszania historii” (wątek jednej rozmowy nie zawiera treści drugiej); e2e/background-tasks.spec.ts „przelaczenie rozmowy i przeladowanie nie anuluja zadania” (pasek stanu nowej rozmowy nie pokazuje wykonania A); tests/agui-stream.test.ts „strumien rozmowy B nie dotyka stanu rozmowy A”, „prosba o zgode trafia do rozmowy zadania, nie do ogladanej”; tests/background-runs.test.ts „zadanie w A trwa, gdy uzytkownik pracuje w B, i wyniki sie nie mieszaja” (symulacja). | e2e/background-tasks.spec.ts „praca w rozmowie B nie miesza sie z zadaniem w A” nie ma trwającego zadania A (instancja skryptowana jest zatrzymywana po poprzednim teście, co przerywa wykonanie), więc równoległa praca A i B w GUI nie jest wykazana. Kontekst następnego polecenia po przełączeniu nie jest sprawdzany; analiza kodu: selection w useAppState nie jest czyszczone przy zmianie rozmowy ani przestrzeni i trafia do kolejnego polecenia. | BL-08 | T03, T22 | potwierdzone — przebieg raw-probe-conc2.log: dwa polecenia w jednej rozmowie (ALFA/BETA) bez mieszania; bez testu GUI |
| **L4.6** | Powtórzenie żądania lub reconnect nie tworzy podwójnych wiadomości. | **częściowe** | rzeczywisty model (szablon) | tests/contracts.test.ts „powtorzona wiadomosc o tym samym id nie tworzy drugiego wpisu”; tests/runtime.test.ts „odtwarza od podanego numeru — reconnect nie powiela zdarzen”; tests/background-runs.test.ts „ponowne podlaczenie od numeru sekwencyjnego nie powtarza zdarzen”; tests/agui-stream.test.ts „strumien z wyslania zajmuje przebieg, wiec podlaczenie w tle go pomija”; e2e/agent-ui.spec.ts (model) i e2e/tool-activity.spec.ts: identyfikatory historii identyczne po reload i restarcie; e2e/background-tasks.spec.ts „powrot do rozmowy pokazuje wynik bez ponownego uruchamiania”. | Powtórzone żądanie POST /api/agui/run nie jest idempotentne: wiadomość użytkownika jest deduplikowana po id, ale każde wywołanie startuje nowe wykonanie (runId od klienta jest ignorowany w packages/platform-server/src/http/app.ts), więc powstaje druga odpowiedź asystenta i ewentualne powtórne skutki narzędzi; bez threadId powstaje druga rozmowa. Brak testu powtórzonego POST. | BL-08 | T06, T22 | potwierdzone — przebieg raw-probe-conc2.log: 3 wiadomości użytkownika przy dwóch równoległych poleceniach; powtórzony POST nie badany |
| **L4.7** | Usunięcie ma określony skutek dla sesji, aktywnego zadania i artefaktów; aplikacja nie pozostawia niespójnych powiązań. | **niespełnione** | analiza kodu (szablon) | packages/platform-server/src/services/conversations.ts (delete) i db/migrations.ts: messages i agent_runs ON DELETE CASCADE, artifacts.conversation_id ON DELETE SET NULL, claude_session_id znika z wierszem. e2e/chat.spec.ts „lista rozmow, przelaczanie i usuwanie” potwierdza usunięcie przez GUI. e2e/chat.spec.ts „usuniecie rozmowy odlacza artefakty zamiast je kasowac” usuwa rozmowę bez żadnego artefaktu, więc liczba artefaktów nie może się zmienić i test nie potrafi oblać. | Brak określonego skutku dla aktywnego zadania: usunięcie nie anuluje wykonania (runtime działa dalej w pamięci), a wiersz agent_runs znika kaskadowo; przy foreign_keys=ON dalsze appendEvent, upsertMessage i publikacja artefaktu trafiają w nieistniejące powiązania (błędy FK, niespójny stan). Odłączenie artefaktu nie ma testu z rzeczywistym artefaktem. | BL-08 | T03 | potwierdzone — test 17-playwright: kaskada wiadomości i uruchomień, artefakty odłączane; test bez artefaktu, aktywne zadanie niebadane |
| **L4.8** | Zakres gotowej obsługi edycji wiadomości, rozgałęziania i przywracania usuniętych rozmów jest opisany jako dostępny lub niedostępny; UI nie sugeruje niezaimplementowanych funkcji. | **częściowe** | analiza kodu (szablon) | packages/platform-contracts/src/conversation.ts (CHAT_CAPABILITIES: editMessage, branchConversation, restoreDeletedConversation = false) wystawione w /api/status i pokazane w packages/platform-ui/src/shell/SettingsPage.tsx (settings-chat-capabilities). Analiza zainstalowanego @openuidev/react-ui 0.13.10: gotowy interfejs nie ma kontrolek edycji, rozgałęziania ani przywracania (jedyna akcja wiersza: Delete). Brak testu odczytującego deklarację. | renameConversation jest deklarowane i pokazywane w Ustawieniach jako dostępne, a UI nie ma kontrolki zmiany tytułu (tylko PATCH w API) — ekran sugeruje funkcję niedostępną dla użytkownika. Deklaracja nie jest pilnowana testem, więc może rozjechać się z UI. | BL-08 | — | potwierdzone — kod: CHAT_CAPABILITIES, /api/status i Ustawienia oznaczają edycję, rozgałęzianie i przywracanie jako niedostępne |
| **L4.9** | Zwykła proza, formatowanie, treść OpenUI i odpowiedź po narzędziu są widoczne w gotowym czacie; test sprawdza treść, a nie pusty kontener. | **częściowe** | rzeczywisty model (szablon) | Proza: e2e/streaming.spec.ts „tekst dociera fragmentami i jest widoczny przed koncem wykonania” i e2e/session-restore.spec.ts sprawdzają treść assistant-message; odpowiedź po narzędziu: e2e/tool-activity.spec.ts „wywolanie, jego wynik i odpowiedz sa widoczne w czacie” (treść „Widac na niej podsumowanie sprawy.”), e2e/agent-ui.spec.ts (model, znacznik w odpowiedzi po narzędziu, także po reload). Wybór renderera: packages/platform-ui/src/chat/AssistantMessage.tsx (isOpenUiLang → Renderer, inaczej MarkDownRenderer). | Żaden test nie sprawdza formatowania markdown (np. pogrubienie, lista) ani odpowiedzi w OpenUI Lang renderowanej w wiadomości czatu; scenariusze skryptowane zawierają zwykły tekst. Skutek: regresja gałęzi Renderer lub utrata formatowania przejdzie niezauważona. | BL-08 | T04 | brak oceny (kryterium spoza 95) |
| **L4.10** | Nadanie identyfikatora nowej rozmowie podczas pierwszej tury nie przemontowuje czatu w sposób gubiący wiadomości lub trwający strumień. | **potwierdzone** | test GUI bez modelu (szablon) | Brak key na AgentInterface (packages/platform-ui/src/chat/ChatPanel.tsx). Pierwsza tura nowej rozmowy w GUI: e2e/tool-activity.spec.ts „wywolanie, jego wynik i odpowiedz sa widoczne w czacie” (oś narzędzi i końcowa odpowiedź w wątku), e2e/streaming.spec.ts „tekst dociera fragmentami i jest widoczny przed koncem wykonania” (przyrost i pełna odpowiedź w assistant-message), e2e/session-restore.spec.ts „po przeladowaniu ta sama rozmowa, jej historia i przestrzen pracy” (identyfikator w adresie, wiadomości zachowane). W regresji brak dedykowanej kontroli negatywnej z przywróconym key; scripts/audit-probe-remount.mjs jest sondą diagnostyczną. | — | — | T04 | brak oceny (kryterium spoza 95) |
| **L4.11** | Wywołania narzędzi, argumenty w dozwolonym zakresie, wyniki i błędy są widoczne podczas pracy oraz po przeładowaniu i restarcie. | **częściowe** | rzeczywisty model (szablon) | e2e/tool-activity.spec.ts „wywolanie, jego wynik i odpowiedz sa widoczne w czacie” (oś i nazwa canvas_add_card), „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu”, „blad narzedzia jest pokazany jako blad, nie jako wynik” (failed i powód po rozwinięciu); e2e/agent-ui.spec.ts (model): oś widoczna i po reload. Zapis argumentów: tests/run-lifecycle.test.ts „wywolanie, argumenty i wynik sa zapisane w wiadomosciach”. | Argumenty narzędzia nie są sprawdzane w GUI (ani w trakcie, ani po reload); błąd narzędzia sprawdzany tylko w trakcie, nie po reload i restarcie; widoczność osi nie jest skorelowana z fazą running (asercja czeka na widoczność bez odczytu data-phase); treść udanego wyniku nie jest sprawdzana. | BL-08 | T06 | brak oceny (kryterium spoza 95) |
| **L4.12** | Trwała reprezentacja wiąże wywołanie z wynikiem i wiadomością, np. przez toolCalls i toolCallId; same końcowe treści odpowiedzi nie zastępują historii narzędzi. | **potwierdzone** | rzeczywisty model (szablon) | tests/run-lifecycle.test.ts „wywolanie, argumenty i wynik sa zapisane w wiadomosciach” (meta.toolCalls, toolCallId, jedna wiadomość odpowiedzi); tests/projection.test.ts „wynik narzedzia trafia do wiadomosci powiazanej identyfikatorem wywolania”; packages/platform-server/src/http/app.ts (toAguiMessage) oddaje toolCalls i wiadomości role tool; e2e/agent-ui.spec.ts (model) sprawdza w /api/threads/get, że toolMessage.toolCallId równa się toolCalls[0].id; e2e/tool-activity.spec.ts sprawdza toolCalls i role tool w historii. | — | — | T06 | brak oceny (kryterium spoza 95) |
| **L4.13** | Identyfikatory wiadomości i narzędzi nie kolidują między wykonaniami; projekcja historii zachowuje kolejność i jest idempotentna. | **częściowe** | test kontraktu lub logiki (szablon) | tests/projection.test.ts „identyfikator wiadomosci narzedzia jest zwiazany z uruchomieniem, nie tylko z wywolaniem”, „identyfikatory sa wyprowadzone, wiec powtorzenie zdarzen nie duplikuje tury”, „projekcja przyrostowa daje ten sam wynik co jednorazowa” oraz sześć scenariuszy porównania kolejności z processStreamedMessage; services/conversations.ts upsertMessage nadaje seq tylko przy pierwszym zapisie; tests/migration.test.ts „powtorne uruchomienie nie dubluje danych”. | toolCallId jest zapisywany bez przestrzeni nazw wykonania (agent/projection.ts), a gotowy czat paruje wywołania z wynikami po toolCallId w całej historii; unikalność zależy od SDK (fallback runtime: tu_<Date.now()>). Brak testu dwóch wykonań w jednej rozmowie z tym samym toolCallId i próby, że błędny toolCallId nie wiąże cudzych wyników. | BL-08 | T06 | brak oceny (kryterium spoza 95) |
| **L4.14** | Przy odtwarzaniu starej rozmowy brak zdarzeń nie powoduje wymyślania aktywności ani utraty zachowanej odpowiedzi. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/migration.test.ts „uruchomienie bez zachowanych zdarzen zostawia stara wiadomosc nietknieta” (bez run_events ta sama wiadomość am_<runId> z treścią, bez dopisanej aktywności) i „zachowuje rozmowy, wiadomosci uzytkownika, kompozycje i dane domenowe” (odbudowa tylko z zachowanych zdarzeń, db/backfill.ts). Dowód na poziomie bazy, nie GUI; gałąź /api/runs/:id/stream syntetyzująca zdarzenie końcowe ze statusu rekordu nie ma testu. | — | — | T06 | brak oceny (kryterium spoza 95) |
| **L4.15** | Rozszerzenie renderera zachowuje prezentację narzędzi i artefaktów; wymaganie gotowego czatu nie blokuje publicznych adapterów potrzebnych do integracji. | **częściowe** | rzeczywisty model (szablon) | packages/platform-ui/src/chat/AssistantMessage.tsx zastępuje tylko treść wiadomości; oś narzędzi biblioteki pozostaje — widoczna z zainstalowanym nadpisaniem w e2e/tool-activity.spec.ts i e2e/agent-ui.spec.ts (model). Publiczne adaptery (restStorage z własnym kanałem artefaktów w chatWiring.ts, platformAguiAdapter, ConversationSync na hookach useThreadList/useThread) działają w testach GUI; e2e/chat-layout.spec.ts „artefakty sa osobna zakladka panelu” otwiera przeglądarkę artefaktów. | Prezentacja artefaktu w czacie (TimelineEntry dla artifactRenderers, widok artefaktu z treścią przez ArtifactContent) nie jest sprawdzana żadnym testem — test zakładki otwiera pustą przeglądarkę; nagłówek AssistantMessage.tsx tylko deklaruje, że wpisy artefaktów rysuje InterleavedTurn. | BL-08 | T04, T06 | brak oceny (kryterium spoza 95) |

### Warstwa 5 — Komunikacja i zdarzenia

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L5.1** | Tekst jest widoczny przyrostowo przed zakończeniem generacji. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” (prawdziwy model subskrypcyjny): sonda MutationObserver z e2e/support/streamProbe.ts i judgeStream z expectedFragment (znacznik unikalny dla przebiegu) wymagają co najmniej 2 różnych długości w fazie queued/running i niepełnej odpowiedzi na początku. Deterministycznie: e2e/streaming.spec.ts „tekst dociera fragmentami i jest widoczny przed koncem wykonania” (serwer skryptowany). Warunek w kodzie: agent/runtime.ts includePartialMessages: true. | — | — | T05 | potwierdzone — test: streaming.spec (3) + 22-strumien-model.json, 75 długości na modelu; detektor z kontrolą negatywną |
| **L5.2** | UI rozpoznaje rozpoczęcie i zakończenie wykonania, błąd oraz anulowanie. | **częściowe** | test GUI bez modelu (szablon) | tests/agui-stream.test.ts „sukces: kolejka → wykonywanie → zakonczone”, „blad niesie kod i komunikat, nie tylko fakt porazki”, „anulowanie nie zostaje nadpisane przez blad, ktory po nim nastepuje”, „stan konczy sie na zdarzeniu koncowym, nie na pojawieniu sie tekstu” (reduktor UI na adapterze biblioteki). GUI: e2e/background-tasks.spec.ts (data-phase running), e2e/tool-activity.spec.ts „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika” (failed i rate_limited), succeeded w wielu testach. | Anulowanie nie jest sprawdzane w GUI: e2e/background-tasks.spec.ts „jawne Stop konczy wskazane wykonanie” i e2e/measurements.spec.ts „zatrzymanie z interfejsu konczy wykonanie i nie zostawia zapisow” sprawdzają tylko status w backendzie. Analiza kodu: stop w kompozytorze (chat/useComposerStop.ts) wywołuje stopRun, ale biblioteka w tym samym kliknięciu przerywa fetch (cancelMessage → abort), więc platform.run_cancelled nie dociera tym strumieniem i pasek może pozostać w fazie running. | BL-08 | T16, T22 | potwierdzone — test: agui-stream (15) + tool-activity; fazy ze zdarzeń, osobny queued; anulowanie tylko w teście reduktora |
| **L5.3** | Wywołania narzędzi i ich wyniki są powiązane i rzeczywiście widoczne w czacie podczas wykonania oraz po odtworzeniu historii. | **częściowe** | rzeczywisty model (szablon) | Powiązanie: tests/projection.test.ts (reduktor biblioteki na 6 scenariuszach), tests/run-lifecycle.test.ts „wywolanie, argumenty i wynik sa zapisane w wiadomosciach”. Widoczność: e2e/tool-activity.spec.ts „wywolanie, jego wynik i odpowiedz sa widoczne w czacie” oraz „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu”; e2e/agent-ui.spec.ts (model): oś widoczna i po reload, historia sparowana po toolCallId. | Asercje nie wiążą widoczności osi z trwającym wykonaniem (czekają na widoczność bez odczytu data-phase), więc oś pokazana dopiero po zakończeniu też przejdzie; treść udanego wyniku narzędzia nie jest sprawdzana w GUI (tylko komunikat błędu po rozwinięciu). Skutek: „podczas wykonania” i „wyniki widoczne” nie mają ostrego dowodu. | BL-08 | T06 | potwierdzone — przebieg + tool-activity: wywołanie i wynik sparowane po toolCallId i widoczne; bez korelacji z fazą wykonania |
| **L5.4** | Dane artefaktów i zmian UI docierają do właściwych rendererów. | **częściowe** | rzeczywisty model (szablon) | tests/agui-stream.test.ts „zmiana kompozycji uniewaznia przestrzen, ktorej dotyczy”, „zmiana danych uniewaznia odczyty modulu, canvas i otwarte artefakty”, „nowy artefakt uniewaznia liste i otwarty podglad”; e2e/agent-ui.spec.ts (model): karta wykresu pojawia się na canvasie bez przeładowania po narzędziu; tests/live-artifacts.test.ts (odczyt artefaktu live po stronie serwera). | Brak testu, w którym dane artefaktu trafiają do renderera artefaktu w czacie lub widoku artefaktu (artifactRenderers w ChatPanel.tsx, components/LiveArtifact.tsx); sprawdzane jest tylko unieważnianie kluczy cache. Skutek: błędne przypisanie typu artefaktu do renderera nie zostanie wykryte. | BL-08 | T07 | potwierdzone — test: unieważnienia właściwych kluczy (agui-stream, live-artifacts); renderowanie artefaktu w czacie bez testu |
| **L5.5** | Pytanie lub prośba o decyzję dociera do interfejsu, a odpowiedź wraca do właściwego wykonania. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety” (prawdziwy model): prośba o zgodę na Bash pojawia się w GUI (permission-prompt), kliknięcie „Zgoda” i wynik wykonania (nowa wersja pliku) potwierdzają powrót odpowiedzi do wykonania; asercja wymaga użycia bramki. tests/agui-stream.test.ts „prosba o zgode trafia do rozmowy zadania, nie do ogladanej”, „prosba o zgode wstrzymuje wykonanie w odroznialnym stanie”. Drugi kanał (polecenie UI z potwierdzeniem): tests/ui-navigation.test.ts. | Brak testu odmowy (brak skutku), braku odpowiedzi (timeout 120 s jako odmowa) oraz ponowionej i spóźnionej odpowiedzi; runtime.answerPermission dopasowuje tylko requestId, a /api/runs/:id/permission nie sprawdza, że requestId należy do wskazanego wykonania. Dowód pozytywny zależy od tego, czy model poprosi o Bash. | BL-08 | T15 | potwierdzone — przebieg 2026-09-16 (files-agent, ui-navigation): zgoda i odmowa wracają do wykonania; odmowa w szablonie bez testu |
| **L5.6** | Rozłączenie i ponowne połączenie nie powielają zdarzeń ani skutków operacji. | **częściowe** | test GUI bez modelu (szablon) | tests/runtime.test.ts „odtwarza od podanego numeru — reconnect nie powiela zdarzen”; tests/background-runs.test.ts „ponowne podlaczenie od numeru sekwencyjnego nie powtarza zdarzen”, „/api/runs/:id/stream odtwarza zakonczone uruchomienie od kursora”; tests/agui-stream.test.ts „strumien z wyslania zajmuje przebieg, wiec podlaczenie w tle go pomija”, „ponowne odtworzenie tego samego strumienia nie zmienia stanu koncowego”; e2e/background-tasks.spec.ts „powrot do rozmowy pokazuje wynik bez ponownego uruchamiania”; tests/contracts.test.ts „powtorzona operacja z tym samym operationId nie dubluje skutku”. | Po reload klient traci lastSeq i zbiór obsłużonych commandId (useRef w shell/UiCommandRunner.tsx), więc ponowne podłączenie do trwającego wykonania odtwarza zdarzenia od 0: ui_command może zostać wykonane drugi raz (zależnie od kolejności efektów), a platform.permission_request ponownie pokazuje prośbę już rozstrzygniętą, bo żadne zdarzenie nie oznacza odpowiedzi. Brak testu GUI reconnectu w trakcie wykonania z poleceniem UI lub zgodą. | BL-08 | T06, T22, T23 | potwierdzone — test runtime.test.ts: odtworzenie od numeru sekwencyjnego bez powielania; skutki chronione idempotencją |
| **L5.7** | Każde wykonanie ma jeden rozstrzygający status końcowy; UI nie pozostaje bezterminowo w stanie ładowania po błędzie. | **potwierdzone** | test GUI bez modelu (szablon) | tests/runtime.test.ts „kazde uruchomienie ma dokladnie jeden rozstrzygajacy status koncowy”, „restart oznacza przerwane zadania zamiast udawac kontynuacje”; tests/run-lifecycle.test.ts „blad modelu konczy sie dokladnie jednym stanem koncowym i zapisuje przyczyne”, „sukces konczy sie dokladnie jednym stanem koncowym” (symulacja, jedno RUN_FINISHED lub RUN_ERROR); e2e/tool-activity.spec.ts „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika” (faza failed, kompozytor aktywny); e2e/measurements.spec.ts: po anulowaniu dziennik zdarzeń przestaje rosnąć. Nie obejmuje stanu paska po stopie z kompozytora (zob. L5.2, L5.14). | — | — | T16 | potwierdzone — przebieg raw-probe-stop.log: dokładnie jeden status końcowy, po anulowaniu 80 → 80 zdarzeń |
| **L5.8** | Pokrycie zdarzeń zostało sprawdzone z rzeczywistym adapterem Claude, a brakujące mapowania są jawnie opisane. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts (prawdziwy model przez @mastra/claude 0.3.1 i most hooków w packages/platform-server/src/agent/runtime.ts): w /api/runs/:id/events są TOOL_CALL_START, TOOL_CALL_RESULT, RUN_FINISHED, ponad 3 TEXT_MESSAGE_CONTENT, narzędzia mcp__app__* i powiązana sesja Claude; e2e/files-agent.spec.ts (model): prośba o zgodę z canUseTool. Brakujące mapowania opisane w docs/NEW-APPLICATION.md §7 (adapter przekazuje tylko tekst, gotowy czat ignoruje CUSTOM) i w nagłówku runtime.ts. | Z rzeczywistym adapterem nie sprawdzono błędu narzędzia (hook PostToolUseFailure), błędu strumienia modelu (chunk error → RUN_ERROR z klasyfikacją) ani treści TOOL_CALL_ARGS; te ścieżki są tylko symulowane (run-lifecycle, tool-activity). Skutek: zmiana nazwy hooka lub formatu błędu w SDK nie zostanie wykryta. | BL-03 | T05, T06 | potwierdzone — kod: analiza processStreamedMessage 0.9.13; 9 zdarzeń obsługiwanych, CUSTOM i STEP_* ignorowane, udokumentowane |
| **L5.9** | Przyrost tekstu ma co najmniej dwa różne obserwowane stany przed zakończeniem wykonania; treść dopiero po końcu, podpowiedź i echo polecenia nie zaliczają testu. | **potwierdzone** | rzeczywisty model (szablon) | Pozytywnie na modelu: e2e/agent-ui.spec.ts (judgeStream z expectedFragment, co najmniej 2 różne długości w fazie queued/running, sawPartialAnswer). Kontrole negatywne (serwer skryptowany): e2e/streaming.spec.ts „cala odpowiedz naraz na koncu jest rozpoznana jako brak strumieniowania” oraz „wiadomosc uzytkownika i podpowiedzi nie zaliczaja asercji strumieniowania” (zero próbek przy widocznym poleceniu, podpowiedziach i osi narzędzi). Reguła: tests/stream-verdict.test.ts (13 przypadków, m.in. „tresc widziana tylko po zakonczeniu nie zalicza asercji”, „dwie probki o pelnej dlugosci to nadal nie strumieniowanie”). Obserwacja wewnątrz strony z fazą czytaną w tym samym takcie (e2e/support/streamProbe.ts). | — | — | T05 | brak oceny (kryterium spoza 95) |
| **L5.10** | Podgląd strumienia jest związany z rozmową i wykonaniem, a po zakończeniu ustępuje właściwej wiadomości bez utraty lub podwojenia odpowiedzi. | **potwierdzone** | test GUI bez modelu (szablon) | Podgląd per rozmowa i wykonanie: state/appState.ts (runs po conversationId), RUN_STARTED zeruje streamingText, RUN_FINISHED i RUN_ERROR go zwalniają (chat/runEvents.ts); tests/agui-stream.test.ts „tekst odpowiedzi jest gromadzony w trakcie tury i zwalniany po jej zamknieciu”, „strumien rozmowy B nie dotyka stanu rozmowy A”, „strumien z wyslania zajmuje przebieg, wiec podlaczenie w tle go pomija” (brak podwojenia przez dwóch konsumentów). GUI: e2e/streaming.spec.ts — po fazie końcowej pełny tekst w jednym assistant-message (lokator w trybie strict wyklucza drugą bańkę), kontrola „cala odpowiedz naraz na koncu” wykrywała wcześniej podwojenie 107 → 214; e2e/background-tasks.spec.ts: brak paska wykonania A w innej rozmowie; e2e/tool-activity.spec.ts „blad wykonania...”: tekst sprzed błędu zostaje. | — | — | T05 | brak oceny (kryterium spoza 95) |
| **L5.11** | Zgodność AG-UI jest sprawdzana dla schematów i zachowania potrzebnych zdarzeń, nie tylko ich nazw lub obecności pakietu. | **częściowe** | test kontraktu lub logiki (szablon) | tests/projection.test.ts uruchamia processStreamedMessage z @openuidev/react-headless 0.9.13 na bajtach SSE platformy i porównuje wynik z projekcją backendu (6 scenariuszy, w tym fragmentacja argumentów i błąd narzędzia, z ochroną przed pustym porównaniem); tests/agui-stream.test.ts „zdarzenie rozbite miedzy pakiety sieciowe jest skladane w calosc” (agUIAdapter biblioteki); tests/runtime.test.ts „koduje zdarzenia dokladnie tak, jak oczekuje agUIAdapter”. | Zgodność jest sprawdzana wobec parsera OpenUI, nie wobec schematów protokołu AG-UI: @ag-ui/core 0.0.53 jest tylko zależnością tranzytywną, żaden test nie waliduje RUN_STARTED, RUN_FINISHED (z dodatkowym durationMs), RUN_ERROR ani CUSTOM, które parser OpenUI ignoruje; ładunki CUSTOM poza platform.ui_command nie mają schematu ani wersji (packages/platform-contracts/src/agui.ts). | BL-08 | T06 | brak oceny (kryterium spoza 95) |
| **L5.12** | Most między tekstem, hookami SDK i zdarzeniami domeny zachowuje korelację; równoległe przebiegi nie korzystają ze wspólnego zmiennego kontekstu. | **częściowe** | symulacja (szablon) | packages/platform-server/src/agent/runtime.ts: serwer MCP i ToolCallContext budowane per wykonanie, hooki i messageId am_<runId> w zamknięciu wykonania, zdarzenia modułu przez ctx.emit do strumienia tego wykonania (analiza kodu). tests/background-runs.test.ts „zadanie w A trwa, gdy uzytkownik pracuje w B, i wyniki sie nie mieszaja” (symulacja); tests/agui-stream.test.ts „strumien rozmowy B nie dotyka stanu rozmowy A” (klient); e2e/ui-navigation.spec.ts „zadanie w tle nie przejmuje widoku innej rozmowy” (korelacja polecenia z rozmową). | Test równoległych wykonań używa dwóch osobnych instancji AgentRuntime i samego tekstu, więc współdzielony stan jednej instancji (#pendingPermissions, #pendingUiCommands, #conversationQueue) oraz korelacja hooków narzędzi i zdarzeń domeny przy równoległych przebiegach nie są sprawdzone. | BL-08 | T07, T22 | brak oceny (kryterium spoza 95) |
| **L5.13** | Kolejność tekst–narzędzie, narzędzie–tekst, wiele narzędzi i brak tekstu prowadzą do poprawnego statusu i prezentacji. | **częściowe** | test GUI bez modelu (szablon) | tests/projection.test.ts: „narzedzie przed tekstem (najczestszy przypadek)”, „tekst przed narzedziem”, „samo narzedzie, bez tekstu”, „wiele narzedzi w jednej turze” zgodne z reduktorem biblioteki; tests/run-lifecycle.test.ts: status i firstTokenMs dla narzędzie→tekst, tekst→narzędzie i braku tekstu (symulacja). GUI (serwer skryptowany): e2e/tool-activity.spec.ts (narzędzie→tekst), e2e/streaming.spec.ts „wiadomosc uzytkownika i podpowiedzi nie zaliczaja asercji strumieniowania” (narzędzie bez tekstu, oś widoczna), „tekst dociera fragmentami...” (sam tekst). | Kolejności tekst→narzędzie i wielu narzędzi nie renderuje żaden test GUI: e2e/support/scripted-server.ts wywołuje wszystkie hooki narzędzi przed pierwszym fragmentem tekstu, więc nie potrafi odtworzyć tekstu przed narzędziem; scenariusz bez tekstu akceptuje fazę succeeded lub failed. Prezentacja tych kolejności jest dowiedziona tylko na poziomie reduktora. | BL-08 | T05, T06 | brak oceny (kryterium spoza 95) |
| **L5.14** | Reconnect, zamknięcie strumienia, anulowanie i restart nie tworzą sprzecznych stanów końcowych ani zakleszczenia odczytu. | **częściowe** | test GUI bez modelu (szablon) | tests/run-lifecycle.test.ts „czytelnik podlaczony po zamknieciu strumienia dostaje wszystko i konczy” (zakleszczenie N-3); tests/agui-stream.test.ts „anulowanie nie zostaje nadpisane przez blad, ktory po nim nastepuje”; tests/runtime.test.ts „kazde uruchomienie ma dokladnie jeden rozstrzygajacy status koncowy”, „restart oznacza przerwane zadania zamiast udawac kontynuacje”, „zatrzymanie serwera przerywa wszystkie uruchomienia”; e2e/background-tasks.spec.ts: reload w trakcie nie anuluje, Stop daje cancelled i pustą listę aktywnych. | Nie sprawdzono stanu UI po stopie z kompozytora (analiza: lokalny abort przerywa strumień przed platform.run_cancelled, pasek może zostać w running), reconnectu do wykonania przerwanego restartem (gałąź /api/runs/:id/stream syntetyzująca zdarzenie końcowe ze statusu nie ma testu) ani ponownie pokazanej prośby o zgodę po reload, dla której brak zdarzenia rozstrzygnięcia. | BL-08 | T16, T22 | brak oceny (kryterium spoza 95) |
| **L5.15** | Ograniczenie parsera biblioteki jest potwierdzone dla użytej wersji i obsłużone adapterem lub jawnym niespełnionym wymaganiem; nie ukrywa braku funkcji. | **częściowe** | test GUI bez modelu (szablon) | Ograniczenia potwierdzone analizą zainstalowanych wersji: processStreamedMessage w @openuidev/react-headless 0.9.13 nie obsługuje CUSTOM, RUN_STARTED ani RUN_FINISHED; InterleavedTurn w @openuidev/react-ui 0.13.10 (Thread.js) wstrzymuje prozę do końca tury (answer = !turnLive lub hasLangSyntax), a ścieżka GenUI nie renderuje prozy. Obsługa adapterami: chat/platformAdapter.ts (tests/agui-stream.test.ts „zdarzenia platformy trafiaja do wlasciwych odbiorcow”), podgląd RunState (e2e/streaming.spec.ts), chat/AssistantMessage.tsx (proza w e2e/streaming.spec.ts). Opis: docs/NEW-APPLICATION.md §7 (CUSTOM), raport archiwalny §7 punkt 1, FEEDBACK P5. | Ograniczenia nie są przypięte testem do wersji: aktualizacja biblioteki renderująca prozę w trakcie tury da podwójną odpowiedź niewykrytą przez sondę (czyta podgląd, gdy istnieje). Wstrzymanie prozy i brak prozy w GenUI nie są w bieżącej dokumentacji (NEW-APPLICATION §7 wymienia tylko CUSTOM). W turze z narzędziem biblioteka wstawia prozę trwającej tury do kroków osi (Thread.js steps) równolegle z podglądem — możliwe podwójne wyświetlenie w trakcie, nieopisane i niesprawdzone. | BL-08 | T05 | brak oceny (kryterium spoza 95) |
| **L5.16** | Polecenia prezentacyjne mają identyfikator, cel, rozmowę i wykonanie oraz potwierdzenie klienta; wysłanie zdarzenia nie jest uznawane za dowód pokazania elementu. | **potwierdzone** | test GUI bez modelu (szablon) | packages/platform-contracts/src/ui.ts (uiCommandSchema: commandId, runId, conversationId, targetId, spaceId; uiCommandResultSchema.executed), runtime.requestUiCommand czeka na POST /api/runs/:id/ui-ack. tests/ui-navigation.test.ts „brak potwierdzenia daje executed=false, nie sukces”, „polecenie niesie rozmowe i stabilny identyfikator”, „element nieobecny na ekranie to porazka z wlasnym powodem”, „cudze uruchomienie nie moze zostac potwierdzone”; e2e/ui-navigation.spec.ts „polecenie otwiera wskazany widok, a nie opisuje do niego drogi” (widoczny files-page i executed=true), „otwarcie ustawienia podswietla kontrolke i niczego nie zmienia” (data-ui-highlight), „niedostepny cel daje czytelna odmowe...”. Klient potwierdza po odnalezieniu elementu (shell/UiCommandRunner.tsx). Zastrzeżenia: scenariusz skryptowany woła bramkę runtime z pominięciem handlera ui_navigate; ack nie sprawdza, że commandId należy do runId z adresu. | — | — | T23 | brak oceny (kryterium spoza 95) |
| **L5.17** | Odłączenie odbiorcy strumienia przy zmianie rozmowy lub reload nie wywołuje anulowania zadania; jawne Stop ma odrębne znaczenie. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „przelaczenie rozmowy i przeladowanie nie anuluja zadania” (status queued/running w backendzie po zmianie rozmowy i po reload), „jawne Stop konczy wskazane wykonanie” (cancelled, brak aktywnych), „zadanie z innej rozmowy da sie zatrzymac z listy zadan”; tests/background-runs.test.ts „przerwanie odczytu strumienia nie zatrzymuje pracy” (symulacja). Kod: chat/chatWiring.ts nie przekazuje sygnału abort do /cancel, Stop działa przez stopRun na nazwanym wykonaniu. Kontrola siły (przywrócenie abort→cancel oblewa) opisana w raporcie, poza regresją. | — | — | T22, T16 | brak oceny (kryterium spoza 95) |

### Warstwa 6 — Kontekst aplikacji dla agenta

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L6.1** | Kontekst obejmuje aktualną rozmowę, zasób, zaznaczenie, filtry i identyfikację kompozycji. | **częściowe** | test GUI bez modelu (szablon) | packages/platform-contracts/src/agent.ts (appContextSchema: conversationId, spaceId, resource, selection, filters, viewport, drafts) budowany w state/appState.ts toAppContext; e2e/chat.spec.ts „czat pokazuje, do jakiego zasobu odnosi sie rozmowa” i „zaznaczenie karty trafia do kontekstu rozmowy” (pasek kontekstu po stronie klienta). | Filtry nigdy nie trafiają do kontekstu: setFilter nie jest wywoływany; zawężenie widoku z 2026-09-17 żyje w adresie URL i w agentFilterKey, a nie w AppContext.filters, więc get_context zwraca filters={} także przy zawężonym ekranie. Identyfikacja kompozycji to tylko spaceId, bez wersji. Brak testu treści kontekstu odebranego przez backend. | BL-11 | T08, T26 | potwierdzone — kod: schemat obejmuje rozmowę, przestrzeń, zasób, zaznaczenie, filtry, viewport i szkice; wypełnienie filtrów niebadane |
| **L6.2** | Zmiana wyboru w UI zmienia kontekst kolejnego polecenia. | **częściowe** | test GUI bez modelu (szablon) | packages/platform-ui/src/chat/chatWiring.ts (send czyta useAppState.getState() przy wysłaniu); e2e/chat.spec.ts „zaznaczenie karty trafia do kontekstu rozmowy” — kliknięcie karty dodaje card: do paska kontekstu, „wyczysc” je usuwa. | Test kończy się na stanie klienta; żaden test nie sprawdza, że appContext kolejnego polecenia (ciało POST /api/agui/run, agent_runs, prompt lub get_context) zawiera nowe zaznaczenie. Dowód FEEDBACK #21 był tylko historyczny. | BL-11 | T08 | potwierdzone — przebieg hist. FEEDBACK #21: kontekst czytany przy wysłaniu, zaznaczona pozycja trafia do właściwego rekordu |
| **L6.3** | Agent potrafi pobrać aktualny kontekst podczas dłuższego zadania. | **niespełnione** | analiza kodu (szablon) | packages/platform-server/src/agent/platform-tools.ts (get_context) zwraca ctx.appContext, który agent/runtime.ts (#execute, toolCtx) ustala raz z ciała POST /api/agui/run; packages/platform-server/src/http/app.ts nie ma kanału aktualizacji kontekstu trwającego wykonania. Testy tylko wymieniają nazwę narzędzia (tests/mcp-schema.test.ts, tests/platform-boundary.test.ts). | Narzędzie zwraca wyłącznie snapshot ze startu, co specyfikacja wprost wyklucza; zmiana zaznaczenia, zasobu lub szkicu w trakcie długiego zadania jest dla agenta niewidoczna. Historyczny przebieg dowodził tylko, że model wywołał narzędzie. | BL-11 | T08 | potwierdzone — przebieg raw-probe-canvas.log: model wywołał get_context; nie badano, czy zwraca kontekst nowszy niż start |
| **L6.4** | Kontekst przesłany przez frontend jest walidowany i nie nadaje uprawnień backendowych. | **częściowe** | test kontraktu lub logiki (szablon) | appContextSchema nie ma pola właściciela; ownerId pochodzi z sesji (http/app.ts, c.get('ownerId')), serwisy sprawdzają właściciela (conversations #row, canvas.getState). tests/contracts.test.ts „drugi uzytkownik nie widzi cudzej sprawy — mimo poprawnego identyfikatora”, „identyfikator wlasciciela z ciala zadania nie daje dostepu” (w praktyce: cudze ciasteczko daje 403 na zasób modułu); tests/ui-navigation.test.ts „cudza przestrzen pracy jest odrzucona przed dotknieciem interfejsu”. | Żaden test nie wysyła do /api/agui/run sfałszowanego lub niepoprawnego appContext (cudzy spaceId, resource, conversationId). Niepoprawny kontekst kończy się ZodError zamapowanym na 500 internal zamiast validation_failed, a spaceId z kontekstu jest zapisywany do rozmowy (create, bindSpace) bez sprawdzenia właściciela przestrzeni — nie daje odczytu, ale tworzy niespójne powiązanie. | BL-11 | T12, T09 | potwierdzone — test contracts + 04-probes-api: brak pola właściciela, identyfikator z ciała daje 403 |
| **L6.5** | Agent odróżnia roboczy stan formularza od danych zapisanych. | **niesprawdzone** | — (—) | Implementacja istnieje: state/appState.ts toAppContext wysyła szkice bez wartości (formId, entity, entityId, dirtyFields), agent/prompt.ts oznacza je jako niezapisane, get_context dodaje notę unsavedDrafts, module-procurement/src/ui/cards.tsx ustawia szkic przy edycji. W szablonie brak testu (z modelem lub bez), który sprawdza, że szkic trafia do kontekstu i że agent podaje wartość zapisaną. | Jedynym dowodem jest historyczna sonda raw-probe-draft.log w AgenticApp (wcześniej FEEDBACK P11: nierozwiązane). Skutek: regresja oznaczenia szkicu lub zachowania modelu wobec szkicu nie zostanie wykryta. | BL-03 | T08 | potwierdzone — przebieg raw-probe-draft.log: model podał wartość zapisaną (5) i odnotował szkic; sonda poza regresją |
| **L6.6** | Polecenie odnoszące się do aktualnego elementu prowadzi do operacji na właściwym rekordzie, co potwierdza wynik backendu. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts (prawdziwy model): polecenie dodania wykresu kosztów „dla tej sprawy” wysłane w przestrzeni sprawy; backend potwierdza nową kartę procurement.costChart w tej przestrzeni (scoped.id) i nienaruszone poprzednie karty. | Test nie sprawdza, że karta wskazuje właściwą sprawę (props caseId), a po przeładowaniu w kontekście jest tylko spaceId (resource ustawia wyłącznie strona sprawy), więc aktualnym elementem jest przestrzeń, nie rekord. Brak próby z zaznaczonym rekordem i mutacją domeny potwierdzoną wynikiem backendu (FEEDBACK #21 tylko historycznie). | BL-03 | T09 | potwierdzone — przebieg hist. FEEDBACK #21 i raw-probe-canvas.log: polecenie o wskazanym elemencie trafia we właściwy rekord |
| **L6.7** | Większe zbiory są pobierane selektywnie z paginacją lub limitem, a nie dołączane w całości do każdego promptu. | **częściowe** | analiza kodu (szablon) | agent/prompt.ts zawiera identyfikatory kontekstu i jednozdaniowy opis zasobu, bez danych biznesowych (analiza kodu). Limity: procurement_search (domyślnie 20, maks. 50 w services.ts), procurement_list_offers (limit 25). tests/contracts.test.ts „wyszukiwanie znajduje rekordy po fragmencie nazwy” wywołuje search z limitem, ale nie sprawdza jego egzekwowania. | procurement_list_cases, procurement_get_case, canvas_list_cards, files_list i ui_catalog zwracają pełne zbiory bez paginacji ani limitu; list_offers przycina dopiero po pobraniu całej sprawy. Brak testu limitu odczytu. | BL-11 | T09 | potwierdzone — kod: limit w list_offers i search; prompt bez danych biznesowych poza opisem zasobu |
| **L6.8** | Kontekst polecenia jest odczytywany w chwili wysłania, a nie utrwalony przy montowaniu komponentu. | **niesprawdzone** | — (—) | chat/chatWiring.ts createChatLlm: obiekt llm tworzony raz (useMemo w ChatPanel.tsx), ale send odczytuje useAppState.getState().toAppContext() przy każdym wysłaniu — analiza kodu wskazuje odczyt w chwili wysłania. Brak testu, który zmienia kontekst po zamontowaniu czatu i sprawdza kontekst odebrany przez backend. | Brak wykonywanego dowodu; e2e/agent-ui.spec.ts przeładowuje stronę przed poleceniem, więc kontekst przy montowaniu i przy wysłaniu jest ten sam i nie rozróżnia obu wariantów. | BL-11 | T08 | brak oceny (kryterium spoza 95) |
| **L6.9** | Próba zmiany zaznaczenia lub szkicu podczas długiego wykonania wykazuje, że agent potrafi odczytać nowszy kontekst, z rozróżnieniem go od kontekstu startowego. | **niespełnione** | analiza kodu (szablon) | Jak L6.3: toolCtx.appContext w agent/runtime.ts jest niezmienny przez całe wykonanie, brak endpointu aktualizacji kontekstu; żaden test nie zmienia zaznaczenia ani szkicu w trakcie wykonania. | Agent nie ma kanału odczytu nowszego kontekstu, więc nie może go odróżnić od startowego; próba zmiany zaznaczenia lub szkicu podczas pracy (T08) jest niewykonalna w obecnym kodzie. | BL-11 | T08 | brak oceny (kryterium spoza 95) |
| **L6.10** | Operacja rozpoczęta dla konkretnego rekordu nie zmienia celu na skutek późniejszej nawigacji; aktualność wersji jest sprawdzana przy zapisie. | **częściowe** | test kontraktu lub logiki (szablon) | Cel operacji: toolCtx.appContext ustalony przy starcie (agent/runtime.ts), więc późniejsza nawigacja nie zmienia celu z konstrukcji (analiza kodu). Wersja przy zapisie: tests/contracts.test.ts „nie nadpisuje nowszych danych przy nieaktualnej wersji”, „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu”. | expectedVersion w procurement_update_offer_item i expectedSpecVersion w canvas_update_card są opcjonalne (services.ts: expected = input.expectedVersion ?? item.version), więc zapis agenta bez wersji nie sprawdza aktualności. Brak testu, że nawigacja w trakcie wykonania nie przenosi mutacji na inny rekord. | BL-11 | T08 | brak oceny (kryterium spoza 95) |
| **L6.11** | Brak lub nieaktualność zasobu i utrata dostępu są odróżniane od pustego wyniku; agent nie uzupełnia braków wymyślonymi danymi. | **częściowe** | test GUI bez modelu (szablon) | e2e/access-context.spec.ts „po przelaczeniu zaden wiersz poprzedniej tozsamosci nie jest widoczny” (access-denied z kodem forbidden/not_found, odróżniony od pustej listy); tests/live-artifacts.test.ts „brak dostepu nie jest przedstawiany jako swieze dane”; e2e/app.spec.ts „brakujace dane sa oznaczone jako brakujace”; narzędzia zgłaszają AppError not_found/forbidden. Od d142b85: procurement_search zwraca totals (liczby spraw, dostawców, pozycji niezależnie od zapytania), a "*" zwraca wszystko — „0 pasujących z N” jest odróżnione od pustej aplikacji (tests/view-filter.test.ts, test kontraktu). | describeResource modułu (module-procurement/src/server/index.ts) zwraca null przy każdym błędzie, więc get_context i prompt nie odróżniają usuniętego, cudzego i nieopisanego zasobu. Zachowanie agenta (brak zmyślania przy braku danych) nie ma testu z modelem w szablonie. | BL-11 | T09, T12 | brak oceny (kryterium spoza 95) |
| **L6.12** | Przełączenie rozmowy, przestrzeni i właściciela nie pozostawia kontekstu poprzedniego zakresu w następnym poleceniu. | **niespełnione** | analiza kodu (szablon) | Zmiana tożsamości: api/queries.ts switchAccessContext → api/accessContext.ts setAccessContext czyści cache i przerywa żądania (e2e/access-context.spec.ts), ale nie zeruje useAppState. Zmiana przestrzeni: appState.setSpace nie czyści selection (czyści ją tylko setResource). Zmiana rozmowy: ConversationSync ustawia conversationId i przestrzeń rozmowy. | Po przełączeniu tożsamości bez przeładowania kolejne polecenie niesie resource, spaceId, selection, drafts i conversationId poprzedniego właściciela; po zmianie przestrzeni zaznaczone karty poprzedniej przestrzeni trafiają do następnego polecenia. Backend odrzuca cudze zasoby, ale kontekst poprzedniego zakresu trafia do promptu. Brak testu kontekstu następnego polecenia po którymkolwiek przełączeniu. | BL-11 | T12, T22 | brak oceny (kryterium spoza 95) |
| **L6.13** | Agent może poznać katalog dostępnych widoków i semantycznych celów UI oraz wywołać dozwoloną nawigację i podświetlenie przez typowany kontrakt. | **potwierdzone** | rzeczywisty model (szablon) | Katalog: registry/ui-targets.ts i uiTargets modułów; handler ui_catalog wykonywany w tests/view-filter.test.ts „ui_catalog podaje filterableFields tylko dla widokow, ktore je deklaruja”; handler ui_navigate z bramką runtime w tests/ui-navigation.test.ts (executed/reason, element nieobecny, cudza przestrzeń); nawigacja przez typowany kontrakt MCP wywołana przez PRAWDZIWY model w e2e/agent-ui.spec.ts „pytanie o dane przenosi na ich widok…” (czysta kopia d142b85) oraz ui_filter przez prawdziwy model w sondzie (docs/evidence/template-update-2026-09-17/przebieg-d142b85/09-sonda-ui-filter-model.json, potwierdzenia executed=true). Od d142b85 narzędzia ui_* mają alwaysLoad, więc są w kontekście modelu, a nie za ToolSearch. Zastrzeżenia: podświetlenie elementu przez prawdziwy model nie ma próby (dowód testem handlera i serwerem skryptowanym); nie sprawdzano, czy model wywołał ui_catalog przed wyborem celu (lista celów jest też w promptcie). | — | — | T23 | brak oceny (kryterium spoza 95) |
| **L6.14** | Zadanie poprzedniej rozmowy nie przejmuje nowego zaznaczenia ani przestrzeni; prezentacja jego wyniku w aktywnym UI wymaga właściwego kontekstu odbiorcy. | **częściowe** | test GUI bez modelu (szablon) | e2e/ui-navigation.spec.ts „zadanie w tle nie przejmuje widoku innej rozmowy” (ścieżka bez zmian, inactive_conversation w historii właściwej rozmowy); shell/UiCommandRunner.tsx odmawia polecenia z nieoglądanej rozmowy; tests/agui-stream.test.ts „zakonczenie poza ogladana rozmowa jest oznaczone jako niezobaczone” (bez zmiany conversationId); shell/BackgroundTasks.tsx: przejście do zadania tylko kliknięciem. | Brak testu, że zadanie A po zmianie przez użytkownika zaznaczenia lub przestrzeni nadal operuje na przestrzeni i zaznaczeniu ze startu — wynika to tylko z niezmiennego snapshotu kontekstu (analiza kodu), bez próby T22 z nowym zaznaczeniem. | BL-11 | T22, T27 | brak oceny (kryterium spoza 95) |
| **L6.15** | Agent może odczytać semantyczny opis aktywnego UI: widok, wersję kompozycji, instancje komponentów, powiązania rekord–pole, aktualne filtry, sortowanie i dozwolone akcje. Katalog komponentów sam w sobie nie zalicza tego kryterium. | **niespełnione** | analiza kodu (szablon) | Brak kontraktu opisującego aktywny widok: get_context zwraca identyfikatory kontekstu; canvas_list_cards opisuje karty bieżącej przestrzeni; ui_catalog od d142b85 podaje deklarowane filterableFields widoków (co wolno zawęzić), ale nie aktualny stan ekranu; AppContext nie ma sortowania, a filtry nie są wypełniane. | Agent nie może odczytać widoku, wersji kompozycji, instancji komponentów z powiązaniami rekord–pole, AKTUALNYCH filtrów (zawężenie jest w adresie, poza kontekstem), sortowania ani dozwolonych akcji aktywnego UI. | BL-01 | T25, T26 | brak oceny (kryterium spoza 95) |
| **L6.16** | Istnieje sprawdzalne mapowanie rekord–pole na cel prezentacyjny; niejednoznaczność, brak renderera i brak uprawnień są rozróżniane. Pomyślne wyszukanie wartości nie jest raportowane jako jej pokazanie. | **niespełnione** | analiza kodu (szablon) | UiTarget (packages/platform-contracts/src/ui.ts) ma tylko identyfikator, trasę i selektor elementu platformy lub modułu; nie ma mapowania rekord–pole na cel prezentacyjny. UI_COMMAND_FAILURES rozróżnia unknown_target, not_present, forbidden, inactive_conversation, no_client — bez niejednoznaczności i braku renderera. | Brak mapowania rekord–pole, rozstrzygania niejednoznaczności i rozróżnienia braku renderera; nie da się wskazać wartości pola rekordu na ekranie ani odróżnić znalezienia wartości od jej pokazania (T25). | BL-01 | T25 | brak oceny (kryterium spoza 95) |
| **L6.17** | Po zmianie filtra lub widoku agent potrafi odczytać potwierdzony nowy stan UI; nie opiera dalszej odpowiedzi wyłącznie na stanie sprzed akcji. Kontekst ma wersję lub znacznik pozwalający wykryć nieaktualność. | **częściowe** | rzeczywisty model (szablon) | Od d142b85 wynik ui_filter zawiera potwierdzenie klienta z liczbami zgłoszonymi przez widok (filtered.matched/total), url i executed, z odróżnieniem not_applied (tests/view-filter.test.ts). Sonda na PRAWDZIWYM modelu (docs/evidence/template-update-2026-09-17/przebieg-d142b85/09-sonda-ui-filter-model.json): po potwierdzeniach 0 z 4 dla country=Polska i country=Poland model poprawił wartość na PL (3 z 4) — dalsze działanie oparł na potwierdzonym stanie po akcji, nie na stanie sprzed niej. | Potwierdzony stan jest dostępny tylko jako wynik narzędzia w tej samej turze: AppContext nie zawiera zawężenia ani widoku, nie ma wersji lub znacznika świeżości kontekstu, więc w kolejnej turze agent nie odczyta nowego stanu UI ani nie wykryje nieaktualności. | BL-01 | T26, T25 | brak oceny (kryterium spoza 95) |

### Warstwa 7 — Orkiestracja backendowa

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L7.1** | Claude SDK jest zarejestrowany i wywoływany przez oficjalną integrację Mastry. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” na produkcyjnym serwerze Playwright bez stand-inu: uruchomienie succeeded, zdarzenia TOOL_CALL_START mcp__app__*, powiązana sesja Claude. Kod: packages/platform-server/src/agent/runtime.ts (AgentRuntime: new Mastra({ agents: { appAgent: ClaudeSDKAgent } }), getter agent → mastra.getAgent('appAgent'), stream/resumeStream); apps/server/src/compose.ts nie ustawia modelAgent. Test nie asercjonuje wprost przejścia przez Mastrę — wynika to z analizy jedynej ścieżki produkcyjnej; query() bezpośrednio tylko w apps/server/src/cli/diag-agent.ts. | — | — | — | potwierdzone — przebieg: wszystkie przebiegi audytu AgenticApp przez mastra.getAgent; query() z SDK tylko w diagnostyce |
| **L7.2** | Żądanie aplikacji jest powiązane z wykonaniem, kontekstem i diagnostyką. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc”: polecenie wpisane w GUI przy otwartej sprawie → /api/conversations/:id/runs (status succeeded, claudeSessionId, firstTokenMs, queuedMs) → /api/runs/:id/events (TOOL_CALL_START, TOOL_CALL_RESULT, RUN_FINISHED); nowa karta procurement.costChart w przestrzeni sprawy wskazanej kontekstem. Kod: http/app.ts POST /api/agui/run (X-Run-Id, X-Conversation-Id), services/runs.ts (agent_runs.app_context, run_events). Nie asercjonowane: nagłówki X-Run-Id i zapisany app_context uruchomienia. | — | — | T22 | potwierdzone — przebieg: nagłówki X-Run-Id/X-Conversation-Id i run_events instancji audytowej AgenticApp |
| **L7.3** | Wynik, błąd i anulowanie przechodzą przez warstwę orkiestracji do klienta. | **częściowe** | rzeczywisty model (szablon) | Wynik: e2e/agent-ui.spec.ts — faza succeeded w GUI przez Mastrę i SDK. Błąd: e2e/tool-activity.spec.ts „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika” (run-error z kodem rate_limited). Anulowanie: e2e/measurements.spec.ts „zatrzymanie z interfejsu konczy wykonanie i nie zostawia zapisow”, e2e/background-tasks.spec.ts „jawne Stop konczy wskazane wykonanie”, tests/background-runs.test.ts „konczy wskazane wykonanie i nie rusza pozostalych”. Błąd i anulowanie sprawdzone wyłącznie ze stand-inem ModelAgentLike, który zastępuje mastra.getAgent — nie przechodzą przez Mastrę, adapter ani proces SDK. | Brak powtarzalnej próby błędu i Stop przez rzeczywisty adapter @mastra/claude i proces SDK (historycznie tylko raw-probe-stop.log w AgenticApp); w szablonie nie wiadomo, czy sygnał przerwania kończy proces SDK i daje cancelled w GUI. | BL-03 | T16, T22 | potwierdzone — przebieg: raw-probe-stop.log i run 6c9c63 (server_sigterm) w AgenticApp; sonda poza regresją |
| **L7.4** | Równoległe żądania do tej samej sesji nie powodują niekontrolowanych wyścigów ani mieszania kontekstów. | **niesprawdzone** | — (—) | Kod: runtime.ts #conversationQueue (łańcuch obietnic per rozmowa, ponowny odczyt claudeSessionId na czele kolejki) i buildMcpServer per uruchomienie z kontekstem w domknięciu. Żaden test w tests/ ani e2e/ nie wysyła dwóch równoczesnych żądań do tej samej rozmowy; tests/background-runs.test.ts dotyczy różnych rozmów i używa dwóch osobnych instancji AgentRuntime. | Brak deterministycznej próby (stand-in) dwóch równoległych żądań w jednej rozmowie: kolejność wykonania, wznowienie właściwej sesji przez drugie zadanie i brak przecieku kontekstu narzędzi między uruchomieniami; regresja kolejki nie zostałaby wykryta. | BL-11 | T10 | potwierdzone — przebieg: sonda raw-probe-conc2.log w AgenticApp (rozłączne okna); pierwsza wersja sondy myliła start z zakolejkowaniem |
| **L7.5** | Orkiestracja nie omija serwisów domenowych przy mutacjach. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „narzedzie MCP i endpoint HTTP prowadza do tej samej reguly” (procurement_update_offer_item przez registry.callTool i PATCH → domain_rule_violated) oraz „porownanie z HTTP i z MCP daje identyczne sumy”. Analiza kodu: agent/mcp.ts buildMcpServer wywołuje def.handler (te same handlery co registry.callTool), handlery modułu wołają ProcurementService; runtime.ts pisze bezpośrednio tylko do agent_runs (workspace_dir). Nie obejmuje obejścia przez wbudowane narzędzia plikowe agenta (L9.16). | — | — | — | potwierdzone — test: contracts.test.ts „…ta sama reguła”; narzędzia MCP jako nakładki na te same serwisy co HTTP |
| **L7.6** | Zakres własnych adapterów i wykorzystanych mechanizmów Mastry jest udokumentowany. | **potwierdzone** | analiza kodu (szablon) | docs/NEW-APPLICATION.md §7 „Znane pułapki stosu” (most hooków dla @mastra/claude 0.3.1, kanał CUSTOM, allowedTools), docs/observability.md (Mastra 1.66.0, telemetria adaptera), komentarze klas runtime.ts i mcp.ts; pełny rejestr własnych adapterów §6.1–6.10 w docs/archive/agenticapp-2026-09/FEEDBACK.md (kod identyczny) i §3 raportu domknięcia. Opis luki adaptera zgodny z zainstalowanym @mastra/claude 0.3.1 (runClaudeAsMastraStream emituje tylko delty tekstu). Szczegółowy rejestr jest w archiwum, bieżące dokumenty zawierają skrót. | — | — | — | potwierdzone — kod: FEEDBACK.md §6 (rejestr własnych adapterów), dziś w archiwum szablonu |
| **L7.7** | Działanie nie wymaga Mastra Factory, dodatkowego harnessu AgentController ani hostingu Mastry. | **potwierdzone** | analiza kodu (szablon) | packages/platform-server/package.json i pnpm-lock.yaml: wyłącznie @mastra/core 1.66.0 i @mastra/claude 0.3.1, brak Mastra Factory, AgentController, deployera i serwera Mastry; apps/server/src/main.ts to zwykły proces Node z @hono/node-server. Rzeczywiste przebiegi e2e/agent-ui.spec.ts i e2e/files-agent.spec.ts działają na tym buildzie. | — | — | — | potwierdzone — kod: tylko @mastra/core i @mastra/claude; zwykły proces Node, bez Factory/AgentController/hostingu |
| **L7.8** | Okna rzeczywistego wykonania dwóch zadań tej samej sesji nie nakładają się; pomiar używa startu wykonania, a nie czasu zakolejkowania. | **częściowe** | test kontraktu lub logiki (szablon) | tests/runtime.test.ts „start wykonania jest osobnym, mierzalnym punktem wzgledem zakolejkowania” (queuedMs ≥ 20, startedAt > enqueuedAt, drugi markExecutionStart nie przesuwa startu — tylko rejestr); tests/run-lifecycle.test.ts „czas pierwszego tekstu liczy sie od startu wykonania, nie od zakolejkowania” (symulacja, słabe asercje: queuedMs ≥ 0, firstTokenMs ≤ durationMs). Kod: runtime.ts #execute woła markExecutionStart dopiero na czele kolejki rozmowy, durationMs liczone od startu wykonania. Żaden test nie obejmuje dwóch zadań tej samej rozmowy. | Brak próby z dwoma zadaniami jednej rozmowy sprawdzającej, że startedAt drugiego ≥ finishedAt pierwszego i że czekające ma queuedMs > 0; rozłączność okien potwierdzała tylko sonda w AgenticApp. | BL-11 | T10 | brak oceny (kryterium spoza 95) |
| **L7.9** | Różne rozmowy mogą działać równolegle bez mieszania narzędzi, odpowiedzi, zgód lub danych właściciela. | **częściowe** | symulacja (szablon) | tests/background-runs.test.ts „zadanie w A trwa, gdy uzytkownik pracuje w B, i wyniki sie nie mieszaja” (WYNIK-A i WYNIK-B tylko we własnych rozmowach) i „lista aktywnych zadan obejmuje wszystkie rozmowy wlasciciela i tylko jego”; tests/agui-stream.test.ts „prosba o zgode trafia do rozmowy zadania, nie do ogladanej” (stan klienta); tests/ui-navigation.test.ts „polecenie z rozmowy w tle nie przelacza widoku i mowi o tym wprost”. Test backendu używa dwóch osobnych instancji AgentRuntime. e2e/background-tasks.spec.ts „praca w rozmowie B nie miesza sie z zadaniem w A” restartuje serwer skryptowy przed testem, więc zadanie A nie trwa podczas pracy w B. | Nie sprawdzono równoległych uruchomień we wspólnej instancji runtime z wywołaniami narzędzi MCP (kontekst per uruchomienie), zgodami ani dwoma właścicielami; POST /api/runs/:id/permission sprawdza właściciela uruchomienia z adresu, ale answerPermission nie wiąże requestId z tym uruchomieniem, więc odpowiedź na zgodę nie jest ograniczona do wykonania z URL. | BL-11 | T10, T22 | brak oceny (kryterium spoza 95) |
| **L7.10** | Błąd i anulowanie zwalniają kolejkę; następne zadanie może osiągnąć poprawny wynik. | **niesprawdzone** | — (—) | Kod: runtime.ts start() — previous.catch(() => undefined).then(#execute), #execute łapie każdy błąd, finally usuwa wpis kolejki; services/runs.ts cancel() obsługuje też zadanie queued. Testy tylko obok treści: tests/runtime.test.ts „uruchomienie czekajace w kolejce mozna anulowac zanim ruszy” (sam rejestr, bez runtime) i tests/background-runs.test.ts „konczy wskazane wykonanie i nie rusza pozostalych” (inna rozmowa, inna instancja runtime). Żaden test nie uruchamia kolejnego zadania w tej samej rozmowie po błędzie lub anulowaniu; e2e/background-tasks.spec.ts restartuje serwer między testami. | Brak próby: błąd lub Stop zadania A, potem zadanie B w tej samej rozmowie kończy się succeeded i wznawia sesję (historycznie tylko raw-probe-stop.log w AgenticApp); zakleszczenie kolejki po błędzie nie zostałoby wykryte przez regresję. | BL-11 | T10, T16 | brak oceny (kryterium spoza 95) |
| **L7.11** | Narzędzia są dostępne w prawdziwej sesji SDK; niezgodny schemat nie usuwa serwera MCP po cichu bez diagnostyki. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts: w prawdziwej sesji wywołane narzędzie mcp__app__* (TOOL_CALL_START, TOOL_CALL_RESULT) i powstała karta; e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety” — files_publish_version utworzył wersję 2. tests/mcp-schema.test.ts „odrzuca z.record() w schemacie narzedzia”, „odrzuca .default() na polu najwyzszego poziomu”, „wszystkie realne narzedzia aplikacji przechodza kontrole”; agent/mcp.ts assertMcpCompatibleShape rzuca błąd już przy budowie serwera w konstruktorze AgentRuntime. Nie sprawdzono pełnej listy narzędzi widocznych w sesji. | Strażnik obejmuje tylko znane przypadki (z.record, z.map, .default); inny niekonwertowalny schemat nadal usunąłby serwer po cichu, bo runtime nie porównuje zadeklarowanych narzędzi z listą w sesji SDK. Porównanie wykonuje tylko pnpm diag (apps/server/src/cli/diag-agent.ts), ręcznie i poza regresją. | BL-03 | — | brak oceny (kryterium spoza 95) |
| **L7.12** | Zastosowanie mostu hooków jest uzasadnione brakami rzeczywistego adaptera; nie powstają dwie niezależne pętle agentowe dla tego samego zadania. | **potwierdzone** | analiza kodu (szablon) | runtime.ts: komentarz klasy i hookCallback (SessionStart, PreToolUse, PostToolUse, PostToolUseFailure, Stop) przekazywany w sdkOptions jednego wywołania agent.stream/resumeStream; w ścieżce wykonania nie ma drugiego query(). Luka uzasadniająca most potwierdzona w zainstalowanym @mastra/claude 0.3.1: runClaudeAsMastraStream emituje tylko delty tekstu, wywołania narzędzi trafiają do telemetrii, session_id nie jest udostępniany. e2e/agent-ui.spec.ts pokazuje zdarzenia narzędzi z mostu w jednym uruchomieniu na modelu. | — | — | — | brak oceny (kryterium spoza 95) |
| **L7.13** | Brak transkryptu SDK dla zachowanej rozmowy daje jawny wynik odzyskiwania lub błąd; aplikacja nie deklaruje zachowania pamięci, której nie odtworzyła. | **niespełnione** | analiza kodu (szablon) | Analiza kodu: runtime.ts #execute woła resumeStream z conversation.claudeSessionId bez sprawdzenia istnienia transkryptu; hookCallback przy odmiennym session_id po cichu przepina rozmowę (bindSession), a błąd wznowienia trafia do classifyModelError jako integration_failed i ustawia stan dostępu failed. Brak testu i brak kodu obsługi; raport wykonawcy §6 i §7 pkt 3 wymienia ten przypadek jako niewywołany. | Aplikacja nie wykrywa brakującego transkryptu SDK i nie daje jawnego wyniku odzyskiwania: zależnie od niesprawdzonego zachowania SDK nowa sesja zostaje podpięta bez informacji (historia UI sugeruje zachowaną pamięć) albo polecenia kończą się ogólnym błędem, a zapisany claude_session_id nie jest czyszczony. | BL-07 | T16 | brak oceny (kryterium spoza 95) |

### Warstwa 8 — Harness i uwierzytelnienie Claude

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L8.1** | Wykonanie korzysta z pętli i narzędzi Claude SDK, a nie wyłącznie modelu Claude w routerze LLM. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc”: zdarzenia TOOL_CALL_START/RESULT z hooków SDK dla narzędzi mcp__app__*, mutacja karty w backendzie, następnie odpowiedź (>3 zdarzenia TEXT_MESSAGE_CONTENT); e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety”: wielokrokowa praca z Bash po zgodzie (asercja /Bash/ w zgodach) i files_publish_version. Hooki, bramka canUseTool i narzędzia wbudowane istnieją tylko w pętli Claude Agent SDK. | — | — | T17, T24 | potwierdzone — przebieg: raw-probe-canvas.log (ToolSearch + 4 narzędzia MCP) w AgenticApp |
| **L8.2** | Działa uwierzytelnienie subskrypcyjne, z potwierdzeniem trybu bez ujawniania tokena. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts i e2e/files-agent.spec.ts przechodzą na prawdziwym modelu z procesem SDK uruchamianym w środowisku subscriptionOnlyEnv (bez ANTHROPIC_API_KEY, ANTHROPIC_BASE_URL). Tryb: e2e/app.spec.ts „ustawienia pokazuja tryb subskrypcji i nie ujawniaja sekretow” (tekst subskrypcja lub brak logowania, brak sk-ant/accesstoken/refreshtoken); tests/runtime.test.ts „rzeczywista wartosc tokena nie wystepuje w odpowiedzi /api/status”. Etykieta subskrypcja w SettingsPage wynika ze stałej method='subscription' w probeAuth, nie z wykrycia. | Żaden test nie potwierdza, że SDK użyło OAuth subskrypcji: apiKeySource z sesji SDK nie jest odczytywane, a po udanym przebiegu nie jest asercjonowany stan access=verified ani plan z poświadczenia; asercja w e2e/app.spec.ts przechodzi także bez logowania. | BL-04 | T17 | potwierdzone — przebieg: /api/status instancji audytowej AgenticApp — mode=subscription, plan=max, bez tokena |
| **L8.3** | Klucz API Anthropic, gateway i automatyczny fallback płatnego API nie są aktywną ścieżką wykonania. | **częściowe** | test kontraktu lub logiki (szablon) | tests/auth.test.ts „kazda zmienna kierujaca na platne API znika ze srodowiska agenta” (klucz, token, base URL, Bedrock, Vertex, model) i „obecnosc klucza API jest raportowana, a polityka pozostaje "refused"”; tests/runtime.test.ts „usuwa zmienne kierujace na platne API i gateway” i „izoluje proces potomny od nadrzednej sesji Claude Code, ale zostawia CLAUDE_CONFIG_DIR”. Kod: runtime.ts przekazuje env: subscriptionOnlyEnv() i settingSources: []; @mastra/claude 0.3.1 scala sdkOptions konstruktora z sdkOptions wywołania; brak kodu fallbacku. | Polityka opiera się tylko na czyszczeniu zmiennych środowiska: logowanie CLI kontem z kluczem API (ApiKeySource '/login managed key' w SDK 0.3.270) nie jest w kodzie wykluczone ani wykrywane, bo runtime nie sprawdza apiKeySource sesji; brak próby z ustawionym kluczem pokazującej, że przebieg nadal idzie przez subskrypcję. | BL-04 | T17 | potwierdzone — symulacja: 03-probes-auth.txt — klucz wykryty, oznaczony jako odrzucony i usuwany z procesu potomnego |
| **L8.4** | Narzędzia MCP są dostępne w SDK i prawdziwe wywołanie zwraca wynik do dalszej pracy agenta. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts: narzędzie mcp__app__* ma TOOL_CALL_START i TOOL_CALL_RESULT, historia paruje toolCallId z wiadomością role=tool, agent kontynuuje odpowiedzią ze znacznikiem i kartą procurement.costChart w przestrzeni sprawy; e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety”: łańcuch narzędzi kończy się files_publish_version z fileId oryginału (wersja 2, derivedFromFileId, B1 równe WORKBOOK_TOTAL). Kompletność listy narzędzi w sesji nie jest sprawdzana (L7.11). | — | — | T17, T24 | potwierdzone — przebieg: pnpm diag (hist.) 21 narzędzi; wyniki używane przez model w przebiegach audytu AgenticApp |
| **L8.5** | Sesja jest kontynuowana przez jej właściwy identyfikator, bez powielania historii. | **częściowe** | test GUI bez modelu (szablon) | e2e/session-restore.spec.ts „dalsze polecenie po przeladowaniu kontynuuje te sama sesje”: po przeładowaniu drugie polecenie trafia do tej samej rozmowy (1 wątek, 2 uruchomienia, claudeSessionId bez zmian, więcej wiadomości). Serwer skryptowy zawsze zwraca sess_scripted, a stand-in traktuje resumeStream jak stream, więc test nie odróżnia wznowienia od nowej sesji. Kod: runtime.ts resumeStream({ message, sessionId: conversation.claudeSessionId }). e2e/agent-ui.spec.ts sprawdza brak duplikacji historii po przeładowaniu, ale dla jednej tury. | Brak próby na prawdziwym modelu z drugą turą w tej samej rozmowie (także po restarcie backendu), potwierdzającej wznowienie właściwego session_id, pamięć poprzedniej tury i brak powielenia historii; historycznie FEEDBACK #19 i raw-probe-stop.log w AgenticApp. | BL-03 | T03 | potwierdzone — przebieg: FEEDBACK #19 (hist.) i raw-probe-stop.log w AgenticApp; wznowienie po restarcie i po anulowaniu |
| **L8.6** | Wygaśnięcie uwierzytelnienia i wyczerpanie limitu dają czytelny błąd oraz zachowują stan pracy. | **częściowe** | symulacja (szablon) | Limit: e2e/tool-activity.spec.ts „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika” (stand-in rzuca Claude usage limit reached; GUI pokazuje rate_limited, kompozytor aktywny, tekst Zaczynam zachowany) i tests/run-lifecycle.test.ts „blad modelu konczy sie dokladnie jednym stanem koncowym i zapisuje przyczyne”. Wygaśnięcie: tests/auth.test.ts „plik z terminem w przeszlosci jest "stale", a nie zdrowa subskrypcja” oraz przypadki odmowy odnowienia i odwołanego logowania tylko na classifyAccessFailure. Symulacja, nie rzeczywiste wygaśnięcie ani wyczerpanie limitu. | Błąd wygasłego lub odwołanego logowania nie jest przeprowadzony przez runtime do GUI (brak scenariusza serwera skryptowego); czytelna podpowiedź (wykonaj /login, poczekaj) jest tylko w Ustawieniach i nie jest sprawdzana testem, czat pokazuje surowy komunikat SDK z kodem. | BL-04 | T17 | potwierdzone — test: auth.test.ts i tool-activity.spec (limit w UI); symulacja, rzeczywiste wygaśnięcie i limit niewywołane |
| **L8.7** | Poświadczenia pozostają poza frontendem, artefaktami i logami. | **częściowe** | test kontraktu lub logiki (szablon) | tests/durability.test.ts „zbudowany frontend nie zawiera wartosci tokena”, „zbudowany backend nie zawiera wartosci tokena”, „baza danych aplikacji nie zawiera wartosci tokena”, „diagnostyka uruchomieniowa nie wypisuje tokena” (pnpm migrate); tests/runtime.test.ts „rzeczywista wartosc tokena nie wystepuje w wyniku probeAuth()” i „…w odpowiedzi /api/status”; e2e/app.spec.ts „ustawienia pokazuja tryb subskrypcji i nie ujawniaja sekretow”. Testy czytają rzeczywisty ~/.claude/.credentials.json i przechodzą pusto (tylko ostrzeżenie), gdy pliku brak. | Nie przeszukano artefaktów i magazynu plików, run_events ani logów serwera po rzeczywistym uruchomieniu agenta; sandboxSettings nie blokuje odczytu katalogu ~/.claude, a Read jest auto-zatwierdzany, i nie sprawdzono, czy agent może odczytać poświadczenie i opublikować je jako artefakt. | BL-04 | T17 | potwierdzone — test: probeAuth i /api/status; wykonawca zaznaczył, że nie przeszukano logów serwera ani artefaktów |
| **L8.8** | Zgodność konkretnej wersji SDK, adaptera i sposobu logowania została sprawdzona rzeczywistym wywołaniem. | **częściowe** | rzeczywisty model (szablon) | Rzeczywiste wywołania w e2e/agent-ui.spec.ts i e2e/files-agent.spec.ts na wersjach z pnpm-lock.yaml (@anthropic-ai/claude-agent-sdk 0.3.270, @mastra/claude 0.3.1, @mastra/core 1.66.0) po pnpm install --frozen-lockfile; wersję CLI i sha lockfile zapisuje skrypt regresji. apps/server/src/main.ts loguje wersje przy starcie, ale Playwright nie utrwala wyjścia serwera. | Sposób logowania użyty przez SDK w tych przebiegach nie jest rejestrowany ani asercjonowany (apiKeySource, access.state), a wersje nie trafiają do wyniku testu; zgodność trójki SDK–adapter–logowanie wynika z założenia o środowisku, nie z zapisanego dowodu. | BL-04 | T17 | potwierdzone — przebieg: FEEDBACK #15/#18/#23 (hist.) i 02-versions.txt; trzy niezgodności znalezione i obsłużone w AgenticApp |
| **L8.9** | Sposób logowania, stan metadanych i ostatni potwierdzony dostęp są odrębnymi informacjami; obecność pliku nie oznacza zdrowego połączenia. | **częściowe** | test kontraktu lub logiki (szablon) | tests/auth.test.ts „plik z terminem w przeszlosci jest "stale", a nie zdrowa subskrypcja” (present=true, access=unverified), „przed pierwszym wywolaniem stan to "unverified", nie "verified"”, „nieczytelny plik jest odrozniony od braku pliku”, „blad nie kasuje faktu wczesniejszego udanego wywolania”; kontrakt authStatusSchema (method, credential, access) i SettingsPage z osobnymi wierszami (kod, bez testu GUI). | Pasek stanu (AppShell StatusBar) pokazuje zieloną kropkę i napis subskrypcja przy obecnym pliku, gdy dostęp jest unverified lub failed, więc obecność pliku nadal wygląda jak zdrowe połączenie; stan dostępu jest globalny w procesie i nadpisywany przez każdy nieanulowany błąd uruchomienia (także sandbox i timeout), a method jest stałą. | BL-04 | T17 | brak oceny (kryterium spoza 95) |
| **L8.10** | Przeterminowany access token nie blokuje automatycznie możliwości odświeżenia przez SDK; skuteczne i odrzucone odświeżenie mają sprawdzone zachowanie. | **częściowe** | symulacja (szablon) | tests/auth.test.ts „wygasniecie lokalnych metadanych samo w sobie nie blokuje uruchomienia” (authIsUsable=true dla stale) i „odmowa odnowienia → stan "refresh_refused" (symulacja na granicy adaptera)” (OAuth token refresh failed: invalid_grant); „odwolane logowanie czyni aplikacje niezdatna do pracy”. Kod: runtime.start nie sprawdza stanu poświadczenia, auth.ts tylko czyta plik i nie odświeża tokenów. Symulacja na klasyfikatorze, bez runtime i GUI. | Nie sprawdzono skutecznego odświeżenia (przeterminowane poświadczenie → udany przebieg → access verified i nowy termin) ani odrzuconego odświeżenia przez runtime do czatu i Ustawień; komunikat odmowy to założony tekst, niepotwierdzony dla SDK 0.3.270. | BL-04 | T17 | brak oceny (kryterium spoza 95) |
| **L8.11** | Limit użycia jest odróżniany od odwołanego logowania, błędu sieci i błędu narzędzia; zachowuje historię i nie powoduje automatycznej powtórki mutacji. | **częściowe** | symulacja (szablon) | tests/auth.test.ts: limit (usage limit, 429) → rate_limited, odwołanie i 401 → revoked, socket hang up → failed, „limit uzycia nie jest mylony z zepsutym logowaniem”; błąd narzędzia nie kończy przebiegu: e2e/tool-activity.spec.ts „blad narzedzia jest pokazany jako blad, nie jako wynik” (faza succeeded) i tests/run-lifecycle.test.ts „blad narzedzia jest zapisany jako blad, nie jako wynik”; zachowanie historii przy limicie: run-lifecycle „blad modelu konczy sie dokladnie jednym stanem koncowym i zapisuje przyczyne” i tool-activity „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika”. Kod: brak ponawiania w runtime.ts. | Brak próby, w której mutacja przez narzędzie poprzedza limit, z asercją jednego skutku i braku powtórki; klasyfikacja opiera się na podciągach komunikatu (np. 429, credential, authenticat) niepotwierdzonych na rzeczywistych błędach SDK, więc inny błąd sieci może zostać uznany za limit lub odwołanie. | BL-04 | T17 | brak oceny (kryterium spoza 95) |
| **L8.12** | Kontrolowane błędy uwierzytelnienia i limitu są sprawdzone na granicy adaptera aż do widocznego UI; symulacja nie jest opisana jako rzeczywiste wyczerpanie limitu. | **częściowe** | symulacja (szablon) | Limit do GUI: e2e/tool-activity.spec.ts „blad wykonania jest widoczny w aplikacji wraz z kodem, a praca nie znika” (scenariusz run-failure na granicy ModelAgentLike, run-error zawiera rate_limited). Błędy uwierzytelnienia tylko w tests/auth.test.ts na classifyAccessFailure i recordVerification. Symulacja jest oznaczona jako symulacja w nagłówkach tests/auth.test.ts, tests/run-lifecycle.test.ts i w raporcie wykonawcy §6. | Odwołane logowanie, odmowa odświeżenia i wygaśnięcie nie są przeprowadzone od granicy adaptera do widocznego UI (czat, pasek stanu, Ustawienia); stan Ustawień po symulowanym limicie też nie jest asercjonowany. | BL-04 | T17 | brak oceny (kryterium spoza 95) |
| **L8.13** | Brak sekretów jest sprawdzony w adekwatnych logach, odpowiedziach HTTP, trwałych danych, artefaktach i buildzie frontendu; dwa endpointy nie stanowią dowodu dla wszystkich powierzchni. | **częściowe** | test kontraktu lub logiki (szablon) | tests/durability.test.ts skanuje apps/web/dist, apps/server/dist, plik app.db po zapisie syntetycznej rozmowy i wyjście pnpm migrate; tests/runtime.test.ts probeAuth() i /api/status; tests/contracts.test.ts „nie ujawnia sekretow w statusie”; e2e/app.spec.ts tekst strony Ustawień. | Nie sprawdzono logów serwera podczas rzeczywistego przebiegu, bazy po przebiegu z modelem (run_events, messages), magazynu plików i artefaktów, katalogów workspace, innych odpowiedzi HTTP (np. /api/runs/:id/events) ani raportów i śladów Playwright; durability.test.ts szuka tokenów tylko w ~/.claude (ignoruje CLAUDE_CONFIG_DIR) i bez pliku przechodzi pusto. | BL-04 | T17 | brak oceny (kryterium spoza 95) |
| **L8.14** | Opis odczytu poświadczeń jest zgodny z kodem; tokeny nie są kopiowane do raportu lub śladów testów, a testy negatywne nie niszczą logowania użytkownika. | **częściowe** | analiza kodu (szablon) | Kod zgodny z komentarzem w packages/platform-server/src/agent/auth.ts: cały .credentials.json (~/.claude lub CLAUDE_CONFIG_DIR) jest parsowany, kopiowane są tylko subscriptionType i expiresAt, plik tylko czytany. tests/auth.test.ts używa syntetycznych poświadczeń w katalogu tymczasowym przekazywanym parametrem env, więc testy negatywne nie dotykają logowania użytkownika; tests/durability.test.ts formułuje asercje jako wartości logiczne z nazwą pliku. | README.md (Konfiguracja) podaje, że aplikacja czyta z pliku poświadczeń wyłącznie subscriptionType i expiresAt, pomijając przejściowy odczyt tokenów przy parsowaniu całego pliku; tests/runtime.test.ts używa expect(serialized).not.toContain(token), więc przy oblaniu komunikat Vitest wypisałby wartość tokena do logu regresji. | BL-04 | T17 | brak oceny (kryterium spoza 95) |

### Warstwa 9 — Model domeny i funkcje backendu

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L9.1** | Encje, relacje i reguły domenowe są opisane dla konkretnego produktu. | **potwierdzone** | analiza kodu (szablon) | packages/module-procurement/src/shared/index.ts: schematy Zod encji (sprawa, dostawca, wymaganie, oferta, pozycja oferty, pochodzenie, kryterium) z relacjami przez identyfikatory i regułami (kwoty w groszach, brak przeliczania walut i netto/brutto, powody wykluczeń); src/server/schema.ts: tabele pc_* z kluczami obcymi; agentBriefing w src/server/index.ts; reguły sprawdzone ręcznymi wyliczeniami w tests/domain-comparison.test.ts. Opis jest w kodzie modułu i skrócie README, bez osobnego dokumentu domeny. | — | — | T09 | potwierdzone — kod: schemat 8 tabel pc_* i shared/index.ts |
| **L9.2** | Endpointy i narzędzia MCP korzystają z tych samych reguł walidacji i mutacji. | **niespełnione** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „narzedzie MCP i endpoint HTTP prowadza do tej samej reguly” (unitPrice -5 przez procurement_update_offer_item i PATCH /api/m/procurement/items/:id → domain_rule_violated) i „porownanie z HTTP i z MCP daje identyczne sumy”; inputs.ts współdzielony dla update_offer_item. Analiza kodu module-procurement/src/server: narzędzie set_criteria_weights waliduje criteriaWeightsInput (klucz z listy, waga 0–100), a POST /api/m/procurement/cases/:caseId/criteria przekazuje body.weights do setCriterionWeights bez walidacji; GET /search nie egzekwuje limitu 1–50 ani długości zapytania jak procurement_search. | Przez HTTP można zapisać wagę spoza 0–100 lub dowolnego typu (pc_criteria nie ma ograniczenia), którą MCP odrzuca; brak body.weights daje 500 internal zamiast validation_failed, a ujemny limit wyszukiwania znosi ograniczenie odczytu. Test obejmuje jedną regułę jednej operacji i rozbieżności nie wykrywa. | BL-11 | T09 | potwierdzone — test: contracts.test.ts — wspólne schematy i serwis dla update_offer_item; endpoint wag kryteriów nieoceniony |
| **L9.3** | Wejścia są walidowane w runtime, a błędy mają rozpoznawalne typy i przyczyny. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „odrzuca wejscie narzedzia niezgodne ze schematem” (validation_failed), „odrzuca ujemna cene regula domenowa, nie schematem”, „odrzuca nieznany komponent karty”, „odrzuca nieprawidlowe wlasciwosci znanego komponentu”, „bez sesji aplikacji zwraca 401”, „nie nadpisuje nowszych danych przy nieaktualnej wersji” (conflict); platform-contracts/src/errors.ts (kody z mapowaniem na HTTP); agent/mcp.ts zwraca błąd narzędzia z kodem i listą issues. | Nie wszystkie wejścia są walidowane w runtime: POST /api/m/procurement/cases/:caseId/criteria i GET /api/m/procurement/search przyjmują dane bez schematu, POST /api/runs/:id/permission parsuje body bez schematu; nieobsłużony wyjątek staje się kodem internal bez rozpoznawalnej przyczyny. | BL-11 | T09 | potwierdzone — test: contracts.test.ts i 04-probes-api.txt; 13 kodów błędów mapowanych na statusy HTTP |
| **L9.4** | Agent potrafi wyszukać rekord, przejść po wielopoziomowych relacjach i pobrać szczegóły. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „agent przechodzi po relacjach do zrodla ceny” (procurement_find_price_provenance: pozycja → oferta → dostawca → plik CSV z ceną 13100.00 i lokalizatorem wiersza) i „wyszukiwanie znajduje rekordy po fragmencie nazwy” — handlery narzędzi wywołane przez registry.callTool, bez modelu. | Brak próby na prawdziwym modelu, w której agent sam wyszukuje rekord, przechodzi po relacjach i pobiera szczegóły; zdolność agenta potwierdzał tylko historyczny przebieg FEEDBACK #21 w AgenticApp. | BL-03 | T09, T25 | potwierdzone — przebieg: FEEDBACK #21 (hist.) pozycja → oferta → dostawca → załącznik → wiersz pliku |
| **L9.5** | Uprawnienia sprawdza backend; identyfikator właściciela dostarczony przez model lub przeglądarkę nie wystarcza do uzyskania dostępu. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „drugi uzytkownik nie widzi cudzej sprawy — mimo poprawnego identyfikatora”, „identyfikator wlasciciela z ciala zadania nie daje dostepu” (403 dla ciasteczka drugiego użytkownika), „bez sesji aplikacji zwraca 401”; tests/runtime.test.ts „cudze uruchomienie nie moze byc anulowane”; tests/file-analysis.test.ts „cudzy plik jest niedostepny do odczytu i do wersjonowania”; e2e/session-restore.spec.ts „rozmowa innego wlasciciela nie jest pokazywana”. Kod: ownerId z ciasteczka HMAC (http/app.ts) i z ToolCallContext runtime; schematy narzędzi nie mają pola właściciela. | Test nazwany „identyfikator właściciela z ciała żądania” wysyła GET bez ciała, więc nie sprawdza podstawionego ownerId; brak testu wejścia narzędzia MCP z polem ownerId. POST /api/auth/session wydaje sesję jednej z dwóch lokalnych tożsamości według userId z przeglądarki bez poświadczenia (zgodne z lokalnym zakresem i T12, ale nieopisane przy tym kryterium). | BL-11 | T09 | potwierdzone — sonda: 04-probes-api.txt (OWNER-*) — 403 dla drugiego właściciela; listy przestrzeni, plików i artefaktów rozłączne |
| **L9.6** | Konflikt aktualności nie nadpisuje nowszych danych bez rozstrzygnięcia. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „nie nadpisuje nowszych danych przy nieaktualnej wersji” (conflict, zostaje 3000) i „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu”; tests/durability.test.ts „konflikt wersji zostawia zapisany stan nietkniety”; formularz UI (module-procurement/src/ui/cards.tsx) wysyła expectedVersion. | expectedVersion w update_offer_item i expectedSpecVersion w canvas_update_card są opcjonalne; bez nich serwis przyjmuje bieżącą wersję i zapisuje bez konfliktu, więc agent, który pominie wersję, po cichu nadpisze nowszą zmianę użytkownika (brak testu tej ścieżki). | BL-11 | T08, T09 | potwierdzone — sonda: 04-probes-api.txt (CONFLICT-*) — nieaktualna wersja daje 409 bez nadpisania |
| **L9.7** | Powtórzenie tej samej operacji nie dubluje skutków biznesowych. | **niespełnione** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts „powtorzona operacja z tym samym operationId nie dubluje skutku” (jeden przyrost wersji, replayed=true), „dodanie karty z tym samym operationId nie tworzy dwoch kart”, „powtorzona wiadomosc o tym samym id nie tworzy drugiego wpisu” — powtórzenia sekwencyjne. Analiza kodu: procurement_save_comparison przyjmuje operationId w schemacie saveComparisonInput i przekazuje go do ProcurementService.saveComparisonArtifact, który go nie używa (brak idempotency.once). | Powtórzone procurement_save_comparison z tym samym operationId tworzy drugi artefakt; operationId jest opcjonalny we wszystkich narzędziach zapisu, więc powtórzenie bez klucza (np. canvas_add_card) dubluje skutek; brak testu równoległych powtórzeń w szablonie. | BL-11 | T10 | potwierdzone — sonda: 04-probes-api.txt — 8 równoległych żądań z jednym operationId, jeden przyrost wersji; poza regresją |
| **L9.8** | Operacja wieloetapowego zapisu jest atomowa albo ma jawny mechanizm odzyskania spójności. | **niespełnione** | test kontraktu lub logiki (szablon) | Transakcje: artifacts.create i addVersion (tests/durability.test.ts „nieudany zapis artefaktu nie tworzy ani wiersza artefaktu, ani jego wersji” — awaria między dwoma INSERT) oraz ProcurementService.updateOfferItem (updateItemChecked + touchOffer w repo.transaction). Analiza kodu: setCriterionWeights wykonuje kilka upsertów pc_criteria bez transakcji; artifact_publish_file zapisuje plik i wiersz files, a potem osobno artefakt; conversations.create z pierwszą wiadomością oraz canvas.addCard z touchSpace poza transakcją; wpis idempotency_keys zapisywany po transakcji domenowej. | Awaria w trakcie setCriterionWeights zostawia część wag zmienionych, a nieudane utworzenie artefaktu w artifact_publish_file zostawia osierocony plik na liście plików; dla tych operacji ani dla utraty wpisu idempotencji po mutacji nie ma opisanego mechanizmu odzyskania spójności. | BL-11 | T11 | potwierdzone — test: durability.test.ts — przerwany zapis artefaktu i odrzucona zmiana pozycji; pozostałe zapisy wieloetapowe nieocenione |
| **L9.9** | Testy potwierdzają rzeczywistą zmianę danych oraz odrzucenie nieuprawnionej operacji. | **potwierdzone** | test kontraktu lub logiki (szablon) | Zmiana danych sprawdzona odczytem z bazy: tests/contracts.test.ts „powtorzona operacja z tym samym operationId nie dubluje skutku” (quantityMilli 7000, wersja +1), „nie nadpisuje nowszych danych przy nieaktualnej wersji” (3000), „artefakt-snapshot zachowuje tresc mimo pozniejszej zmiany danych” (sumy live zmienione). Odrzucenie nieuprawnionych operacji: contracts.test.ts 403 i 401, tests/runtime.test.ts „cudze uruchomienie nie moze byc anulowane”, tests/file-analysis.test.ts „cudzy plik jest niedostepny do odczytu i do wersjonowania”, tests/background-runs.test.ts „nie mozna zatrzymac cudzego wykonania”. Brak testu mutacji domenowej updateOfferItem przez drugiego właściciela. | — | — | T09 | potwierdzone — test: contracts.test.ts, pnpm test 69/69 w stanie historycznym |
| **L9.10** | Platforma nie importuje modułu biznesowego; kompozycja aplikacji rejestruje moduły, a ich serwisy są testowalne bez modelu i UI. | **potwierdzone** | test kontraktu lub logiki (szablon) | scripts/check-boundaries.mjs w pnpm verify (zależności package.json, importy @module/* i słownik domenowy w packages/platform-*/src); tests/platform-boundary.test.ts „startuje z pustym rejestrem modulow” i „bez modulu serwuje pusty stan zamiast bledu”; moduły rejestruje kompozycja apps/server/src/compose.ts i apps/web/src/compose.tsx; serwisy modułu testowane bez modelu i UI w tests/contracts.test.ts i tests/domain-comparison.test.ts. | — | — | T20 | brak oceny (kryterium spoza 95) |
| **L9.11** | Moduł rejestruje schematy, narzędzia, odczyty live, komponenty, nawigację i migracje przez jawne kontrakty; brak modułu nie powoduje odwołań do jego tabel. | **częściowe** | test kontraktu lub logiki (szablon) | Kontrakty: platform-contracts/src/module.ts ServerModule (migrations, tools, readOperations, cardComponents, uiTargets, routes, defaultComposition) i platform-ui/src/catalog/registry.tsx UiModule (cardRenderers, openuiComponents, artifactRenderers, menu). tests/platform-boundary.test.ts „bez modulu nie ma tabel domenowych zakupowych” i „przyjmuje dowolny modul testowy: migracja, narzedzie, trasa, komponent”; odczyty live i cele UI modułu przykładowego w tests/live-artifacts.test.ts i tests/ui-navigation.test.ts; pnpm check:module-swap: brak tabel pc_* i kart dla zakresu procurement_case po wymianie. | Ekrany modułu nie są rejestrowane kontraktem: trasy tworzy ręcznie apps/web/src/router.tsx, a typowane Link w module-procurement/src/ui/pages.tsx zależą od globalnej rejestracji routera aplikacji, przez co próba wymiany musiała usunąć połówkę UI modułu; moduł kontrolny nie ma połówki UI, odczytów live ani celów UI, więc te kontrakty nie są sprawdzone na drugim module. | BL-06 | T20 | brak oceny (kryterium spoza 95) |
| **L9.12** | Minimalny drugi moduł o innej nazwie działa bez zmian w platformie; test zależności obejmuje kod i konfigurację, nie tylko nazwy pakietów. | **częściowe** | test GUI bez modelu (szablon) | scripts/check-module-swap.mjs (pnpm check:module-swap): na kopii repozytorium zmienia tylko apps/ i usuwa połówkę UI modułu przykładowego; sumy SHA-256 packages/platform-* bez zmian; install z lockfile, check-boundaries, typecheck, build, start serwera, rejestr wyłącznie probe, narzędzia probe_*, trasa /api/m/probe/notes, kompozycja probe.noteList, tabela probe_notes bez pc_*, powłoka i /files, /settings w Chromium bez błędów strony. scripts/check-boundaries.mjs sprawdza deklaracje package.json, importy i słownik w kodzie platformy; tests/platform-boundary.test.ts wywołuje probe_add_note. | Moduł kontrolny ma tylko połówkę serwerową, więc UI drugiego modułu (renderer karty, menu, ekrany) nie działa w próbie, a połówka UI przykładu nie kompiluje się bez tras aplikacji; kontrola zależności nie skanuje konfiguracji poza package.json (CSS, JSON, konfiguracja Vite i TS) i bierze słownik tylko z modułów, które go deklarują (module-devkit-probe nie deklaruje); testy platformy w tests/ zależą od modułu przykładowego. | BL-06 | T20 | brak oceny (kryterium spoza 95) |
| **L9.13** | Schematy narzędzi przechodzą kontrolę zgodności SDK oraz próbę wykrycia i wywołania; własności opcjonalne i domyślne mają zgodną semantykę w runtime. | **częściowe** | rzeczywisty model (szablon) | tests/mcp-schema.test.ts „wszystkie realne narzedzia aplikacji przechodza kontrole” (≥20 narzędzi, nazwy mcp__app__*) oraz odrzucenie z.record i .default; e2e/agent-ui.spec.ts i e2e/files-agent.spec.ts: wykrycie i wywołanie wybranych narzędzi w prawdziwej sesji SDK; tests/platform-boundary.test.ts „domyslna kompozycja modulu testowego jest walidowana tym samym katalogiem” (domyślny limit 20 nadany walidacją katalogu). | Brak w regresji próby wykrycia wszystkich narzędzi w sesji SDK (pnpm diag poza nią); semantyka pól opcjonalnych i domyślnych jest sprawdzana tylko statycznie strażnikiem, który dopuszcza .default() zagnieżdżone pod .optional() bez sprawdzenia, jak SDK ogłasza takie pole, gdy obiekt nadrzędny jest podany. | BL-03 | T09 | brak oceny (kryterium spoza 95) |
| **L9.14** | Jednoczesne ponowienia tej samej operacji powodują dokładnie jeden skutek; ten sam klucz z inną treścią jest odrzucany lub jednoznacznie rozstrzygany. | **niespełnione** | test kontraktu lub logiki (szablon) | Tylko powtórzenia sekwencyjne: tests/contracts.test.ts „powtorzona operacja z tym samym operationId nie dubluje skutku” i „dodanie karty z tym samym operationId nie tworzy dwoch kart”. Analiza kodu packages/platform-server/src/services/idempotency.ts: once() wykonuje get → await fn() → put (ON CONFLICT DO NOTHING) bez rezerwacji klucza przed wykonaniem i bez odcisku treści żądania; klucz ograniczony właścicielem i zakresem operacji. | Ten sam operationId z inną treścią nie jest odrzucany: zwracany jest zapisany wynik pierwszego żądania (updateOfferItem tylko z replayed=true, canvas.addCard bez sygnału), więc wywołujący nie wie, że zmiany nie zastosowano; jednokrotność przy równoczesnych żądaniach zależy od tego, że fn nie oddaje sterowania przed zapisem, nie jest egzekwowana atomowo i nie ma testu równoległego w szablonie. | BL-11 | T10 | brak oceny (kryterium spoza 95) |
| **L9.15** | Wymuszona awaria w połowie wieloetapowej mutacji potwierdza atomowość lub odzyskanie; test kopii bazy nie zastępuje tej próby. | **częściowe** | test kontraktu lub logiki (szablon) | tests/durability.test.ts „nieudany zapis artefaktu nie tworzy ani wiersza artefaktu, ani jego wersji”: nieserializowalna treść rzuca wyjątek po pierwszym INSERT w transakcji artifacts.create; sprawdzony brak wiersza artefaktu i wersji. Test „nieudana zmiana pozycji oferty nie zmienia ani wartosci, ani wersji” odrzuca ilość -5 walidacją przed repo.transaction, czyli przed jakimkolwiek zapisem, mimo komentarza o awarii w połowie operacji. | Wymuszona awaria w połowie istnieje tylko dla operacji platformy (artefakt); dla mutacji domenowej (updateItemChecked + touchOffer) i pozostałych zapisów wieloetapowych (wagi kryteriów, publikacja pliku jako artefaktu) brak próby awarii między krokami. | BL-11 | T11 | brak oceny (kryterium spoza 95) |
| **L9.16** | Narzędzia odczytu i mutacji mają opis skutków i zakresu dostępu; model nie może ominąć reguł przez bezpośredni dostęp do tabel. | **częściowe** | test kontraktu lub logiki (szablon) | Każde narzędzie ma effect read lub write i opis (module-procurement/src/server/tools.ts, platform-server/src/agent/platform-tools.ts), instrukcja serwera MCP zakazuje bezpośredniej zmiany bazy (agent/mcp.ts); tests/runtime.test.ts „konfiguracja sandboxa odcina siec i chroni katalog danych aplikacji” (denyRead i denyWrite katalogu danych, allowWrite tylko workspace, autoAllowBashIfSandboxed=false). Test sprawdza obiekt konfiguracji, nie zachowanie sandboxu. | Opisy narzędzi rzadko podają zakres dostępu i skutki; brak wykonywanej próby, że agent nie odczyta ani nie zmieni app.db przez Bash lub auto-zatwierdzone Read, Write, Edit (runtime.ts AUTO_APPROVED_FILE_TOOLS) — dowód izolacji był tylko historyczny (FEEDBACK #24/#25 w AgenticApp). | BL-03 | T09 | brak oceny (kryterium spoza 95) |

### Warstwa 10 — Trwałość, cache i artefakty

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L10.1** | Własność każdego rodzaju danych odpowiada tabeli modelu własności; nie istnieją niezależnie mutowane kopie domeny. | **potwierdzone** | analiza kodu (szablon) | Analiza kodu szablonu wobec tabeli „Model własności danych” w docs/ARCHITECTURE.md: dane biznesowe i reguły tylko w serwisach backendu (packages/module-procurement/src/server/services.ts), frontend czyta je przez TanStack Query (packages/platform-ui/src/api/queries.ts) i nie używa localStorage, sessionStorage ani IndexedDB; rozmowy i wiadomości zapisuje backend (restStorage na /api/threads, projekcja w packages/platform-server/src/agent/runtime.ts), kompozycja w canvas_cards, artefakty w artifacts i artifact_versions, stan zadań w agent_runs; szkice formularzy tylko w stanie klienta (state/appState.ts) i przekazywane agentowi jako niezapisane. Snapshot artefaktu jest jawną kopią. scripts/check-boundaries.mjs sprawdza kierunek zależności, nie własność danych. Nie obejmuje: testu wykrywającego drugą, niezależnie mutowaną kopię. | — | — | T09, T13 | potwierdzone — analiza kodu: tabela własności w FEEDBACK §4 i check-boundaries.mjs, który sprawdza granicę modułów, a nie własność danych |
| **L10.2** | Migracje tworzą i aktualizują bazę bez utraty obsługiwanych danych; sprawdzona jest kopia i odtworzenie trwałego stanu lokalnego. | **częściowe** | test kontraktu lub logiki (szablon) | tests/migration.test.ts „zachowuje rozmowy, wiadomosci uzytkownika, kompozycje i dane domenowe”, „powtorne uruchomienie nie dubluje danych”, „uruchomienie bez zachowanych zdarzen zostawia stara wiadomosc nietknieta”, „migracja na pustym katalogu tworzy kompletny schemat”: baza z samą platform-0001 migrowana przez createPlatform; zachowane rozmowy, wiadomości użytkownika, karty, claude_session_id, odtworzona tura, brak zmian po powtórnym starcie (odciski wszystkich tabel). tests/runtime.test.ts „skopiowany katalog danych otwiera sie z kompletnym stanem”: checkpoint WAL w procesie, cpSync katalogu, ponowne otwarcie z artefaktem, plikiem i kartą. Nie obejmuje: platform-0003-file-versions na niepustej tabeli files (fikstura nie ma plików), skryptów scripts/backup-state.mjs i scripts/migration-rehearsal.mjs (niewykonywane w testach ani w verify), odtworzenia według docs/odzyskiwanie-stanu.md. | Migracja platform-0003 nie jest sprawdzona na danych z plikami, a próba migracji na kopii sprzed 0003 z plikami zgłosiłaby fałszywą zmianę tabeli files (brak w EXPECTED_TO_CHANGE, analiza kodu). W szablonie nie wykonano kopii, próby migracji ani odtworzenia na żadnej kopii (brak danych użytkownika, próby syntetycznej nie przeprowadzono), więc zachowanie istniejących danych przy migracji do tej wersji nie jest wykazane. | BL-07 | T18 | potwierdzone — przebieg: kopia i próba migracji na danych AgenticApp sprzed platform-0003, potem migracja za zgodą; dowody poza repozytorium |
| **L10.3** | Rozmowa aplikacji jest jednoznacznie powiązana z sesją Claude i jej właścicielem. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” (prawdziwy model): po przebiegu /api/conversations/:id ma claudeSessionId, uruchomienie ma claudeSessionId i status succeeded. e2e/session-restore.spec.ts „dalsze polecenie po przeladowaniu kontynuuje te sama sesje” (skryptowany serwer, po restarcie backendu): kolejne polecenie trafia do tej samej rozmowy z tym samym claudeSessionId; „rozmowa innego wlasciciela nie jest pokazywana”: druga tożsamość dostaje stan zastępczy bez wiadomości. tests/run-lifecycle.test.ts „sesja Claude zostaje powiazana nawet gdy uruchomienie nie uzywa narzedzi” (symulacja). Nie obejmuje: rzeczywistego wznowienia sesji modelu (stand-in ma stały identyfikator sesji) ani przypadku utraconego transkryptu SDK. | — | — | T03 | potwierdzone — przebieg: tabela conversations instancji audytowej (claude_session_id i owner_id) |
| **L10.4** | Restart przywraca historię, kompozycję, powiązania sesji i artefakty. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/tool-activity.spec.ts „aktywnosc przezywa przeladowanie, przelaczenie rozmowy i restart backendu”: po restarcie serwera i przeładowaniu oś narzędzi widoczna, identyfikatory wiadomości identyczne. e2e/session-restore.spec.ts „dalsze polecenie po przeladowaniu kontynuuje te sama sesje” i „przestrzen pracy wraca razem z rozmowa” (po scripted.restart). tests/live-artifacts.test.ts „snapshot zachowuje liczby, live liczy od nowa po ponownym otwarciu bazy”; tests/run-lifecycle.test.ts „historia przezywa restart backendu”; tests/runtime.test.ts „skopiowany katalog danych otwiera sie z kompletnym stanem” (karta canvasu, artefakt i plik po ponownym otwarciu). Restart w Vitest to nowa instancja platformy na tym samym katalogu. Nie obejmuje: ciągłości pamięci modelu po restarcie (tylko powiązanie sesji). | — | — | T03, T06, T13 | potwierdzone — przebieg: restart instancji audytowej AgenticApp |
| **L10.5** | Mutacja przez UI lub MCP odświeża właściwe dane na froncie bez pełnego przeładowania. | **częściowe** | rzeczywisty model (szablon) | e2e/measurements.spec.ts „zmiana pozycji oferty jest widoczna w zestawieniu bez przeladowania”: kliknięcie Zapisz w formularzu karty zmienia widoczne sumy bez przeładowania (karta dodana przez API jako przygotowanie). e2e/agent-ui.spec.ts (prawdziwy model): narzędzie MCP dodaje kartę wykresu, która pojawia się na canvasie bez przeładowania. tests/agui-stream.test.ts „zmiana danych uniewaznia odczyty modulu, canvas i otwarte artefakty” i „zmiana kompozycji uniewaznia przestrzen, ktorej dotyczy”. Nie obejmuje: mutacji danych domenowych przez MCP z widocznym odświeżeniem w GUI ani odświeżenia otwartego artefaktu po mutacji z UI. | Mutacja z formularza modułu (packages/module-procurement/src/ui/cards.tsx) unieważnia tylko klucze module i spaces, a refetchOnWindowFocus jest wyłączony (apps/web/src/main.tsx): otwarty artefakt live i lista plików po edycji w UI zostają nieaktualne do innego odświeżenia. Unieważnianie po mutacjach HTTP zależy od każdego modułu, platforma nie ma wspólnego mechanizmu ani testu. | BL-11 | T09 | potwierdzone — przebieg: FEEDBACK #21 (hist.) i 08-probes-browser.txt; dowody poza repozytorium |
| **L10.6** | Komponenty współdzielą pobrania dla tego samego zasobu; klucze cache uwzględniają kontekst dostępu i filtry. | **częściowe** | test kontraktu lub logiki (szablon) | tests/access-context.test.ts „ten sam zasob u dwoch tozsamosci ma rozne klucze” i „klucz rozroznia zasob i filtry w obrebie jednej tozsamosci”: klucze qk.* zawierają zakres dostępu, zasób i filtr. Analiza kodu: karty i ekrany modułu używają useModuleData z tym samym kluczem dla tego samego zasobu, więc TanStack Query współdzieli pobranie. Nie obejmuje: pomiaru liczby żądań przy kilku komponentach tego samego zasobu. | Część komponentów pobiera te same zasoby poza cache: przeglądarka artefaktów OpenUI przez createChatStorage (apiGet bez klucza), wątki przez restStorage biblioteki, upload w ComposerAttachments przez fetch; podgląd artefaktu w wiadomości i lista biblioteki nie dzielą pobrań i mogą pokazywać różne stany. Brak testu deduplikacji. | BL-11 | T12, T26 | potwierdzone — analiza kodu: klucze qk.* w queries.ts; bez pomiaru liczby żądań |
| **L10.7** | Zmiana kontekstu właściciela nie ujawnia danych z poprzedniego cache. | **częściowe** | test kontraktu lub logiki (szablon) | tests/access-context.test.ts „pamiec podreczna jest pusta po przelaczeniu”, „zadanie w locie jest przerywane w momencie przelaczenia”, „odpowiedz, ktora mimo wszystko dobiegnie, jest odrzucana po epoce”: prawdziwy QueryClient i api() z wstrzymanym fetch; cache pusty, odpowiedź poprzedniej tożsamości odrzucona. e2e/access-context.spec.ts „po przelaczeniu zaden wiersz poprzedniej tozsamosci nie jest widoczny”: po kliknięciu przełącznika test wykonuje page.goto('/cases'), czyli pełne przeładowanie strony, więc sprawdza sesję i odmowę dostępu backendu, a nie czyszczenie cache w tej samej instancji klienta. | Stan klienta poza TanStack Query nie jest czyszczony przy zmianie właściciela: lista wątków i wiadomości AgentInterface (restStorage) oraz appState (aktywna rozmowa, zadania, załączniki, szkice) zostają w tej samej karcie; brak testu GUI bez przeładowania, który wykryłby taki wyciek. | BL-11 | T12 | potwierdzone — test: access-context.test.ts (8) i e2e; e2e po przełączeniu przeładowuje stronę przez page.goto |
| **L10.8** | Artefakt ma stabilny identyfikator, wersję, właściciela i trwałą treść; tytuł nie jest jego kluczem. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „artefakt cudzego wlasciciela nie jest czytelny” (403) i „snapshot zachowuje liczby, live liczy od nowa po ponownym otwarciu bazy” (treść odczytana po id po ponownym otwarciu); tests/durability.test.ts „nieudany zapis artefaktu nie tworzy ani wiersza artefaktu, ani jego wersji”; tests/runtime.test.ts „skopiowany katalog danych otwiera sie z kompletnym stanem” (meta i treść po id). Analiza schematu (packages/platform-server/src/db/migrations.ts, services/artifacts.ts): artifacts.id jako klucz, owner_id, current_version, artifact_versions z kluczem (artifact_id, version); tytuł to zwykła kolumna zmieniana przez rename bez zmiany id. Nie obejmuje: testu zmiany tytułu. | — | — | T12, T13 | potwierdzone — test: contracts.test.ts; klucz (artifact_id, version), tytuł zwykłą kolumną |
| **L10.9** | Podgląd i pełny widok wskazują tę samą wersję; pliki można pobrać po restarcie. | **częściowe** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „podglad i pelny widok czytaja te sama wersje, wiec nie moga sie roznic”: dwa odczyty GET /api/artifacts/:id (bez i z ?version) dają te same dane i definitionVersion. tests/contracts.test.ts „plik przetrwa restart procesu backendu”: treść pliku po ponownym otwarciu platformy przez FileService.read. Analiza kodu: podgląd w wiadomości i karta canvasu używają ArtifactContent z kluczem qk.artifact. Nie obejmuje: GUI ani pobrania przez HTTP po restarcie. | Brak próby w przeglądarce, że podgląd artefaktu w wiadomości i pełny widok w przeglądarce artefaktów pokazują tę samą wersję (przeglądarka biblioteki czyta przez osobne apiGet), oraz brak pobrania przez /api/files/:id/content po rzeczywistym restarcie serwera. | BL-11 | T13, T24 | potwierdzone — przebieg: restart instancji audytowej z sumą sha przed i po; bez testu GUI podglądu i pełnego widoku |
| **L10.10** | Raport historyczny zachowuje treść, a artefakt oznaczony jako żywy pobiera aktualne dane. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „snapshot zamraza liczby, live pokazuje biezace — po zmianie zrodla” (zmiana danych przez serwis domenowy, asercja że wynik faktycznie się zmienił, snapshot zwraca starą sumę, live nową ze stanem fresh) i „artefakt live nigdy nie przechowuje danych, tylko pytanie”; tests/contracts.test.ts „artefakt-snapshot zachowuje tresc mimo pozniejszej zmiany danych” (snapshot z narzędzia MCP procurement_save_comparison). Nie obejmuje: widoku w przeglądarce. | — | — | T13 | potwierdzone — test: live-artifacts.test.ts (9); snapshot i live po zmianie źródła i po restarcie |
| **L10.11** | Przełączenie właściciela w tej samej instancji klienta usuwa poprzedni cache i odcina opóźnione żądania oraz strumienie; nowe wyniki nie zawierają poprzednich danych. | **częściowe** | test kontraktu lub logiki (szablon) | tests/access-context.test.ts (8 testów): czyszczenie cache, przerwanie żądania w locie wspólnym sygnałem, odrzucenie spóźnionej odpowiedzi po epoce, klucze z zakresem dostępu — na prawdziwym QueryClient i api(). Analiza kodu: ponowne podłączenie do strumienia zadania (packages/platform-ui/src/chat/runStreams.ts attachToRun) jest związane z accessSignal. e2e/access-context.spec.ts przeładowuje stronę po przełączeniu (page.goto), więc nie dowodzi działania w tej samej instancji klienta. Nie obejmuje: strumieni i stanu klienta poza TanStack Query. | Strumień wysyłania POST /api/agui/run (chat/chatWiring.ts) używa tylko sygnału biblioteki, bez accessSignal i epoki, więc opóźnione zdarzenia poprzedniej tożsamości nadal trafiają do applyRunEvent i unieważnień; żądania wątków restStorage i upload plików omijają api(); appState i lista wątków AgentInterface nie są resetowane. Brak testu z opóźnionym strumieniem przy zmianie właściciela w jednej karcie. | BL-11 | T12 | brak oceny (kryterium spoza 95) |
| **L10.12** | Nieaktualne dane nie są przedstawiane jako świeży wynik po nieudanym odświeżeniu; stan błędu jest widoczny. | **częściowe** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „brak dostepu nie jest przedstawiany jako swieze dane”: odczyt live przy niedostępnym źródle zwraca content null i state forbidden lub failed z komunikatem. Analiza kodu: components/LiveArtifact.tsx (ArtifactContent sprawdza error przed data i pokazuje błąd odświeżenia zamiast wyniku), CanvasHost i karty modułu używają stanów błędu. Nie obejmuje: żadnego testu GUI z nieudanym odświeżeniem (e2e nie symulują błędu sieci ani odpowiedzi 500). | Brak próby, w której dane są już na ekranie, a kolejne odświeżenie się nie udaje; nie sprawdzono, że widoki nie pokazują wtedy starych danych jako aktualnych, a ekran Dane modułu przykładowego (DataPage w pages.tsx) w ogóle nie obsługuje błędu zapytania. | BL-11 | T13, T26 | brak oceny (kryterium spoza 95) |
| **L10.13** | Deskryptor live wskazuje typowany odczyt modułu, a backend przy każdym odczycie sprawdza uprawnienia; nie zawiera dowolnego SQL ani kodu. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „deskryptor wskazujacy nieznana operacje jest odrzucany przy zapisie”, „deskryptor z niepoprawnym wejsciem jest odrzucany przy zapisie”, „tresc, ktora nie jest deskryptorem, jest odrzucana”, „artefakt cudzego wlasciciela nie jest czytelny”, „brak dostepu nie jest przedstawiany jako swieze dane”. Analiza kodu services/artifacts.ts resolveLive: przy każdym odczycie sprawdzenie właściciela artefaktu, ponowna walidacja deskryptora {operation, input} schematem operacji zarejestrowanej przez moduł i wywołanie jej z ownerId z sesji; deskryptor nie niesie kodu ani SQL. Nie obejmuje: testu własnego artefaktu wskazującego sprawę innego właściciela (użyto nieistniejącej sprawy) oraz ścieżki PATCH /api/artifacts/:id, która dopisuje wersję bez walidacji deskryptora (przy odczycie taki deskryptor daje stan failed). | — | — | T12, T13 | brak oceny (kryterium spoza 95) |
| **L10.14** | Po zmianie źródła snapshot zachowuje treść, a live pokazuje aktualny wynik przy otwarciu i w otwartym widoku; obie ścieżki są sprawdzone po restarcie. | **częściowe** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „snapshot zamraza liczby, live pokazuje biezace — po zmianie zrodla” (otwarcie przez HTTP) i „snapshot zachowuje liczby, live liczy od nowa po ponownym otwarciu bazy” (po ponownym otwarciu, przez serwis). tests/agui-stream.test.ts „zmiana danych uniewaznia odczyty modulu, canvas i otwarte artefakty” (zdarzenie data_changed z przebiegu agenta unieważnia klucz artifact). Nie obejmuje: otwartego widoku w przeglądarce ani ścieżki HTTP po restarcie. | Otwarty widok live nie odświeża się po zmianie źródła z UI (formularz modułu nie unieważnia klucza artifact, refetchOnWindowFocus wyłączony); brak testu GUI otwartego widoku live i obu ścieżek przez HTTP po rzeczywistym restarcie. | BL-11 | T13 | brak oceny (kryterium spoza 95) |
| **L10.15** | Wersja definicji i świeżość wyniku live są rozróżnione; podgląd i pełny widok nie pokazują sprzecznych danych bez oznaczenia. | **częściowe** | test kontraktu lub logiki (szablon) | tests/live-artifacts.test.ts „podglad i pelny widok czytaja te sama wersje, wiec nie moga sie roznic” i „artefakt live nigdy nie przechowuje danych, tylko pytanie”: definitionVersion i operation zgodne, state fresh i resolvedAt rozdzielone od wersji definicji (kontrakt liveResolutionSchema w platform-contracts/src/artifacts.ts). Analiza kodu: LiveBadge pokazuje stan i czas odczytu, snapshot ma etykietę snapshot. Nie obejmuje: GUI ani wersji źródła danych. | Wynik live nie niesie wersji źródła (np. wersji rekordów), więc świeży odczyt nie jest odróżniony od odczytu starszego stanu źródła; nie sprawdzono w przeglądarce, że podgląd w wiadomości i przeglądarka artefaktów (czytająca poza cache) nie pokazują sprzecznych danych bez oznaczenia. | BL-11 | T13 | brak oceny (kryterium spoza 95) |
| **L10.16** | Kopia zachowuje zatwierdzony stan SQLite wraz z WAL oraz powiązane pliki; jest ponownie odczytana i sprawdzona pod względem integralności. | **częściowe** | test kontraktu lub logiki (szablon) | tests/runtime.test.ts „skopiowany katalog danych otwiera sie z kompletnym stanem”: wal_checkpoint(TRUNCATE) na otwartym połączeniu, cpSync całego katalogu danych, ponowne otwarcie kopii i odczyt artefaktu, pliku i karty. Analiza kodu scripts/backup-state.mjs: kopiuje app.db, app.db-wal i app.db-shm jako bajty bez otwierania źródła, zwija WAL w kopii, kopiuje files i workspaces z SHA-256, zapisuje manifest i weryfikuje kopię (sumy, liczby i odciski tabel, integrity_check), odmawia przy bazie trzymanej przez proces. Nie obejmuje: wykonania skryptu w szablonie. | scripts/backup-state.mjs nie jest wykonywany przez żaden test ani pnpm verify i w szablonie nie wykonano żadnej kopii (brak danych użytkownika, próby syntetycznej nie przeprowadzono); test Vitest używa innego mechanizmu bez integrity_check i manifestu, a wykrywanie uszkodzonej kopii lub niespójnego manifestu nie jest sprawdzone. | BL-07 | T18 | brak oceny (kryterium spoza 95) |
| **L10.17** | Próba migracji na kopii porównuje tożsamość i treść danych, jawnie dopuszczone zmiany oraz drugie uruchomienie bez dodatkowego skutku. | **częściowe** | test kontraktu lub logiki (szablon) | tests/migration.test.ts „zachowuje rozmowy, wiadomosci uzytkownika, kompozycje i dane domenowe” (tożsamość rozmowy, treść wiadomości, odciski canvas_cards, canvas_spaces i run_events) i „powtorne uruchomienie nie dubluje danych” (liczby i odciski wszystkich tabel po drugim i trzecim starcie) — na bazie zbudowanej w teście, nie na kopii. Analiza kodu scripts/migration-rehearsal.mjs: kopia do katalogu tymczasowego, spis z odciskami, prawdziwy composeApp, porównanie z listą EXPECTED_TO_CHANGE, sprawdzenie tożsamości rozmów, wiadomości, kart, plików i artefaktów, drugi start z wymogiem identycznego spisu. | Próba na kopii nie jest wykonywana w szablonie, a EXPECTED_TO_CHANGE nie zawiera tabeli files zmienianej przez platform-0003-file-versions przy porównaniu wszystkich kolumn: dla kopii sprzed 0003 z plikami próba zgłosi fałszywą zmianę (analiza kodu, bez próby), więc nie może dziś potwierdzić gotowości migracji takich danych. Test Vitest nie ma jawnej listy dopuszczonych zmian i nie zawiera plików. | BL-07 | T18 | brak oceny (kryterium spoza 95) |
| **L10.18** | Istnieje sprawdzona procedura odtworzenia, określająca zachowanie stanu sprzed próby, zgodność wersji kodu oraz los sesji aplikacji i transkryptów SDK. | **częściowe** | analiza kodu (szablon) | docs/odzyskiwanie-stanu.md §4 i §5: przed odtworzeniem obecny data przenoszony do data.przed-odtworzeniem-<data>, kopiowanie app.db, files i workspaces, polecenie sprawdzające liczby wierszy i integrity_check przed startem, session.secret poza kopią (sesje przeglądarki wygasają na nowej maszynie), transkrypty SDK poza katalogiem danych, ani kopiowane, ani usuwane. Nie obejmuje: wykonania procedury. | Procedura nie była wykonana ani w szablonie, ani w AgenticApp (raport §6: krok kopiowania do data nie był sprawdzony), więc nie jest „sprawdzona”; nie określa zgodności wersji kodu z kopią (kopia nowsza niż build, brak cofania migracji) ani skutku braku transkryptu SDK dla rozmów z zapisanym claude_session_id po odtworzeniu. | BL-07 | T18 | brak oceny (kryterium spoza 95) |
| **L10.19** | Kopie i testy migracji nie zmieniają aktywnej bazy użytkownika; gotowość migracji jest odróżniona od jej zastosowania na docelowych danych. | **częściowe** | analiza kodu (szablon) | Analiza kodu: scripts/backup-state.mjs czyta źródło wyłącznie jako bajty i odmawia pracy przy otwartej bazie; scripts/migration-rehearsal.mjs pracuje na kopii w katalogu tymczasowym i odmawia, gdy --backup wskazuje katalog data repozytorium; tests/migration.test.ts i tests/durability.test.ts używają katalogów tymczasowych przez APP_DATA_DIR. docs/odzyskiwanie-stanu.md §3 i README („Istniejące dane — najpierw kopia”) oddzielają próbę na kopii od uruchomienia na danych. Nie obejmuje: wykonanego sprawdzenia sum źródła przed i po kopii lub próbie. | Brak wykonanej w szablonie kontroli, że kopia i próba nie zmieniają aktywnej bazy; ochrona próby porównuje ścieżkę tylko z data w repozytorium, nie z APP_DATA_DIR; pnpm start stosuje zaległe migracje automatycznie bez wymogu wcześniejszej próby, więc rozdział gotowości i zastosowania opiera się tylko na dokumentacji. | BL-07 | T18 | brak oceny (kryterium spoza 95) |

### Warstwa 11 — Pliki, sandbox i cykl życia zadań

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L11.1** | Użytkownik wgrywa plik, agent odczytuje go i przetwarza kodem, a użytkownik pobiera poprawny wynik. | **potwierdzone** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety” (prawdziwy model): XLSX dołączony przez pole załącznika w GUI, agent pisze i uruchamia skrypt (asercja, że bramka zgody objęła Bash), publikuje nową wersję; pobrany przez /api/files/:id/content wynik otwiera się, ma arkusz Podsumowanie z sumą 5300 policzoną z komórek i zachowane arkusze; „wynik jest dostepny po powrocie do rozmowy”: plik widoczny na ekranie /files i pobieralny. tests/file-analysis.test.ts „kod uruchomiony w katalogu workspace importuje exceljs”. Nie obejmuje: pobrania kliknięciem w GUI (pobranie przez fetch w stronie) ani plików CSV i tekstowych. | — | — | T14, T24 | potwierdzone — przebieg: files-agent.spec (prawdziwy model) i file-analysis.test (10); rozszerzenie z 2026-09-16 |
| **L11.2** | Limity wielkości, nazwy plików i zakres katalogów są egzekwowane przez backend. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/runtime.test.ts „czysci nazwe pliku ze sciezek i znakow sterujacych”, „odrzuca zbyt duzy plik i niedozwolony typ”, „zapisuje plik pod bezpieczna nazwa i liczy sha256”, „odrzuca sciezke wychodzaca poza workspace” (resolveInWorkspace); tests/file-analysis.test.ts „plik ponad limit jest odrzucony z podaniem limitu” i „xlsx jest przyjmowany, a macro-enabled i legacy nie”; e2e/app.spec.ts „odrzucony typ pliku daje czytelny blad”. Limity egzekwuje FileService.store używany przez POST /api/files i narzędzia publikacji. Nie obejmuje: limitu ciała żądania przed wczytaniem całego uploadu do pamięci ani zakresu katalogów dla wbudowanych narzędzi plikowych SDK (L11.4). | — | — | T14 | potwierdzone — test: runtime.test.ts; sanitizeFilename, limit 8 MB, allowlista typów, resolveInWorkspace |
| **L11.3** | Izolacja jest aktywna na docelowym systemie; kontrolowane próby niedozwolonego odczytu, zapisu i dostępu do sieci są odrzucane. | **niesprawdzone** | — (—) | W szablonie jedynie tests/runtime.test.ts „konfiguracja sandboxa odcina siec i chroni katalog danych aplikacji” sprawdza wartości zwracane przez sandboxSettings (enabled, failIfUnavailable, allowedDomains puste, allowWrite tylko workspace, denyRead i denyWrite katalogu danych) — to test konfiguracji, nie próba izolacji. Żaden test ani kontrola w regresji nie wykonuje niedozwolonego odczytu, zapisu ani połączenia sieciowego w sandboxie. Próby istnieją tylko historycznie (AgenticApp, FEEDBACK #24/#25). | Brak powtarzalnej próby w szablonie, że na docelowym systemie sandbox jest aktywny i odrzuca odczyt katalogu danych, zapis poza workspace i ruch sieciowy; utrata izolacji po aktualizacji SDK lub zmianie systemu nie zostałaby wykryta. | BL-03 | T14 | potwierdzone — przebieg (hist.) FEEDBACK #24/#25: sieć odrzucona, data/app.db niewidoczna; nie powtórzony w audycie ani w szablonie |
| **L11.4** | Sandbox poleceń i uprawnienia narzędzi plikowych obejmują wszystkie udostępnione sposoby dostępu, a nie tylko powłokę. | **niesprawdzone** | — (—) | Analiza kodu packages/platform-server/src/agent/runtime.ts: Read, Write, Edit, Glob i Grep są w allowedTools (auto-zatwierdzane, canUseTool bez reguł ścieżek), Bash poza listą trafia do canUseTool, sandbox.filesystem ogranicza zapis do workspace i blokuje katalog danych, disallowedTools nie jest ustawione. Żaden test w szablonie nie wywołuje narzędzi plikowych ani sieciowych SDK na zasobach poza workspace i nie sprawdza, czy reguły sandboxu obejmują coś poza powłoką. | Nie wykazano, że narzędzia plikowe (auto-zatwierdzone bez reguł ścieżek) i narzędzia sieciowe SDK (WebFetch i WebSearch nie są zabronione, trafiają do pytania o zgodę) podlegają tym samym ograniczeniom co powłoka; obejście sandboxu inną klasą narzędzia pozostaje niesprawdzone. | BL-03 | T14 | potwierdzone — przebieg (hist.) FEEDBACK #17/#23: narzędzia plikowe w workspace, powłoka przez canUseTool; nie powtórzony w szablonie |
| **L11.5** | Narzędzia nie mają niejawnego dostępu do bazy domenowej pozwalającego ominąć MCP i serwisy backendu. | **częściowe** | test kontraktu lub logiki (szablon) | tests/file-analysis.test.ts „workspace nie wystawia zaleznosci serwera sandboxowanemu kodowi”: skrypt uruchomiony w workspace nie importuje better-sqlite3, @anthropic-ai/claude-agent-sdk ani hono. tests/runtime.test.ts „konfiguracja sandboxa odcina siec i chroni katalog danych aplikacji” (denyRead i denyWrite katalogu danych w konfiguracji). Analiza kodu: narzędzia platformy (agent/platform-tools.ts) nie udostępniają SQL, dane idą przez serwisy z ownerId. Test importu działa poza sandboxem (execFileSync), więc nie dowodzi blokady odczytu pliku bazy. | Brak wykonanej w szablonie próby, że kod w sandboxie i narzędzia plikowe nie mogą odczytać ani zapisać data/app.db (np. bezpośredni odczyt pliku, sqlite3 CLI, import sterownika po ścieżce bezwzględnej); ochrona opiera się na konfiguracji denyRead potwierdzonej tylko historycznie. | BL-03 | T14 | potwierdzone — przebieg (hist.) FEEDBACK #25: katalog danych niewidoczny dla procesu sandboxu; nie powtórzony w szablonie |
| **L11.6** | Zadanie ma trwały status i powiązanie z rozmową; zamknięcie panelu nie usuwa informacji o pracy. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „przelaczenie rozmowy i przeladowanie nie anuluja zadania” (skryptowany serwer): po przejściu do nowej rozmowy i przeładowaniu zadanie w backendzie ma status queued lub running, lista zadań w tle jest widoczna bez przejęcia widoku. tests/runtime.test.ts „zadanie ma trwaly status i powiazanie z rozmowa”; tests/background-runs.test.ts „przerwanie odczytu strumienia nie zatrzymuje pracy” i „lista aktywnych zadan obejmuje wszystkie rozmowy wlasciciela i tylko jego” (symulacja). Nie obejmuje: zwinięcia panelu czatu bez przeładowania (sprawdzone są przeładowanie i zmiana rozmowy). | — | — | T22, T16 | potwierdzone — przebieg: background-tasks.spec (5) i background-runs.test (10) po naprawie mapowania abort na /cancel |
| **L11.7** | Stop dociera do wykonania i jego procesów potomnych; pomiar czasu anulowania znajduje się w odbiorze. | **częściowe** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „jawne Stop konczy wskazane wykonanie” i „zadanie z innej rozmowy da sie zatrzymac z listy zadan”: status cancelled, brak aktywnych zadań. e2e/measurements.spec.ts „zatrzymanie z interfejsu konczy wykonanie i nie zostawia zapisow”: czas od kliknięcia do stanu końcowego w backendzie zapisany z warunkami, liczba zdarzeń nie rośnie po zakończeniu. tests/background-runs.test.ts „konczy wskazane wykonanie i nie rusza pozostalych”. Wszystko na stand-inie modelu honorującym sygnał przerwania; historyczny pomiar procesów potomnych (raport §5, raw-probe-stop.log) nie jest częścią szablonu. | Brak w szablonie próby Stop na prawdziwym procesie Claude SDK z liczeniem procesów potomnych przed, w trakcie i po (w tym poleceń powłoki w sandboxie); pomiar anulowania kończy się na statusie w backendzie, więc osierocone procesy nie zostałyby wykryte. | BL-09 | T16 | potwierdzone — przebieg: raw-probe-stop.log (potomkowie 0/1/0, potwierdzenie 3 ms); dowód poza repozytorium, nie powtórzony w szablonie |
| **L11.8** | Restart rozróżnia zadanie zakończone od przerwanego; wznowienie nie udaje kontynuacji utraconego procesu. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/runtime.test.ts „restart oznacza przerwane zadania zamiast udawac kontynuacje”: reconcileOnBoot oznacza zadanie queued lub running jako failed z komunikatem „Przerwane restartem”; „kazde uruchomienie ma dokladnie jeden rozstrzygajacy status koncowy” (stan końcowy nie jest nadpisywany). Analiza kodu: reconcileOnBoot wywoływany w createPlatform (packages/platform-server/src/index.ts) i obejmuje tylko queued i running; GET /api/runs/:id/stream dla przebiegu bez zdarzenia końcowego dosyła RUN_ERROR ze statusu zamiast czekać. Nie obejmuje: zabicia procesu serwera w trakcie zadania w teście przeglądarkowym. | — | — | T16 | potwierdzone — przebieg: run 6c9c63 failed/server_sigterm w instancji audytowej |
| **L11.9** | Wymagane pytania i zgody pojawiają się w aplikacji; odmowa nie wykonuje operacji, zgoda nie wykonuje jej podwójnie. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety” (prawdziwy model): prośba o zgodę na Bash pojawia się w GUI (permission-prompt), kliknięcie Zgoda wznawia wykonanie, asercja że zgoda dotyczyła Bash. tests/agui-stream.test.ts „prosba o zgode wstrzymuje wykonanie w odroznialnym stanie” i „prosba o zgode trafia do rozmowy zadania, nie do ogladanej”. Nie obejmuje: odmowy ani liczby wykonań po zgodzie. | W szablonie żaden test nie klika Odmowa i nie sprawdza braku skutku ani nie sprawdza, że po zgodzie operacja wykonuje się dokładnie raz (tylko historycznie FEEDBACK #23); skryptowany serwer nie wywołuje canUseTool, więc bez modelu ścieżki zgody nie da się dziś sprawdzić. | BL-03 | T15 | potwierdzone — przebieg: files-agent.spec (zgoda na modelu) i FEEDBACK #23 (hist.) dla odmowy i pojedynczego wykonania |
| **L11.10** | Opublikowane wyniki pozostają trwałe po sprzątnięciu plików tymczasowych. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „wynik jest dostepny po powrocie do rozmowy” (po zakończonym przebiegu, gdy runtime usuwa workspace w finally w agent/runtime.ts): wersja pliku na liście i GET /api/files/:id/content zwraca 200. tests/runtime.test.ts „tworzy workspace z katalogami input/output i sprzata po sobie” (dispose usuwa katalog). Analiza kodu: files_publish_version i artifact_publish_file kopiują bajty do magazynu plików przed sprzątaniem. | Żaden test nie sprawdza jawnie, że workspace został usunięty przed pobraniem, ani nie porównuje sumy kontrolnej wyniku po sprzątaniu (test sprawdza tylko status 200); ścieżka artifact_publish_file nie jest wykonywana w żadnym teście. | BL-09 | T14 | potwierdzone — przebieg (hist.) FEEDBACK #20 i restart instancji audytowej: plik artefaktu pobieralny po dispose workspace |
| **L11.11** | Próby izolacji obejmują zarówno narzędzia powłoki, jak i plikowe; obejście jednej ścieżki przez drugą nie zapewnia dostępu do bazy lub sekretów. | **niesprawdzone** | — (—) | W szablonie brak prób izolacji dla jakiejkolwiek klasy narzędzi; jedyny związany test (tests/runtime.test.ts „konfiguracja sandboxa odcina siec i chroni katalog danych aplikacji”) sprawdza wartości konfiguracji. Analiza kodu: denyRead obejmuje tylko katalog danych aplikacji, narzędzia plikowe SDK są auto-zatwierdzone bez reguł ścieżek (agent/runtime.ts). | Nie sprawdzono, czy narzędzie plikowe może odczytać lub zapisać to, czego powłoka nie może (i odwrotnie), ani czy sekrety spoza katalogu danych, w tym plik poświadczeń subskrypcji w katalogu konfiguracji Claude (niewymieniony w denyRead), są niedostępne z obu ścieżek; możliwe obejście pozostaje niewykryte. | BL-03 | T14 | brak oceny (kryterium spoza 95) |
| **L11.12** | Polityka automatycznych zgód i pytań odpowiada faktycznej kolejności mechanizmów SDK; lista allowedTools nie jest traktowana jako gwarancja wywołania bramki zgody. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts (prawdziwy model): uruchomienie skryptu przez Bash przeszło przez bramkę zgody w GUI (asercja na zatwierdzenie Bash), więc ciche auto-zatwierdzenie powłoki oblałoby test. tests/runtime.test.ts „konfiguracja sandboxa odcina siec i chroni katalog danych aplikacji”: autoAllowBashIfSandboxed false. Analiza kodu agent/runtime.ts (AUTO_APPROVED_FILE_TOOLS, CONSENT_REQUIRED_TOOLS, opis CLAUDE_SDK_CAN_USE_TOOL_SHADOWED) i docs/NEW-APPLICATION.md §7 (allowedTools omija canUseTool). | Brak jawnej macierzy uprawnień z kategorią operacji zabronionych: disallowedTools nie jest ustawione, więc np. WebFetch i WebSearch trafiają do pytania o zgodę zamiast być zabronione; auto-zatwierdzanie części poleceń powłoki przez klasyfikator SDK przed canUseTool (np. echo) jest opisane tylko w archiwum (FEEDBACK #23, raport §7 pkt 9), nie w dokumentacji szablonu, i nie ma testu tej kolejności. | BL-09 | T15 | brak oceny (kryterium spoza 95) |
| **L11.13** | Zgoda i odmowa są przypisane do konkretnego wykonania; ponowiona odpowiedź nie wykonuje operacji drugi raz. | **niespełnione** | analiza kodu (szablon) | Analiza kodu: POST /api/runs/:id/permission (packages/platform-server/src/http/app.ts) sprawdza tylko właściciela uruchomienia z adresu, a AgentRuntime.answerPermission rozstrzyga oczekującą prośbę po samym requestId z globalnej mapy, bez porównania z runId; powtórna odpowiedź zwraca answered false (wpis usunięty), brak odpowiedzi po 120 s daje odmowę. tests/agui-stream.test.ts „prosba o zgode trafia do rozmowy zadania, nie do ogladanej” dotyczy tylko stanu klienta. Brak testu ponowionej lub spóźnionej odpowiedzi. | Backend nie wiąże odpowiedzi z konkretnym wykonaniem: odpowiedź wysłana na adres uruchomienia A z requestId uruchomienia B (także innego właściciela) rozstrzyga prośbę B, a ochroną jest tylko losowy requestId z Math.random; brak testu, że ponowiona i spóźniona odpowiedź nie wykonuje operacji. | BL-09 | T15 | brak oceny (kryterium spoza 95) |
| **L11.14** | Pomiar Stop rozdziela potwierdzenie żądania, zakończenie strumienia i procesów; po zakończeniu nie występują dalsze mutacje, a kolejka działa. | **częściowe** | test GUI bez modelu (szablon) | e2e/measurements.spec.ts „zatrzymanie z interfejsu konczy wykonanie i nie zostawia zapisow” (skryptowany serwer): czas od kliknięcia do stanu końcowego w backendzie z warunkami i liczbą zdarzeń przed i po, asercja że dziennik zdarzeń nie rośnie po zakończeniu. tests/runtime.test.ts „anulowanie dociera do wykonania i jest mierzalne” (sygnał przerwania synchronicznie, poniżej 50 ms) i „uruchomienie czekajace w kolejce mozna anulowac zanim ruszy”. | Pomiar nie rozdziela potwierdzenia żądania Stop, zakończenia strumienia SSE i zakończenia procesów (jedna liczba do statusu w backendzie, na stand-inie); nie sprawdza braku mutacji domeny po anulowaniu (tylko liczbę zdarzeń) ani tego, że kolejne polecenie w tej samej rozmowie wykonuje się po Stop. | BL-05 | T16 | brak oceny (kryterium spoza 95) |
| **L11.15** | Zamknięcie panelu i utrata sieci odłączają obserwację, a zadanie kontynuuje na backendzie; wyłącznie jawne anulowanie lub udokumentowany warunek zakończenia zatrzymuje wykonanie. | **częściowe** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „przelaczenie rozmowy i przeladowanie nie anuluja zadania”: odpięcie obserwacji przez zmianę rozmowy i przeładowanie nie zmienia statusu w backendzie; tests/background-runs.test.ts „przerwanie odczytu strumienia nie zatrzymuje pracy” (symulacja). Analiza kodu chat/chatWiring.ts: abort strumienia nie jest przekazywany do /cancel, anulowanie tylko przez stopRun. Kontrola siły (przywrócenie mapowania abort na cancel oblewa test) opisana w raporcie §4c, wykonana w AgenticApp. | Nie symulowano utraty sieci ani zamknięcia panelu bez przeładowania; warunki zakończenia bez Stop nie są opisane jako takie: twardy limit APP_RUN_TIMEOUT_MS (domyślnie 300 s) przerywa długie zadanie w tle, a prośba o zgodę bez odpowiedzi przez 120 s kończy się odmową — widać to tylko w kodzie i tabeli konfiguracji README. | BL-09 | T22, T16 | brak oceny (kryterium spoza 95) |
| **L11.16** | Pliki wynikowe są opublikowane atomowo do trwałego magazynu przed sprzątaniem workspace; zerwane zadanie nie publikuje niekompletnego artefaktu jako gotowego. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts (prawdziwy model): wersja pliku opublikowana przez files_publish_version jest pobieralna po zakończeniu przebiegu. Analiza kodu services/files.ts store: bajty zapisywane bezpośrednio pod docelową nazwą, wiersz files wstawiany po zapisie; artifact_publish_file (agent/platform-tools.ts) wykonuje files.store i artifacts.create jako dwa osobne kroki poza wspólną transakcją. Nie obejmuje: przerwania zadania w trakcie publikacji. | Publikacja nie jest atomowa: brak zapisu do pliku tymczasowego z przemianowaniem i brak transakcji obejmującej plik, wiersz files i artefakt, więc awaria między krokami zostawia osierocony plik lub plik bez artefaktu; brak testu, że przerwane zadanie nie publikuje niekompletnego wyniku jako gotowego. | BL-09 | T14 | brak oceny (kryterium spoza 95) |
| **L11.17** | Po przejściu z rozmowy A do B zadanie A nadal działa i zapisuje wynik w A; w B można prowadzić niezależną rozmowę bez mieszania rezultatów. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „przelaczenie rozmowy i przeladowanie nie anuluja zadania”: po przejściu z A do nowej rozmowy zadanie A nadal queued lub running, a pasek stanu nowej rozmowy nie pokazuje przebiegu A. tests/background-runs.test.ts „zadanie w A trwa, gdy uzytkownik pracuje w B, i wyniki sie nie mieszaja” (symulacja: B kończy się, A trwa, tekst A tylko w A, tekst B tylko w B). tests/agui-stream.test.ts „strumien rozmowy B nie dotyka stanu rozmowy A” i „zakonczenie poza ogladana rozmowa jest oznaczone jako niezobaczone”. Zastrzeżenie: e2e „praca w rozmowie B nie miesza sie z zadaniem w A” wysyła polecenie B po restarcie serwera, gdy A już nie trwa, więc równoczesna praca w GUI jest pokazana tylko w pierwszym teście, a zapis wyniku A podczas pracy w B tylko w symulacji. | — | — | T22 | brak oceny (kryterium spoza 95) |
| **L11.18** | Po odświeżeniu lub ponownym połączeniu klient odzyskuje status i wynik zadania bez uruchamiania go drugi raz; backend pracuje także bez otwartego panelu. | **częściowe** | test GUI bez modelu (szablon) | e2e/background-tasks.spec.ts „powrot do rozmowy pokazuje wynik bez ponownego uruchamiania”: po zakończeniu, zmianie rozmowy, powrocie i przeładowaniu odpowiedź widoczna, liczba uruchomień bez zmian; „przelaczenie rozmowy i przeladowanie nie anuluja zadania”: po przeładowaniu w trakcie zadania backend nadal running, lista zadań w tle widoczna. tests/background-runs.test.ts „ponowne podlaczenie od numeru sekwencyjnego nie powtarza zdarzen” i „/api/runs/:id/stream odtwarza zakonczone uruchomienie od kursora”; tests/agui-stream.test.ts „strumien z wyslania zajmuje przebieg, wiec podlaczenie w tle go pomija”. | Brak próby przeładowania w trakcie zadania, po którym klient dołącza do strumienia i pokazuje dokończony wynik w rozmowie A bez podwojenia tekstu (ścieżka, w której wcześniej wystąpiło podwojenie odpowiedzi, raport §4c); utrata i powrót sieci nie są symulowane. | BL-09 | T22 | brak oceny (kryterium spoza 95) |
| **L11.19** | Wykonanie w tle wymagające decyzji ma widoczny sygnał przy swojej rozmowie; brak otwartego panelu nie oznacza automatycznej zgody ani niewidocznego oczekiwania. | **częściowe** | test kontraktu lub logiki (szablon) | tests/agui-stream.test.ts „prosba o zgode trafia do rozmowy zadania, nie do ogladanej” i „prosba o zgode wstrzymuje wykonanie w odroznialnym stanie”: stan awaiting_consent i pendingPermission przypisane do rozmowy zadania. Analiza kodu: shell/BackgroundTasks.tsx pokazuje „czeka na zgode” przy rozmowie; agent/runtime.ts po 120 s bez odpowiedzi zwraca odmowę, nie zgodę. Nie obejmuje: GUI. | Brak testu przeglądarkowego sygnału zgody dla zadania z nieoglądanej rozmowy; status awaiting_consent nie jest zapisywany w agent_runs (tylko w zdarzeniach i pamięci procesu), a widoczność automatycznej odmowy po 120 s przy zamkniętym panelu nie jest sprawdzona, więc oczekiwanie może pozostać niezauważone. | BL-09 | T22, T15 | brak oceny (kryterium spoza 95) |
| **L11.20** | PNG/JPEG, XLSX, CSV i tekst można dołączyć do polecenia, odczytać przez właściwe narzędzie oraz powiązać z odpowiedzią; nieobsługiwany format jest jasno odrzucony. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts (prawdziwy model): PNG i XLSX dołączone przez pole załącznika w kompozytorze i odczytane (obraz przez model, skoroszyt skryptem w sandboxie). e2e/app.spec.ts „odrzucony typ pliku daje czytelny blad” (.exe na stronie plików) i „wgranie poprawnego pliku dziala” (CSV na stronie plików, nie jako załącznik); tests/file-analysis.test.ts „xlsx jest przyjmowany, a macro-enabled i legacy nie”. | JPEG, CSV i tekst nie są sprawdzone jako załączniki polecenia z odczytem przez agenta; odrzucenie nieobsługiwanego formatu sprawdzone tylko na stronie plików, nie w polu załącznika; powiązanie załącznika z odpowiedzią istnieje tylko w trakcie przebiegu (attachFileIds nie są zapisywane przy wiadomości), więc później nie widać, do którego polecenia dołączono plik. | BL-09 | T24 | brak oceny (kryterium spoza 95) |
| **L11.21** | Próba na obrazie potwierdza odczyt jego rzeczywistej treści; znajomość nazwy, MIME lub rozmiaru nie zalicza analizy. | **potwierdzone** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „agent odnosi sie do tresci obrazu, nie do jego nazwy” (prawdziwy model): PNG wygenerowany z trzech pasów kolorów, dołączony pod mylącą nazwą dokument-tekstowy.png; asercje na czerwony, zielony i niebieski w kolejności od lewej, czego nie da się wywnioskować z nazwy, typu MIME ani rozmiaru. Nie obejmuje: JPEG ani tekstu na obrazie. | — | — | T24 | brak oceny (kryterium spoza 95) |
| **L11.22** | Próba XLSX potwierdza odczyt wielu arkuszy i typów komórek, wykonaną zmianę oraz poprawny plik wynikowy; oryginał pozostaje nienaruszony. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „agent zmienia skoroszyt w sandboxie, a oryginal zostaje nietkniety” (prawdziwy model): suma 5300 z arkusza Pozycje (ilość razy cena), nowy arkusz Podsumowanie w pobranym pliku, zachowane arkusze Pozycje, Metryka i Pusty, oryginał identyczny bajt w bajt. tests/file-analysis.test.ts „czyta wszystkie arkusze, a formule oddziela od zapisanej wartosci” (exceljs: arkusze, liczby, tekst, data, wartość logiczna, pusta komórka) i „nowa wersja to nowy plik wskazujacy na oryginal”. | Próba z modelem wymaga odczytu tylko jednego arkusza i nie sprawdza, że agent odczytuje wiele arkuszy i typy komórek (data, wartość logiczna, formuła), ani że typy komórek są zachowane w pliku wynikowym; uszkodzony plik sprawdzony tylko na poziomie biblioteki. | BL-09 | T24 | brak oceny (kryterium spoza 95) |
| **L11.23** | Formuły i zakres zachowania skoroszytu mają jawną semantykę; wynik nie udaje przeliczonego, jeżeli wykonano tylko zapis formuły lub odczyt starej wartości. | **częściowe** | test kontraktu lub logiki (szablon) | tests/file-analysis.test.ts „zapisana wartosc formuly moze byc nieaktualna — i dlatego nie jest wynikiem”: formuła i zapisana wartość rozdzielone, FILE_ANALYSIS.spreadsheet.limits zawiera „formuly NIE sa przeliczane”. Analiza kodu: FILE_ANALYSIS (platform-contracts/src/artifacts.ts) trafia do promptu (agent/prompt.ts) i do interfejsu (chat/ComposerAttachments.tsx); README i docs/NEW-APPLICATION.md §7 opisują zakres zachowania skoroszytu. | Nie sprawdzono na modelu, że odpowiedź agenta nie podaje zapisanej wartości formuły jako wyniku przeliczenia; e2e/files-agent.spec.ts akceptuje w B1 zarówno liczbę, jak i komórkę formuły z zapisanym result, więc nie odróżnia wyniku policzonego od samego zapisu formuły; utrata wykresów i formatowania przy zapisie nie ma testu. | BL-09 | T24 | brak oceny (kryterium spoza 95) |
| **L11.24** | Plik przetworzony w sandboxie jest widoczny jako artefakt z podglądem i pobraniem, także po zmianie rozmowy i restarcie; kontrola dostępu obejmuje wejście i wynik. | **częściowe** | rzeczywisty model (szablon) | e2e/files-agent.spec.ts „wynik jest dostepny po powrocie do rozmowy”: plik wersji widoczny na ekranie /files i pobieralny (status 200). tests/file-analysis.test.ts „cudzy plik jest niedostepny do odczytu i do wersjonowania” (dostęp do wejścia i do tworzenia wyniku). Analiza kodu: FileService.read sprawdza właściciela także dla pliku wynikowego, attachFileIds przechodzą przez files.meta z ownerId. | Wynik files_publish_version jest wersją pliku, a nie artefaktem, więc nie ma podglądu w zakładce artefaktów rozmowy; test nie wraca do rozmowy (przechodzi na /files), nie sprawdza zmiany rozmowy ani restartu, a odmowa odczytu cudzego pliku wynikowego przez HTTP nie jest testowana. | BL-09 | T24, T14 | brak oceny (kryterium spoza 95) |

### Warstwa 12 — Obserwowalność i odbiór integracji

| ID | Wymaganie (ze specyfikacji) | Stan w szablonie | Rodzaj dowodu | Zakres i odniesienie | Brak | Backlog | Próby | Ocena historyczna AgenticApp |
|---|---|---|---|---|---|---|---|---|
| **L12.1** | Rozmowę można powiązać z wykonaniem, narzędziem, mutacją i artefaktem w danych diagnostycznych. | **częściowe** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts (prawdziwy model): rozmowa ma claudeSessionId, /api/conversations/:id/runs zwraca uruchomienie, /api/runs/:id/events zawiera TOOL_CALL_START i TOOL_CALL_RESULT narzędzia mcp__app__, historia wiąże toolCalls z wiadomością tool po toolCallId. Analiza kodu: zdarzenia CUSTOM canvas_changed, data_changed i artifact_created (z artifactId) zapisywane w run_events przed emisją (agent/events.ts), artifacts.conversation_id. | Żaden test nie odczytuje z danych diagnostycznych powiązania rozmowa–wykonanie–mutacja–artefakt; artefakt ma conversation_id, ale nie run_id, a mutacja domenowa jest identyfikowalna tylko z argumentów i wyniku narzędzia oraz zdarzenia data_changed bez identyfikatorów i wersji rekordów. | BL-05 | T21 | potwierdzone — przebieg: baza instancji audytowej (conversations, agent_runs, run_events, artifacts); bez testu w repozytorium |
| **L12.2** | Błędy integracji, domeny, modelu i sandboxu są rozróżnialne; sekrety nie występują w logach. | **częściowe** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts (validation_failed, domain_rule_violated, conflict, forbidden, 401), tests/auth.test.ts (klasy błędów dostępu, „limit uzycia nie jest mylony z zepsutym logowaniem”), tests/run-lifecycle.test.ts „blad modelu konczy sie dokladnie jednym stanem koncowym i zapisuje przyczyne” (rate_limited). Sekrety: tests/durability.test.ts „zbudowany frontend nie zawiera wartosci tokena”, „zbudowany backend nie zawiera wartosci tokena”, „baza danych aplikacji nie zawiera wartosci tokena”, „diagnostyka uruchomieniowa nie wypisuje tokena” (wyjście pnpm migrate); tests/contracts.test.ts „nie ujawnia sekretow w statusie”. Testy wycieku tokena są pomijane, gdy na maszynie brak poświadczenia. | Kod model_failed nie jest nigdzie emitowany: ogólny błąd modelu i błąd integracji dostają ten sam kod integration_failed; sandbox_denied z przebiegu wynika tylko z dopasowania słowa sandbox w komunikacie i nie ma testu; logi serwera podczas przebiegu z modelem nie są skanowane na obecność sekretów. | BL-05 | T21 | potwierdzone — test: contracts.test.ts i raw-probe-stop.log; brak testu rozróżnienia błędu modelu od integracji i sandboxu |
| **L12.3** | Zmierzone są czas pierwszej odpowiedzi, wykonania, odświeżenia po mutacji i anulowania, z podaniem warunków pomiaru. | **częściowe** | rzeczywisty model (szablon) | e2e/measurements.spec.ts „zmiana pozycji oferty jest widoczna w zestawieniu bez przeladowania” i „zatrzymanie z interfejsu konczy wykonanie i nie zostawia zapisow”: czasy odświeżenia i anulowania zapisane z opisem od–do i warunkami (build produkcyjny, Chromium, jeden worker, stand-in przy anulowaniu). e2e/agent-ui.spec.ts (prawdziwy model): firstTokenMs większy od 0 i nie większy od durationMs, queuedMs nieujemny. tests/run-lifecycle.test.ts (symulacja) dla kolejności narzędzie–tekst. | Czas pierwszej odpowiedzi i wykonania na prawdziwym modelu jest tylko asertowany, nie raportowany jako pomiar z warunkami; anulowanie mierzone na stand-inie, bez procesu SDK; wyniki trafiają do katalogu o historycznej nazwie docs/evidence/closure-2026-09-15 bez wersji kodu. | BL-05 | T21 | potwierdzone — test: run-lifecycle (13) i 20-pomiary.json; anulowanie mierzone na scenariuszu bez procesu modelu |
| **L12.4** | Testy kontraktów obejmują walidację, konflikty, powtórzenia i kontrolę dostępu. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/contracts.test.ts: walidacja („odrzuca wejscie narzedzia niezgodne ze schematem”, „odrzuca ujemna cene regula domenowa, nie schematem”, „odrzuca nieznany komponent karty”), konflikty („nie nadpisuje nowszych danych przy nieaktualnej wersji”, „konflikt kompozycji karty nie niszczy ostatniego poprawnego ukladu”), powtórzenia („powtorzona operacja z tym samym operationId nie dubluje skutku”, „dodanie karty z tym samym operationId nie tworzy dwoch kart”, „powtorzona wiadomosc o tym samym id nie tworzy drugiego wpisu”), dostęp („drugi uzytkownik nie widzi cudzej sprawy — mimo poprawnego identyfikatora”, „identyfikator wlasciciela z ciala zadania nie daje dostepu”, „bez sesji aplikacji zwraca 401”, „odrzuca zadanie z niedozwolonego origin”). Asercje sprawdzają kody błędów i stan po operacji. Nie obejmuje: równoczesnych powtórzeń. | — | — | — | potwierdzone — test: contracts.test.ts (pnpm test 69/69 w AgenticApp) |
| **L12.5** | Testy przeglądarkowe obejmują dynamiczny UI, rozmowy, narzędzia, artefakty i wznowienie. | **częściowe** | rzeczywisty model (szablon) | Testy Playwright w e2e: dynamiczny UI (app.spec.ts canvas i karty, ui-navigation.spec.ts), rozmowy (chat.spec.ts, chat-drawer.spec.ts, session-restore.spec.ts), narzędzia (tool-activity.spec.ts: wywołanie, wynik, błąd, restart backendu), wznowienie (session-restore.spec.ts, background-tasks.spec.ts), ścieżka z prawdziwym modelem (agent-ui.spec.ts, files-agent.spec.ts). Każdy plik używa e2e/support/fixtures.ts z izolacją instancji. | Artefakty nie są pokryte: e2e/chat-layout.spec.ts „artefakty sa osobna zakladka panelu” sprawdza tylko przełączenie zakładki, e2e/chat.spec.ts „usuniecie rozmowy odlacza artefakty zamiast je kasowac” używa wyłącznie API; żaden test nie otwiera artefaktu, jego podglądu, pełnego widoku ani stanu live w przeglądarce. | BL-12 | T05, T19 | potwierdzone — przebieg: 46 testów e2e w AgenticApp; bez testu otwarcia artefaktu w przeglądarce |
| **L12.6** | Rzeczywista ścieżka subskrypcja Claude → SDK → Mastra → AG-UI → OpenUI została potwierdzona; mocki są oznaczone osobno. | **potwierdzone** | rzeczywisty model (szablon) | e2e/agent-ui.spec.ts „polecenie → narzedzie → mutacja → canvas → strumien → trwalosc” i e2e/files-agent.spec.ts na prawdziwym modelu subskrypcyjnym: polecenie w GUI, AgentRuntime przez Mastra (ClaudeSDKAgent z @mastra/claude) i Claude Agent SDK, zdarzenia AG-UI z /api/agui/run parsowane przez agUIAdapter OpenUI, narzędzie mcp__app__, przyrost tekstu w stronie, karta na canvasie. Zmienne płatnego API usuwane z procesu agenta (tests/durability.test.ts „srodowisko przekazywane agentowi nie niesie zmiennych platnego dostepu”, tests/auth.test.ts). Stand-iny oznaczone osobno: e2e/support/scripted-server.ts, opisy suit „scenariusz zamiast modelu”, symulacje w tests/run-lifecycle.test.ts i tests/background-runs.test.ts. Zastrzeżenia: AG-UI kodowane własnym kodem (bez @ag-ui/mastra); status zależy od przejścia obu suit z modelem w regresji. | — | — | T05, T21 | potwierdzone — przebieg: przebiegi audytu na modelu; AG-UI jako własne kodowanie zdarzeń, bez @ag-ui/mastra |
| **L12.7** | Opis odbioru wskazuje wersje, dowody, nieudane próby, brakujące możliwości i własne adaptery. | **częściowe** | analiza kodu (szablon) | docs/CONSOLIDATION-REPORT.md: wersje i środowisko przebiegów, dowody z odnośnikami, nieudane przebiegi (§5.1, §5.3), różnice kodu (§3), braki rozdzielone na implementację, brak próby, dostęp i decyzje (§6); docs/ACCEPTANCE.md i docs/BACKLOG.md generowane przez scripts/acceptance-matrix.mjs; docs/NEW-APPLICATION.md §7 (pułapki i własne obejścia: most hooków SDK, platformAdapter.ts, portal kompozytora); docs/observability.md (wersje Mastry i SDK). | Brak aktualnego, pełnego rejestru własnych adapterów szablonu z opisem zakresu i próby zgodności (pełny rejestr jest tylko w archiwalnym FEEDBACK §6 i nie obejmuje nowszych adapterów, np. ConversationSync, ComposerAttachments, useComposerStop, runStreams); wersje wszystkich kluczowych zależności nie są zebrane w jednym opisie odbioru. | BL-12 | — | potwierdzone — analiza kodu: RAPORT-STANU-PLATFORMY.md w AgenticApp; rejestr adapterów tylko w archiwalnym FEEDBACK §6 |
| **L12.8** | System działa bez Langfuse; możliwość eksportu i ewentualne ograniczenia kompatybilności są udokumentowane. | **potwierdzone** | test kontraktu lub logiki (szablon) | tests/observability.test.ts „bez konfiguracji telemetrii aplikacja dziala i nic nie wysyla”, „pakiet @mastra/observability nie jest zainstalowany”, „zadna zaleznosc Langfuse nie jest zainstalowana”, „surowy obiekt eksportera jest odrzucany, a obserwowalnosc wylaczana”; cała regresja działa bez Langfuse. docs/observability.md opisuje punkt wpięcia (Observability z @mastra/observability), kroki podłączenia i ograniczenia (brak zdarzeń narzędzi w telemetrii Mastry, wysyłka danych poza maszynę, niesprawdzona zgodność schematu Langfuse). Nie obejmuje: próby eksportu. | — | — | T21 | potwierdzone — test: observability.test.ts i docs/observability.md; zgodność Langfuse z tą wersją Mastry niesprawdzona |
| **L12.9** | Kryteria mają unikalne identyfikatory; liczby i statusy warstw są liczone z aktualnej macierzy, ze sprawdzeniem braków, duplikatów i zmiany wymagań. | **potwierdzone** | analiza kodu (szablon) | scripts/acceptance-matrix.mjs w pnpm verify (pnpm check:acceptance): kryteria i próby czytane z docs/ARCHITECTURE.md; sprawdza kolejność i unikalność identyfikatorów, 12 warstw / 200 kryteriów / 27 prób, braki i nadmiar ocen, zgodność próby z warstwą, opis braku i pakiet dla otwartych kryteriów, zakaz „potwierdzone” bez dowodu z szablonu; sumy, zamknięte warstwy i backlog są wyliczane; --check wykrywa dryf docs/ACCEPTANCE.md i docs/BACKLOG.md (także zmianę brzmienia wymagań). Kontrole negatywne: docs/evidence/template-consolidation/kontrole-negatywne-macierzy.txt. Nie obejmuje: zmiana brzmienia kryterium wymusza ponowne wygenerowanie, ale nie ponowną ocenę. | — | — | — | brak oceny (kryterium spoza 95) |
| **L12.10** | Każdy dowód wskazuje wersję kodu, środowisko i rodzaj wykonania; dawny wynik nie potwierdza automatycznie zmienionej integracji. | **częściowe** | analiza kodu (szablon) | scripts/acceptance-matrix.mjs rozdziela rodzaj dowodu i pochodzenie (szablon lub historyczne), odrzuca „potwierdzone” bez dowodu z szablonu i pokazuje ocenę historyczną osobno; meta.codeState podaje jeden stan kodu dla całej macierzy. e2e/support/isolation.ts i tests/isolation.test.ts identyfikują instancję testową przed mutacją. | Poszczególne dowody nie wskazują wersji kodu ani środowiska: pliki wynikowe e2e (20-pomiary.json, 21-strumien.json, 22-strumien-model.json) nie zapisują commitu, wersji SDK ani modelu i trafiają do katalogu o historycznej nazwie docs/evidence/closure-2026-09-15, przez co wynik z szablonu wygląda jak dowód z AgenticApp. | BL-12 | T19 | brak oceny (kryterium spoza 95) |
| **L12.11** | Próba streamingu ma kontrolę negatywną: pełna odpowiedź na końcu i narzędzie bez tekstu nie zaliczają przyrostowej odpowiedzi. | **potwierdzone** | test GUI bez modelu (szablon) | e2e/streaming.spec.ts (skryptowany serwer, detektor judgeStream z sondą w stronie): „tekst dociera fragmentami i jest widoczny przed koncem wykonania” (co najmniej dwie różne, rosnące długości w fazie queued lub running), „cala odpowiedz naraz na koncu jest rozpoznana jako brak strumieniowania” (streamed false mimo dotarcia odpowiedzi), „wiadomosc uzytkownika i podpowiedzi nie zaliczaja asercji strumieniowania” (narzędzie bez tekstu: brak próbek odpowiedzi). tests/stream-verdict.test.ts (13) dla reguły orzekania; e2e/agent-ui.spec.ts używa tego samego detektora na modelu z unikalnym znacznikiem. | — | — | T05 | brak oceny (kryterium spoza 95) |
| **L12.12** | Testy krytycznych napraw wykazują zdolność wykrycia defektu przez kontrolowany wadliwy wariant lub adekwatną próbę negatywną; samo przejście nie wystarcza. | **częściowe** | test GUI bez modelu (szablon) | Wbudowane kontrole negatywne wykonywane w regresji: e2e/streaming.spec.ts (odpowiedź naraz, narzędzie bez tekstu), tests/stream-verdict.test.ts, tests/agui-stream.test.ts „dwukrotne zastosowanie tego samego zdarzenia podwaja tekst — dlatego jeden konsument”, tests/access-context.test.ts „odpowiedz, ktora mimo wszystko dobiegnie, jest odrzucana po epoce” (fetch ignorujący abort), tests/migration.test.ts „powtorne uruchomienie nie dubluje danych”, tests/live-artifacts.test.ts (asercja, że zmiana źródła zmieniła wynik), tests/observability.test.ts (surowy eksporter wyłącza obserwowalność), e2e/agent-ui.spec.ts (usunięcie zastanej karty przed testem). Cofnięcia napraw (raport §5 „Kontrola siły testów”) wykonano w AgenticApp; kontrole negatywne check-boundaries opisane w FEEDBACK.md T1 wykonano ręcznie na kopii. | Kontrolowane wadliwe warianty krytycznych napraw (szuflada D-1, projekcja D-2, zakleszczenie N-3, mapowanie abort na cancel w zadaniach w tle, detektor strumienia, guard izolacji) nie są powtarzalne w szablonie; brak skryptu lub testu wykazującego, że testy tych napraw oblewają na wadliwym wariancie, a części napraw nie chroni wbudowana próba negatywna. | BL-12 | T05, T19, T21 | brak oceny (kryterium spoza 95) |
| **L12.13** | Pomiary oddzielają czas kolejki, start wykonania, pierwszy tekst, zakończenie, widoczny refetch i faktyczne anulowanie; brak tekstu ma poprawny brak metryki. | **częściowe** | rzeczywisty model (szablon) | tests/run-lifecycle.test.ts (symulacja): „narzedzie przed tekstem — czas pierwszego tekstu jest zapisany”, „tekst przed narzedziem — czas pierwszego tekstu jest zapisany”, „odpowiedz bez tekstu — brak czasu pierwszego tekstu jest poprawny, nie podstawiony”, „czas pierwszego tekstu liczy sie od startu wykonania, nie od zakolejkowania”; tests/runtime.test.ts „start wykonania jest osobnym, mierzalnym punktem wzgledem zakolejkowania” (queuedMs); e2e/measurements.spec.ts: widoczny refetch po mutacji i czas do stanu końcowego po Stop; e2e/agent-ui.spec.ts (model): firstTokenMs większy od 0 w kolejności narzędzie–tekst. | Faktyczne anulowanie nie jest mierzone: pomiar kończy się na statusie w backendzie na stand-inie, bez zakończenia strumienia i procesów SDK; punkty kolejki, startu, pierwszego tekstu i końca z przebiegu na modelu są tylko w rekordzie uruchomienia, bez raportu pomiaru. | BL-05 | T21, T05 | brak oceny (kryterium spoza 95) |
| **L12.14** | Raport zawiera nieudane przebiegi i ponowienia na tym samym kodzie; korzystny wynik nie zastępuje wyjaśnienia niestabilnej próby. | **potwierdzone** | analiza kodu (szablon) | docs/CONSOLIDATION-REPORT.md §5.1 (czysty klon bf83bb6: pnpm verify = 1 i pnpm test:e2e = 1, przyczyna i poprawka w 7d6bd78, surowe logi w docs/evidence/template-consolidation/przebieg-1-bf83bb6-nieudany/), §5.3 (trzy nieudane próby wymiany modułu przed udaną: dwa błędy skryptu i rzeczywista wada typowanych tras), §5.2 (kontener: nieudany dostęp z hosta z kontrolą środowiska); FEEDBACK.md T1. playwright.config.ts ma retries 0, więc niestabilność nie jest maskowana ponowieniem; przebieg 2 nie miał ponowień. Aktualizacja 2026-09-17 (docs/CONSOLIDATION-UPDATE-2026-09-17.md): pierwsze uruchomienie sondy ui_filter nie wystartowało (config sondy poza pakietem ESM — błąd narzędzia próby, log 09-sonda-ui-filter-model-proba1-blad-configu.txt), drugie przeszło; w regresji d142b85 brak oblań i ponowień. | — | — | — | brak oceny (kryterium spoza 95) |
| **L12.15** | Własne adaptery i ograniczenia są opisane zgodnie z działaniem; niespełniony obowiązek nie staje się zaliczony przez przeniesienie do sekcji ograniczeń. | **częściowe** | analiza kodu (szablon) | AGENTS.md: niespełnione wymaganie nie staje się spełnione przez przeniesienie do ograniczeń; scripts/acceptance-matrix.mjs wymaga dla każdego otwartego kryterium opisu braku i pakietu backlogu, więc ograniczenie nie zamyka kryterium. Ograniczenia opisane w README („Najważniejsze ograniczenia”), docs/NEW-APPLICATION.md §7, docs/observability.md, docs/odzyskiwanie-stanu.md (znane ograniczenie próby migracji). | Część opisów nie odpowiada wykonanym dowodom: README deklaruje, że zamknięcie panelu nie anuluje pracy i że załączniki JPEG, CSV i tekst działają w polu wiadomości, a ekran Ustawień (SettingsPage.tsx) twierdzi, że sandbox odcina sieć i katalog danych — bez próby w szablonie; rejestr własnych adapterów istnieje tylko w archiwum. | BL-12 | — | brak oceny (kryterium spoza 95) |
| **L12.16** | Jeżeli eksport telemetrii jest włączony, potwierdzony jest rzeczywisty odbiór śladu; brak wyjątku konstruktora nie dowodzi aktywnego eksportera. | **potwierdzone** | test kontraktu lub logiki (szablon) | Kryterium warunkowe, warunek nie zachodzi: eksport telemetrii jest wyłączony, co potwierdzają tests/observability.test.ts „pakiet @mastra/observability nie jest zainstalowany” i „zadna zaleznosc Langfuse nie jest zainstalowana”. Druga część treści pokryta testem „surowy obiekt eksportera jest odrzucany, a obserwowalnosc wylaczana”: brak wyjątku konstruktora Mastry nie jest traktowany jako aktywny eksporter (asercja na ostrzeżenie „Expected an Observability instance”). docs/observability.md stwierdza, że odbioru śladu nie sprawdzano. Włączenie eksportu wymaga nowej próby rzeczywistego odbioru śladu. | — | — | T21 | brak oceny (kryterium spoza 95) |
| **L12.17** | Końcowa ocena rozdziela ukończenie implementacji, odbiór w izolacji i uruchomienie z migracją na danych użytkownika. | **potwierdzone** | analiza kodu (szablon) | docs/CONSOLIDATION-REPORT.md §0 i §6 rozdzielają: stan implementacji (macierz, braki implementacji), odbiór w izolacji (regresja w czystej kopii na portach testowych i efemerycznych, §5) oraz migrację na danych użytkownika — niewykonaną w tej fazie (§6.4, §7); README „Stan: bazowa wersja robocza”; docs/odzyskiwanie-stanu.md oddziela próbę na kopii od uruchomienia na danych. | — | — | — | brak oceny (kryterium spoza 95) |
